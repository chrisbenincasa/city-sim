# raised.toml -- founded.toml with five [[life_stage]] tables, and nothing else changed.
#
# ⚠ THEY ARE aged.toml'S TABLES AS THEY STOOD BEFORE 2026-08-31 AND NOT AS THEY STAND NOW.
# plans/0045 row 13 halved that file's stage floors to double their spreads, and the edit was
# scoped there on purpose (plans/0046). The numbers below are not a second opinion about a life:
# they are the old ones, unre-measured, and this file demonstrates founding rather than pacing.
#
# THE ONLY SHIPPED FILE IN WHICH A CITY BOTH GENERATES ITS OWN POPULATION AND FOUNDS BUSINESSES, and
# it exists for one reason: plans/0046 stage 4's working-age gate has TWO callers and no world could
# reach the second. `EmploymentEngine.Assign` and `PlacementEngine.Found` both ask
# `World.IsOfWorkingAge`; aged.toml has demographics and no [founding], founded.toml has [founding]
# and no demographics. ***So the founding half of the gate was correct and unobservable*** -- which
# is plans/0034 F17's shape exactly, a mechanism shipped right and unreachable for want of Ruleset
# CONTENT rather than code. This file is the content.
#
# WHY IT IS founded.toml AND NOT aged.toml PLUS A CHANNEL. Founding is a MEANS TEST: it reads the
# Household's balance against a band, and every Household on minimal.toml -- which aged.toml is built
# from -- holds exactly zero. A gate on a channel that refuses everybody anyway would demonstrate
# nothing. founded.toml is taxed.toml underneath and puts money in Households, which is what makes
# the refusal attributable to AGE rather than to poverty.
#
# ⚠ IT INHERITS founded.toml'S OWN WARNINGS AND THEY STILL APPLY. Its shop count is bounded by the
# SOURCE EXHAUSTING rather than by any sink, so DO NOT QUOTE A MONEY FIGURE OUT OF A RUN OF THIS AS
# ANYTHING ABOUT THE ECONOMY. Read that file's header before reading this one's numbers.
#
# ⚠ AND IT INHERITS aged.toml'S: the fertility draw is UNCONDITIONED (adr/0011 wants it conditioned
# on housing cost, dwelling size and job security, and none of that machinery exists), the band's
# mean of 1.5 sits below the 2.0 that replaces exactly, and the founding cohort's echo does not damp.
#
# 🔴 SO THIS FILE RATIFIES NOTHING. It is a reachability fixture: it makes one branch executable that
# no other world could reach. Every number in it is somebody else's, unratified, and carried here so
# the diff against founded.toml is exactly the stage table.
#
# ⚠ `gives_up_after_days` IS ALREADY HERE and is founded.toml's own, at 30 Days. It happens to
# satisfy the refusal plans/0046 stage 3 added for a spawning Ruleset -- a Pool with a door and no
# sink grows without bound, and internal generation is a door. The key was written for the
# unpremised pool's bound and it covers both, which is a coincidence worth naming rather than a
# design: adr/0144 has a Business share the Household's bound as a declared stand-in.

# The keys below are minimal.toml's and its comments carry their arguments -- read them there.
# What is commented HERE is what this file changes and why.

[[resource]]
name = "sundries"
family = "good"

[[resource]]
name = "repairs"
family = "good"

[[resource]]
name = "money"
family = "money"

[[business]]
name = "shop"
jobs = 8
shift_start_earliest_hour = 6
shift_start_latest_hour   = 10

[[building]]
name = "dwelling"
bins = [
    { resource = "sundries", capacity = 48, owner = "occupant" },
    { resource = "repairs",  capacity = 4 },
]
occupants = 4
business = "shop"

[[rule]]
name    = "restock"
kind    = "dwelling"
rate    = 8
apply   = { min = 1, max = 4 }
inputs  = []
outputs = [ { scope = "local", resource = "sundries", amount = 4 } ]

[[rule]]
name    = "consume"
kind    = "dwelling"
rate    = 32
apply   = { min = 1, max = 1 }
inputs  = [ { scope = "local", resource = "sundries", amount = 4 } ]
outputs = []

[[rule]]
name    = "upkeep"
kind    = "dwelling"
rate    = 16
apply   = { min = 1, max = 1 }
inputs  = [ { scope = "local", resource = "repairs", amount = 4 } ]
outputs = []

[[zone_rule]]
name          = "housing"
kind          = "dwelling"
zone          = 0
interval      = 32
revisit_ticks = 2048

[placement]
interval      = 32
revisit_ticks = 1024
candidates    = 3
# ADDITION 3 OF 3 -- THE SINK, and the loader refuses this file without it. See the header: [founding]
# is an inflow into the unpremised pool and nothing tenants a Business yet, so a bound is what keeps
# adr/0006 satisfied. It is [placement]'s key because adr/0144 has a Business SHARE the Household's
# bound as a declared stand-in rather than author a second one nothing can ratify.
gives_up_after_days = 30
# ADDITION 1 OF 3 -- THE TRADES. adr/0141's own example, and the same two tenanted.toml names. Two
# rather than one because a single trade could not show that the choice is drawn at all.
#
# BOTH EMPLOY SOMEBODY AND THE LOADER REFUSES THIS FILE OTHERWISE. adr/0146: a founder becomes their
# Business's first worker, which is the whole of the labour cost milestone 27 ships -- so a trade
# nobody can work at is a trade nobody can found. The trade is drawn UNIFORMLY, so it is every
# declared one rather than at least one.
#
# THE NUMBERS ARE THE DWELLING'S OWN AND ARE NOT A SECOND CLAIM. Eight is what `shop` above declares,
# derived from the row ratios; the band 6-10 is the same ordinary spread of opening times. A founded
# shop that employed a DIFFERENT number from the shop a dwelling comes with would be asserting
# something about scale that nothing in this file measures.
#
# ⚠ THREE IS DELIBERATE ON `barber` and it is the one number here that is not a copy: a trade the city
# founds and a trade the city is built out of do not have to be the same size, and a file in which
# every trade employs eight could not show that the ceiling is read per trade at all.
[[business]]
name                      = "bakery"
jobs                      = 8
shift_start_earliest_hour = 6
shift_start_latest_hour   = 10

[[business]]
name                      = "barber"
jobs                      = 3
shift_start_earliest_hour = 9
shift_start_latest_hour   = 9
# ADDITION 2 OF 3 -- THE CHANNEL. adr/0145 and its amendment. There is no demand key here and that
# absence IS the decision: a Household founds on its own MEANS and never on the city's NEED, so
# nothing in this table reads shop count, vacancy or anything unsold.
[founding]
founding_band    = 400
reconsider_ticks = 8192

[layers]
noise_range_metres            = 300
noise_intensity_percent       = 400
desirability_pollution_percent = 100
desirability_noise_percent     = 100
pollution_period      = 64
pollution_offset      = 0
land_value_period     = 256
land_value_offset     = 16
kernel_metres         = 1024
pollution_decay_ticks = 2048
land_value_tau        = 8

[roads]
block_tiles                    = 32
arterial_count                 = 0
arterial_junction_tiles        = 512
foot_crossing_every            = 4
foot_paths_per_thousand_blocks = 40
street_speed_kph               = 50
arterial_speed_kph             = 90
walk_speed_kph                 = 5
street_capacity_per_hour       = 3600
arterial_capacity_per_hour     = 12000
foot_path_capacity_per_hour    = 1000

[lots]
lots_per_segment = 5

[trips]
crossing_seconds = 30
commute_fast_minutes = 20
commute_moderate_minutes = 40
commute_budget_minutes = 50

[jobs]
interval      = 32
revisit_ticks = 1024
candidates    = 3
shift_hours_min = 6
shift_hours_max = 10
arrive_early_max_minutes = 15
# ===========================================================================================
# ADDITION 1 OF 3 -- the money in the city. Nothing above this line moved.

[households]
# NOBODY DRIVES, which is minimal.toml's own state said out loud rather than by omission. The table
# has to exist to carry the two keys below and car_ownership_percent is required inside it, so 0 is
# the value that leaves this file behaving exactly as minimal.toml does. adr/0098: 0 is a legitimate
# city -- one before the motor car -- and is not a placeholder.
car_ownership_percent = 0
# WHAT A HOUSEHOLD IS FOUNDED WITH, drawn uniformly per Household at creation and issued through
# World.Endow. See the header for why this is fixture content rather than adr/0116's deferred
# founding balance, and why it is a band.
#
# THE FLOOR IS ZERO ON PURPOSE. A Household that opens with nothing is a Household the levy takes
# nothing from and the rebate lifts, which is the first thing worth watching in this circuit -- and
# it is what makes `floored to zero` read non-zero on the first sweep instead of being a counter
# nobody has ever seen fire.
opening_balance_min = 0
opening_balance_max = 1000
# ===========================================================================================
# ADDITION 2 OF 3 -- the levy. DECLARED FIRST, and the order is load-bearing: a Policy acts where it
# runs (there is no proposal and no settle), so the levy declared above the rebate funds it on the
# same trigger. The same pair in the other order pays out of last Day's remainder.

[[policy]]
name = "household_levy"
sweeps = "household"
interval = 2048
apply = { derived = "balance", percent = 10 }
transfer = { from = "local", to = "global", resource = "money", amount = 1 }
# ===========================================================================================
# ADDITION 3 OF 3 -- the rebate. A FLAT amount, which CONTEXT -> Policy prefers against because a
# flat amount goes stale as the economy grows. It is flat here anyway and the reason is that the
# alternative is worse: a percentage paid OUT of a balance pays the rich most, which is a mechanic
# nobody would author. What CONTEXT actually asks for -- a percentage of gross income below a line --
# needs an income (milestone 15) and a poverty line (a hash-bearing number with no ratifier), and
# adr/0070 says the answer to an unbuilt mechanism is to build it rather than to approximate it.

[[policy]]
name = "household_rebate"
sweeps = "household"
interval = 2048
apply = { min = 100, max = 100 }
transfer = { from = "global", to = "local", resource = "money", amount = 1 }
# THE FERTILITY DECISION, and the band is the whole of it. adr/0011: "Decision: how many children,
# possibly zero. Zero sends the Household to Childless; otherwise to Family."
#
# 0..3 STRADDLES REPLACEMENT ON PURPOSE. Two children per Household is EXACT Citizen replacement --
# two adults replaced by two -- and adr/0011 is emphatic that the threshold "falls out of
# conservation rather than being chosen". A band whose whole range sat above or below 2 would make
# the outcome a restatement of this file; one straddling it makes Replacement Rate a reading of the
# city. The mean of 0..3 is 1.5, so THIS WORLD IS BELOW REPLACEMENT BY CONSTRUCTION and the
# population declines. That is a property of the band and not a finding about cities.
#
# 🔴 THE DRAW IS UNCONDITIONED. adr/0011 conditions fertility on housing cost, dwelling size and job
# security, and the discrete-choice machinery all three would read is UNBUILT -- adr/0070, which
# says that is a mechanism nobody has written rather than evidence about what fertility should be.
# So do not read a number out of a run of this file as a statement about affordability.
#
# UNRATIFIED, chosen by taste, no plans/0002 §D row. plans/0045 suspends adr/0052 to allow that.
[[life_stage]]
name          = "young"
duration_days = 24
spread_days   = 8
next          = "family"
childless     = "childless"
children_min  = 0
children_max  = 3
# The age an adult carries, in Days, drawn on formation and NEVER ADVANCED -- adr/0011, and adr/0010
# is why: real ageing is arithmetically impossible under one clock. It is stated HERE because this is
# the stage children become adults in, and the loader refuses a children_become target without it.
#
# ⚠ 1..160 IS THE LENGTH OF A LIFE IN THIS FILE and not a human age. A full chain is ~160 Days at the
# floor, so the band spans one lifetime -- which is the only scale this world has. There is no
# calendar and CONTEXT.md bans inventing one, so a number that looked like human years would be a
# unit this project does not have.
#
# ⚠ ZERO IS NOT IN THE BAND and cannot be: zero is what a CHILD carries, and it is the only marker of
# childhood there is. plans/0046 stage 4 is the gate that reads it.
adult_age_min_days = 1
adult_age_max_days = 160

[[life_stage]]
name          = "family"
duration_days = 48
spread_days   = 16
next          = "mature_family"
# adr/0011: "On exit the children become adults and form new Young Households, entering the Unplaced
# Pool." That is stage 3 and it is why THIS row is the one to watch when generation lands -- under
# stage 1 its exit is an ordinary scheduled transition and spawns nobody.
[[life_stage]]
name           = "mature_family"
duration_days  = 48
spread_days    = 16
next           = "empty_nest"
children_become = "young"
# Adults who never had children. REACHABLE AS OF STAGE 3, and it is `young`'s zero draw that reaches
# it. Under stages 1 and 2 nothing routed into it at all, because the only
# thing that would is the fertility decision. Terminal, like empty_nest, and kept separate from it
# because they are different diagnoses -- see the header.
[[life_stage]]
name          = "childless"
duration_days = 40
spread_days   = 16
# Adults whose children have left. Terminal under stage 1, which is why a long run of this file ends
# with the whole city here. Dissolution is stage 2.
[[life_stage]]
name          = "empty_nest"
duration_days = 40
spread_days   = 16
