<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Shell-side appearance: Building geometry and Appearance Family selection. It never
    references Godot, so the headless runner can call it (procedural-buildings session, Q5), and
    it never enters Borough.Core or the State Hash (adr/0150). Floating point is allowed here.
  -->

  <ItemGroup>
    <ProjectReference Include="..\Borough.Formats\Borough.Formats.csproj" />
  </ItemGroup>

</Project>
