# 0049 — The visuals queue

Scoped 2026-09-01, against `main` at `790267e`. **The renderer's own document, and it iterates
separately from the simulation on purpose.**

[`0045`](0045-amnesty.md)'s *Situation* names the hole this fills in five words — ***no renderer, no
plan for one*** — and it has been true since the first commit. The shell draws a city; nothing has
ever said what the city is supposed to look like, so every visual decision has been taken inside a
method by whoever was there.

---

## Why this is a separate queue

**Every row here is shippable alone, and all but one moves no State Hash.** That is the property
worth protecting: the drawing can be worked on for a week without the simulation moving, and the
simulation can move without invalidating a row.

⚠ **The one exception is marked**, and it is marked because it is a Ruleset change wearing a visual
motive — `block_tiles` and `occupants` are the city's own numbers, and a skyline is what you get
rather than what you asked for.

**Three standing rules, and the first two already exist.**

1. **The renderer cannot influence the simulation** (`adr/0007`, `05 §4`). Nothing in this queue may
   feed back. A frame is a reading.
2. **Invented geometry is labelled as invention.** `PlotDepthMetres` already carries the sentence —
   *a thickness the city does not have, invented so the picture reads as a city rather than as a bar
   chart* — and every row below that adds geometry inherits it. ⚠ **It draws on no `purpose_tag` and
   must not**: the simulation's stream is for decisions, and a shape nobody in the city can perceive
   is not one.
3. **A row is done when somebody watched it and something surprised them** ([`0045`](0045-amnesty.md)'s
   amended Definition of done). `plans/0048`'s `--drive` is how a row is watched twice.

---

## The queue

| # | Row | Hash | Status |
|---|---|---|---|
| **1** | **Fill factor and roofline.** A Building filled 72–98% of its Lot's frontage, so a block face was two slabs meeting at the corners. Widen the band, and give it a roof | no | ✅ 01-09 |
| **2** | **The ground is drawn from the ground.** Five `[[terrain]]` types, Woodland, a water graph and a Hazard Region all exist in the world and all rendered as one olive plane | no | ✅ 01-09 |
| **3** | **Block interiors.** Trees, rocks, yards, outbuildings — and **a built ring deep enough to be one**. ⚠ **Dressing, by rule 2** — the middle of a block is empty in the *model* and always will be | no | ✅ 01-09 |
| **4** | **Density and height.** ~~`[roads] block_tiles` **32 → 16**, `[[building]] occupants` up, and a second dwelling kind~~ — **all three named actions were superseded before this row was picked up**, and by [`0052`](0052-the-parcel.md) and [`0053`](0053-the-block.md) rather than by an argument: `block_tiles` is superseded at `0052`'s own *Alternatives*, `occupants` is **retired** into `RefuseRetired`, and the second kind is what the block pattern ladder does from the ground instead | no, as delivered | ✅ **HALF, 03-09.** The drawing reads the city's storey count exactly (**F42**). 🔴 **The other half is not a drawing problem and is not done** — the city never expresses more than three of its five rungs at any population (**F43**), which is measured, has a working fix, and is on branch `row4-generator` rather than here (**F45**) |
| **5** | **Night.** Light follows the clock, and windows are lit from **occupancy** | no | ✅ **02-09**, and it took two plans. Row 6 built the openings and wired occupancy to them; [`0051`](0051-the-four-pillars-and-a-city-to-photograph.md) row 5 put the sun on the clock and lit the panes; and 0051 **F23-F25** finished it — the wall stopped leaking light through its own antialiasing, and a lamp stopped fading in |
| **7** | **The overlays.** Pollution, land value and Sealing, tinting the map. ⚠ **Unplanned, and it is not in this queue's original six** — it is `docs/07 §6.1`'s first *built, and the drawing is silent about it* row, and the one the pitch rests on | no | ✅ **02-09** |
| **6** | **The wall, the roofline and the sun.** Openings drawn procedurally on the box; a front door on the kerb the Lot steps to; the roof gets eaves, a pitch cap, a ridge that sometimes turns, and two tones; the wall gets a narrow colour band; and the sun gets a **shadow**. ⚠ **Unplanned, and it took the shape of the three things a person asked for** — *vary the pitch*, *put some windows on*, *anything else that helps* | no | ✅ 01-09 |

⚠ **In order, cheapest first, and each is visible on its own** — which is the test that they really
are separable.

---

## What is already there, so that nobody rebuilds it

**More than the picture suggests.** `Main.Buildings()` already varies height, varies frontage,
derives storeys from the kind's `occupants`, sets back from the kerb by half the carriageway plus
half its own depth, and keys every draw on the Building's monotonic row id — so a Building keeps its
shape as long as it stands and a rebuild on the same Lot is visibly a different building.

⚠ **The MultiMesh is per *kind of thing* and not per Chunk**, which is not what `05 §2` says.
`_buildings`, `_travellers`, `_roads`, `_plots`, `_cells`, `_ground`, `_water`, `_flood`, `_hazard`
and `_cursor` are one apiece, with per-instance colour where it is wanted.

⚠ **A MultiMesh instance colour is multiplied into albedo in *linear* space**, which is recorded in
the shell already and is the trap anybody picking a palette will hit first.

---

## Findings

| | |
|---|---|
| **F1** | 🔴 **THE BUILDINGS ARE WIDE BECAUSE THE LOTS ARE, AND THE LOT WIDTH IS DERIVED.** `Frontage` is `2 × block_tiles × 4 m ÷ lots_per_segment`; at the shipped `block_tiles = 32` and `lots_per_segment = 5` that is a **128 m block face carrying 2.5 Lots of 51.2 m each**. The renderer then filled **72–98%** of it — `FrontageFill` 0.85 against a 0.85–1.15 jitter — so a **37–50 m** building sat on a 51.2 m Lot and the gap was too narrow to see. Beside a depth of 9.6–16.8 m and a height of 7.7–25.9 m, ***the shipped building was a slab three to four times wider than it is deep, and wider than it is tall.*** ⚠ **`lots_per_segment` IS NOT THE LEVER**: the 5 is `CONTEXT.md` → Address's own *five Buildings share a Segment* and is the premise of the *an Address is never a Node* refusal. **The levers are `block_tiles` and the renderer's own fill fraction**, and they are a Ruleset change and a free one respectively |
| **F2** | ⚠ **THE MIDDLE OF A BLOCK IS EMPTY IN THE MODEL AND IS NOT A DEFECT.** `adr/0078` — a Lot has no depth and there is no depth key; Lots hang on Segments. The renderer reaches 10–17 m back from the kerb and the remaining ~48 m to the centre of a 128 m block is nothing the city knows about. ***So it is the one large surface the renderer may dress without lying***, which is row 3. ⚠ **Filling it with BUILDINGS would be a design change against a settled ADR** and is not what row 3 is. 🔴 ⚠ **AMENDED 2026-09-01 — THE DEAD INTERIOR IS A GAME MECHANIC AND SOMETHING ELSE RESTS ON IT.** `LotSubdivider`'s own remarks say it: everything not on a block face *"stays unlotted **structurally** — not because a depth ran out"*, which is `02 §2.2`'s ***punish the player mechanically rather than through a penalty number*** taken at its word. [`adr/0025`](../docs/adr/0025-density-is-a-cap-and-it-trades-land-for-materials.md) **refused a road-derived density cap because the player gets dead block interiors instead** — so removing the interior would reopen a settled refusal. ⚠ **`block_tiles` is the lever and it is strong**: at the shipped 32 Tiles about **60% of a block's usable land is interior**, at 16 Tiles about **28%**. ***A 128 m block ringed by 13 m of building is a perimeter block with a courtyard in it***, which is what row 3 has to draw |
| **F3** | ⚠ **A MIXED SKYLINE IS BUILDABLE TODAY AND A *DOWNTOWN* IS NOT.** `ZoneRuleDefinition` carries one `Kind`, but several `[[zone_rule]]`s may target one zone and contend for Lots, with declaration order load-bearing — so two dwelling kinds at different `occupants` give a mixed skyline now, and the renderer draws the tower without being told to. **What is missing is the SIGNAL that would put the tall ones in the middle**: siting is best-of-N scored over the sample (`adr/0170`), and the score that would want centrality is land value, which is **zero everywhere by construction except `fouled.toml`** because nothing else emits. ***Unbuilt rather than refused*** (`adr/0070`) |
| **F4** | ⚠ **ROW 4 GOES STALE ON A DOZEN HEADERS THE DAY IT LANDS.** Raising `occupants` houses the same population in fewer Buildings, so the Pool drains faster and fewer Lots are consumed; every census figure quoted in the Ruleset headers and in `CLAUDE.md` — the *602 built, 385 abandoned, 387 vacant* line and the decline shares — is measured against the old value. ***That is a documentation refresh and not a reason to defer*** ([`adr/0100`](../docs/adr/0100-moving-the-state-hash-costs-nothing-until-somebody-is-carrying-a-save.md)) |
| **F5** | ✅ **ROW 1, AND THE BAND DID MORE THAN THE ROOF DID.** `BuildingFillLow`/`High` **0.42–1.0** puts a terrace beside a house with a yard where there were two slabs; **150 of 241** Buildings take a gable, the rest being over `PitchCeilingMetres` or drawn flat by the scramble. ⚠ **The gable is a `PrismMesh` off the shelf** ([`adr/0018`](../docs/adr/0018-prefer-off-the-shelf-infrastructure.md)) on a second MultiMesh, and **one derivation feeds both layers** — a second walk over the Building table re-deriving the same setback and footprint is `plans/0012` **Cause 1** with a frame between the copies |
| **F6** | ✅ **THE ROOF WAS CHECKED AGAINST ITS BUILDING RATHER THAN LOOKED AT.** A gable is a prism extruded along its own Z, so the east–west case turns a quarter — and ***a 90° error is the easiest thing in this row to commit and the hardest to see.*** `draw` dumps both layers with a yaw column; joined by id, **all 150 roofs match their body's footprint and centre to 0.01 m**. ⚠ **That is `plans/0048`'s thesis arriving** — the answer is the draw list and not the frame |
| **F7** | 🔴 **THE BLOCKS LOOKED HALF-EMPTY BECAUSE A CORNER LOT WAS CUT BY 69%, AND THE FIX IS TO SLIDE IT RATHER THAN SHRINK IT.** Measured off the `draw` dump, counting each **kerb** — one side of one 128 m Segment — separately: **59% of a kerb was built**, two or three Buildings on it, median gap **16.3 m** and the biggest **52.6 m**. ⚠ **A Segment's five Lots ALTERNATE SIDES**, so one kerb carries 2–3 of them at 51.2 m spacing, and `Frontage.OffsetOf` leaves the outermost half a spacing from the junction — which put a full-width Building **12.8 m into the cross street**. The old rule took the lesser of the wanted width and the room and cut a 51.2 m Lot to **16 m**. ✅ **Keeping the width and moving the centre** cut only a Building too wide for the whole block: **59% → 72%**, and 78% once `BuildingFillLow` followed to 0.55. 🔴 ⚠ **BOTH OF THOSE FIGURES WERE A SUM OF WIDTHS AND THE SUM DOUBLE-COUNTED OVERLAPS — see F9**, where the same kerbs measure **74% by union with nothing overlapping**. ⚠ **0.65 reaches further and was not taken** — it narrows the width range to 1.5× and the slab wall comes back |
| **F8** | ⚠ **THE BARE GRID IS THE LATTICE AND NOT A DEFECT.** `schooled.toml` at 2,000 Citizens paves **8×8 blocks of 128 m** — 144 Segments over 1,024 m square — and subdivides Lots into about **28 of the 81 block cells**, in raster order rather than outward from a centre. Every cell that has any has **exactly 10** (Buildings plus vacant Lots), so ***the built part is full and the empty part has no Lots at all***. 🔴 **A city that grows in reading order rather than from its middle is a finding about `SyntheticCity` and not about the drawing**, and it is the thing to look at before row 4 moves any density number |
| **F9** | 🔴 **A LOT'S FRONTAGE WAS RIGHT ON AVERAGE AND WRONG ON EVERY KERB, AND THE CORNER FIX TURNED THAT INTO BUILDINGS STANDING INSIDE EACH OTHER.** `Frontage.SideOf` is **odd-and-even house numbering** — indices 0, 2, 4 left and 1, 3 right — so a Segment's five Lots split **three and two**. At 51.2 m each that is **153.6 m of Lot on a 128 m kerb** on one side and **102.4 m** on the other: ***one side over-subscribed by a fifth and the other short by a fifth***, which is why the short kerbs measured 67% and could not have done better, and why the long ones measured "100%". ⚠ **F7's slide removed the cut that had been hiding it** and the over-subscribed side interpenetrated — **64 overlapping pairs, measured**, in a build that had already been committed. ✅ **A Lot's frontage is now the half-way line to its neighbours ON ITS OWN SIDE** (`Main.Kerb`, calling `Frontage.OffsetOf` rather than restating it), which tiles each kerb exactly and is asymmetric where the offsets are. **74% built by union on both kerb types, 0 overlaps, median gap 9.0 m, biggest 25.5 m** — and the two types now measure *the same*, which is the check that the geometry rather than the taste was fixed. ⚠ **What is left unbuilt is the width jitter and the junction clearance, and nothing else** |
| **F10** | ⚠ **A SUM OF WIDTHS IS NOT A COVERAGE, AND IT REPORTED 100% ON THE KERBS THAT WERE BROKEN.** The instrument that found F7 added the widths on a kerb and divided by 128 — so overlapping Buildings counted twice and the most defective kerbs scored highest. ***A measure that cannot go above its own maximum is the one to write***: the union of the intervals can, and does |
| **F11** | ✅ **EVERY SHIPPED WORLD HAS HAD A LANDSCAPE SINCE MILESTONE 24 AND NOT ONE OF THEM HAD EVER SHOWN IT.** `TerrainGenerator` and `WoodlandGenerator` run **from the `WorldKey` alone** — *"a file that states no `[terrain]` still gets forest"* — so this was never a `varied.toml` feature. `TerrainCellTable` is one byte a Cell on all 262,144, `WoodlandCellTable` one count, and the shell drew a single olive slab over both |
| **F12** | ⚠ **A TEXTURE AND NOT 262,144 COLOURED SQUARES, AND THE REASON IS THE FILTER.** The data is per **Cell**, which is 128 m — so an instance apiece draws the map as a chequerboard with hard edges. One texel a Cell on one quad costs a single draw call and gets **bilinear blending free**, which is exactly what a shoreline and a field edge want. ⚠ **It smooths the DRAWING and not the data**: the hover still reads the Cell and a Rule still gets the step. ⚠ **Written in sRGB and not converted**, unlike every instance colour in the shell — an albedo texture is sRGB by contract |
| **F13** | 🔴 **THE FLOODPLAIN OVERLAY STRIPED AT MAP DISTANCE AND READ AS CORRUPTED TERRAIN.** 11,063 boxes lifted 0.02 m, seen from 30 km, is under depth-buffer precision — so `coastal.toml`'s lakes came ringed in torn red bands. ✅ **It went into the skin as a tint** — *a floodplain is a claim ABOUT ground rather than a kind of it*, so a wooded floodplain still reads as wood. ⚠ **The box layer survives and is hidden**, because `plans/0048`'s answer is the draw list and ***a texture is invisible to one***: `draw` still counts the Cells |
| **F14** | 🔴 **NOTHING COULD POINT THE CAMERA AT THE OTHER 62 KILOMETRES, AND THAT IS WHY NONE OF THIS WAS EVER SEEN.** `Furthest()` is the **city's** span × 3 — the right leash for a mouse wheel and the wrong one for a review: it stops about 3 km out on a 65.5 km map, and `zoom out 22` moved nothing. ✅ **`focus <east> <north> [metres]`**, `plans/0048` tier 2, and **the distance is deliberately not clamped** — *a script is not a hand*. ⚠ **The first attempt aimed at Tile 32,768 and got a blank screen**: the map is **16,384 Tiles**, and 32,768 is the *Cell*-count arithmetic showing up in the wrong unit |
| **F15** | 🔴 **THE INTERIOR LOOKED VAST BECAUSE THE BUILT RING WAS A RIBBON, AND THE RIBBON WAS THE RENDERER'S OWN NUMBER.** `PlotDepthMetres` stood at **12 m** — a depth the city does not hold and the shell invents — so a 128 m block carried two 13 m strips and **~60% of its usable land was interior**. ***A real perimeter block is 15–25 m deep***, and at **26 m** the interior falls to **~27%**. ⚠ **`adr/0078` was never the obstacle it looked like**: the ADR governs where a Lot *is*, and how far back the drawing reaches was a free variable the whole time. ***Two of the three levers on this were the renderer's and only the third was the city's*** |
| **F16** | ✅ **THE COURTYARD IS DRESSED, AND THE COUNT COMES FROM THE WORLD WHILE THE POSITIONS DO NOT.** An outbuilding per Building drawn from its own scramble (181 on `schooled.toml`), and trees scattered per Cell at `Woodland ÷ TilesInCell × CrownsPerCell` — **3,522** around a 2,000-Citizen town. ⚠ **Woodland is `(saved AND hashed)` and `MapLayers.Seal` takes it back as ground is built on**, so a built Cell thins out by itself and nothing had to be special-cased. ⚠ **Where each crown stands is the shell's `Scramble` and never a `purpose_tag`** — the simulation's stream is for decisions, and a shape nobody in the city can perceive is not one |
| **F17** | ⚠ **THE YARD TEST FOLLOWS THE PAVING AND NOT THE LOTS.** A crown must clear `YardMarginMetres` of every block edge — derived from half a carriageway plus the deepest Building, not chosen — or it stands in a carriageway. ⚠ **The rectangle drawn round the *Lots* is too small**, because `SyntheticCity` lays a lattice larger than it subdivides (**F8**), so paved blocks fall outside it. ⚠ **And the test is not applied in open country**: a regular gap out there would draw the road grid where there are no roads |
| **F18** | 🔴 **"IT IS A COURTYARD" WAS WRONG, AND THE REAL RULE IS THAT A BLOCK'S SHORT SIDE IS TWO LOT DEPTHS.** Checked against real plans rather than asserted. **Manhattan** (Commissioners' Plan, 1811): blocks **200 ft × 600–920 ft**, the standard Lot **25 × 100 ft**, and ***two rows of 100 ft Lots exactly fill the 200 ft width*** — **the interior is zero**, and a rear yard is a setback *inside* a Lot rather than leftover land. **Chicago**: 660 × 330 ft blocks with a **mid-block alley**, which is what a block too deep for one rank does — the alley *makes* interior frontage. **Barcelona Eixample**: 113 m square, perimeter 20–28 m deep, a ~50 m *pati d'illa* — the courtyard case, and most were built over in the 20th century. **Paris**: 12–18 m deep around small *cours*. ***So: short side ≈ 2 × Lot depth → no interior; deeper than that → an alley or a courtyard. Nobody rings a 128 m SQUARE block with one rank of detached buildings.*** ⚠ **And a courtyard is ENCLOSED.** Ours is 74–78% built with a 9 m median gap, so you see straight through it — ***an open void is not a courtyard, it is a field with houses round the edge***, which is a suburb |
| **F19** | ✅ **MEASURED, AND `block_tiles` IS MOST OF THE FIX.** At the shipped **32** (128 m) with a 27 m median depth the unbuilt middle is **66 m — 55% of the block's width and 30% of its usable area**. At **16** (64 m) it is **21.3 m, 38% and 15%** — and the blocks read as a continuous urban fabric. ⚠ **The remaining strip was `Depth`'s own cap**: `room × 0.8` never bites at 128 m (room 60 m against a 21–36 m want) and **is** the depth at 64 m. At **0.95** the two ranks meet with ~3 m between them, which is Manhattan. ***The cap is the renderer's and free; `block_tiles` is the city's and hash-bearing*** |
| **F20** | ⚠ **THE MANHATTAN GEOMETRY IS NOT REACHABLE, AND THE REASON IS ONE FIELD.** `StreetGrid.BlockTiles` is a single number and `Span` derives from it, so ***every block in every world is SQUARE by construction***. Manhattan's 61 × 244 m block — short side two Lot depths, long side carrying the frontage — needs a block with **two** dimensions, which is a `Borough.Core` change and not a Ruleset one. **Unbuilt rather than refused** ([`adr/0070`](../docs/adr/0070-an-unbuilt-mechanism-is-not-a-design-constraint.md)) |
| **F21** | 🔴 ⚠ **THE OVERLAPPING BUILDINGS WERE THE SHELL INVENTING A PARCEL FOUR TIMES OVER, AND THE FIRST VERSION OF THIS ROW CALLED IT UNFIXABLE ARITHMETIC.** It was not. The claim was that four `deep × L` bands contest `4·deep²` of corner and a square block therefore cannot be full — but that is arithmetic about a shape nobody draws. ***A limitation of the author's own rectangle was reported as a property of the block.*** ⚠ **The real cause is that a Lot is an ADDRESS and owns no ground** ([`adr/0078`](../docs/adr/0078-frontage-is-derived-on-the-epoch-and-a-lots-width-is-the-segments-own-building-count.md)), so the shell invents a parcel per Building, four faces invent onto one patch, and **nothing arbitrates**. ⚠ **Every other city builder allocates the LAND first**: a SimCity 4 lot is a rectangle of tiles and a tile belongs to exactly one lot; a Cities: Skylines block is 8 m zoning cells, each claimed by exactly one road's strip. ***The fix is allocation, not geometry.*** ✅ **Two faces take the corners and two give them up** — the horizontal pair runs the block's length, the vertical pair starts a depth in from each end, **which is what a real corner does**: the corner building belongs to one street and the cross street's terrace begins after it. Measured from the shell's own `draw` dump on `declining.toml` at 2,000 Citizens: **28 overlapping pairs and 2,610 m² → 0 and 0**. 🔴 ⚠ **THE RESERVE IS THE WORST-CASE DEPTH AND NOT THE BUILDING'S OWN**, which is what the first attempt got wrong — depth is jittered per Building, so a shallow Lot held back a shallow distance and the deeper Building opposite reached straight through it; that version cut the overlap from 12,172 m² and closed **none** of it. 🔴 **WHAT IT COSTS IS 21 OF 216 BUILDINGS UNDRAWN** — the first and last Lot on each vertical face, whose stretch clamps to **0.0 m**. ***That is not a rendering choice, it is a SIM-LEVEL DUPLICATION showing through***: `LotSubdivider` subdivides four faces independently, so the corner ground carries **two Lots**, one of which has an address and no land. **A missing Building is honest and a doubled one is a lie**, so the shell draws neither — but the defect is in the subdivision and the renderer is only where it became visible. |
| **F22** | ⚠ **`block_tiles` IS THE CITY'S DIAMETER DIAL AND NOT A BLOCK-SIZE DIAL, because `SyntheticCity` sizes the lattice by HOUSING NEED.** Measured on `declining.toml` at 2,000 Citizens, 32 → 16: the road graph is **identical** — 81 nodes, 149 Segments, 298 Arcs — and so are 276 Lots, 241 Buildings, 720 Households and every decline figure over 20,480 Ticks (99 built, 94 abandoned, 83 vacant; 319 raised, 461 condemned, **digit for digit**). What moves is the **ground**: 4,833 → 2,414 road Tiles, and the city goes ~1.0 km across to ~0.5. 🔴 **The same lock catches the other density lever** — `lots_per_segment` 5 → 10 at block 32 gives **49 nodes and 86 Segments** where 5 gave 81 and 149. ***At a fixed population you cannot make this city denser without making it smaller***, so **a dense large city is reached by adding Citizens and by nothing else**. |
| **F23** | 🔴 **THE CURSOR DREW A CELL AND LOOKED RIGHT FOR AS LONG AS A CELL WAS A BLOCK.** A Cell is **32 Tiles by design and never moves**; `block_tiles` is Ruleset data and does; every shipped file set it to 32. A highlight denominated in the wrong unit sat exactly on the thing a click acts on, so nothing could report it — until `block_tiles` went to 16 and the box covered **four** blocks. ⚠ **What a click acts on is the block** (`Virgin` and `LotSubdivider.SubdivideBlock` both take `FloorDiv(tile, BlockTiles)`), so that is what the box is now. ***A unit error is invisible while the two units are equal***, which is `plans/0012` **Cause 1** wearing a coincidence instead of a second copy. |
| **F24** | ⚠ **`block_tiles` 32 → 16 across the 30 shipped files takes the assertion lane from green to 33 failures, and only 9 of them are hashes or a stated constant.** Four are the golden session's 13 zone commands carving **0 Lots where 117 were expected** — they name coordinates on a 128 m lattice, so it is a **re-record** rather than a re-hash. **Seven are demonstrations that stopped demonstrating**, all saying one thing: *at 160,000 Citizens every seeker still holds the whole city in its box (100.0%)*; *no commute reaches the unsavoury rung*; *the ceiling refuses nothing*; *a ten-minute ceiling refuses nobody*. 🔴 ***Halving the block halves the city's width, so every mechanism whose job is to say "that is too far" loses its subject.*** ⚠ **The measurement that said this change was free was taken on `declining.toml`, the one world with no distance-dependent mechanism in it** — `plans/0042` **F12** exactly, a figure generalised past what it measured. |
| **F25** | ✅ **THE CORNER NOW HAS AN OWNER IN THE SIM, AND THE SHELL'S GUARD WENT INERT THE SAME DAY.** `LotSubdivider.CornerTiles` reserves **one Lot's frontage** at each junction on the **north–south** faces, so the ground under a corner carries one Lot instead of two. ⚠ **The reservation is not a depth and cannot be** — `adr/0078` refused the key and the refusal stands — so it spends **the only length the city knows about a Lot**, capped at a quarter-block so a coarse Ruleset cannot yield a whole face. Measured on `declining.toml` at 2,000 Citizens: **241 Buildings in the sim, 241 drawn, 0 overlapping pairs**, where the shell alone had to drop 21. ⚠ **A block yields 8 Lots where it yielded 10 and the SUPPLY BARELY MOVES** — 276 → **280** — because the generator sizes the lattice by housing need and answers a thinner block with more blocks (F22 again). ⚠ **Which pair of faces yields is ARBITRARY and is recorded as arbitrary**; that *some* rule must exist is not. |
| **F26** | 🔴 ⚠ **THREE ASSERTION-TIER TESTS WERE DEMONSTRATING NOTHING, EACH SITED ON A CITY TOO SMALL FOR THE MECHANISM IT NAMES.** All three were pre-existing and all three were surfaced by one change to the Lot supply, which is the only reason anybody looked. **`TasteTests`** asserted a centrality preference at 2,000 Citizens where the gap over five seeds is **−11, +2, +10, −5, −1** — noise, and the committed −11 was one draw from it; the effect is real at **8,000** (−27, −8, −5) and **20,000** (−12, −29, −4). **`CarOwnershipTests`** asserted `drivers >= walkers` at 4,000 where the advantage is **exactly zero** — the city fits inside a walk — and the `>=` was satisfied by a tie; it is **+41 at 16,000 and +1,070 at 64,000**. **`CarRouteLengthTests`** asserted a drive and a walk take the same streets, naming the Arterial as the only mode mask and **missing the FootPath**, which is the mirror case: with `foot_paths_per_thousand_blocks = 0` the sums are **exactly equal**, with the shipped 40 they differ by 50. ***A comparison run on a city where the mechanism under comparison is inert measures the city*** — a sentence `CarRouteLengthTests` already carried, about itself, while two neighbours did the same thing. ⚠ **The shared tell is an assertion that can be satisfied by a TIE**, and the repair in all three cases was to re-site rather than to relax. |
| **F27** | 🔴 **THE ROOF PITCH WAS ALREADY RANDOM AND EVERY ROOF LOOKED THE SAME, BECAUSE THE BAND WAS NEVER WHAT THE EYE WAS READING.** `RoofRiseLow`/`High` stood at **0.30–0.75** of the depth — 31° to 56°, a wide band by any measure — and the ask that opened this row was *vary the pitch*. ⚠ **The roof was cut flush with its own wall**, so a slope met the wall as a colour change and nothing else: ***there was no eave line to measure an angle against***. **0.9 m of overhang** is what made the existing variation visible, and the band was then *narrowed* to 0.18–0.45 (20°–42°) because the old one had been putting **16 m of roof on a 36 m span**. ***A parameter that renders as nothing is not a parameter***, which is `adr/0093`'s failure mode arriving in geometry rather than in prose |
| **F28** | ⚠ **THE OPENINGS ARE A SHADER AND NOT A MESH, AND THE SIZE IS RECOVERED RATHER THAN PASSED.** One `BoxMesh` serves 65,536 Buildings, so a window cannot be modelled; `buildings.gdshader` takes the box's metres from the **instance transform's own column lengths** and lays a storey grid on them. ***So a Building drawn taller gets more storeys without anybody counting them***, and the storey spacing is **fitted rather than fixed** — a blank strip under the eaves reads as a mistake. ⚠ **Smoothed against `fwidth` and faded out past 500 m**, because a hard step on a wall a kilometre off is a sparkle |
| **F29** | ✅ **THE PICTURE NOW SAYS `occupants` OUT LOUD, AND THAT IS THE SURPRISE THE DEFINITION OF DONE ASKS FOR.** The share of a Building's openings left **shut** is `held / occupants` — the panel's own *"3 of 4 occupied"*, which nothing in the frame had ever said — and which openings is the drawing's own draw. ⚠ **It is a SHARE and never one Household per window**; a window is invented geometry and a Household is not. 🔴 **What it exposed was the HEIGHT**: at `occupants = 4` and a 0.55–1.85 jitter a dwelling is 7.7–25.9 m, and until there were storeys on it nobody could see that the shipped world is built of **five- and six-storey tenements**. ***A height jitter that nothing read as storeys was reading as texture***, which is row 4's subject arriving from the side |
| **F30** | ⚠ **THE FRONT DOOR IS THE LOT'S `Side`, AND IT IS INVISIBLE ON HALF THE CITY BY CONSTRUCTION.** Which kerb a Building steps to is the one thing about its orientation the city actually holds (`adr/0074`), and it reaches the shader as the street face's outward normal in two channels of `INSTANCE_CUSTOM`. ⚠ **The camera's pitch never moves** (`atan(1/√2)`, and it is the framing), so the fronts facing away are simply not in the frame — ***rotating is how you see the other half***, and that is the isometric bargain rather than a defect. 🔴 **A door alone was drawn correctly and could not be seen**: two metres on a twenty-metre wall is three pixels. The **surround** is what made it read, by giving the mark an edge in the other direction |
| **F31** | ✅ **THE SUN CAST NOTHING UNTIL TODAY, AND SHADOWS COST NOTHING MEASURABLE.** Every Building met the grass at a seam, so a box read as pasted over the map rather than standing on it — and a pitch, which is two planes catching the light differently, was being read off a colour change. ⚠ **The split distances are set against the CITY**: the default 100 m puts every cascade inside one block, and the 65.5 km map would spend them all on empty ground. ⚠ **A flat layer is out of the shadow pass by a stated argument** — the ground, the water, the flood, the Hazard Region, the carriageway, the pads, the lattice and the cursor are slabs casting onto what they cover, and on `bordered.toml` that is **535,817** Segments rasterised four extra times a frame for nothing. **Measured on `congested.toml` at 4,000 Citizens, 640 Ticks at the 32/s rung: 20.76 s against a 20.0 s floor**, and a 240-Tick run timed **63.60 s with shadows and SSAO, 63.51 s without, 63.47 s on the pre-change tree** — ⚠ **which is the rung's own clock and not a render cost**, and is the one thing those three readings are good for |
| **F33** | 🔴 **TURNING THE SUN ON PUT THE WHOLE MAP IN SHADOW, AND IT ONLY DID IT FROM A KILOMETRE UP.** At a street it looked right; at `zoom in 10` on `declining.toml` every surface in the frame went to ambient and the picture read as **the sun having gone out**. ⚠ **Two causes, and the second is the one nobody would guess.** The terrain is a **65.5 km `PlaneMesh` at y = 0.01** and it was a shadow **caster** — casting onto the ground it *is*, held off the screen by nothing but the depth bias. ***A bias is denominated in shadow-map texels***, and a cascade stretched to 2,400 m has texels metres wide, so the bias stops working exactly when the camera pulls back. ✅ **The plane casts nothing** — a flat surface can shadow only itself — and the range came to **1,600 m with a 0.85 fade**, so a distant city is *unshadowed* rather than *wrong*. ⚠ **The eight flat MultiMesh layers were already out of the pass** and were not enough: ***the one caster that mattered was the only surface not created through `Layer`***. 🔴 **Found by looking at a wide shot after three close ones**, which is the amended Definition of done doing its job — ***a lighting defect that is invisible at the zoom you are working at is invisible for as long as you work there*** |
| **F32** | 🔴 **A RUIN'S EMPTY WINDOWS CAME OUT LIGHTER THAN ITS WALLS, AND THE ALBEDO WAS NOT THE REASON.** Glass is smooth, and a smooth surface over a near-black albedo is mostly the highlight it catches off the sky — so the holes in a shell rendered as **glints**. ***There is no glass left in a shell***, which is the physical fact the material was not being told; the roughness now follows the state. ⚠ **The state is read off the wall's own luminance and not out of a fifth channel** — `Derelict` is 0.033 in linear against a standing wall's 0.24–0.29, which is why the new wall-colour band is held to ±10%: ***abandonment is the one thing a Building's colour is allowed to mean.*** |
| **F34** | 🔴 **AN OVERLAY OF NOTHING IS A BLANK SCREEN, AND EVERY SHIPPED WORLD BUT ONE WAS GOING TO GIVE ONE.** ***The only thing in the build that gives a Cell a row at all is a pollution emission***, and `scope = "map"` appears in exactly **one** of the thirty shipped files — so on the other twenty-nine pollution, land value and Sealing are not zero, they are **ABSENT**, and an overlay would have drawn an empty map rather than a clean city. ⚠ **`Borough.Headless` already knew and refuses `--land-value` against such a file by name**; nothing in the shell did. ✅ **`pictured.toml` gets one emitting Rule**, and the row could not have been done without it |
| **F35** | ⚠ **THE EMITTER WENT ON THE SHOPFRONT AND NOT ON THE DWELLING, AND THAT IS ABOUT THE PICTURE RATHER THAN THE CONTENT.** `fouled.toml` emits from every dwelling and spends a paragraph saying it would rather not have had to — what that gets is a **blanket**: every Cell of the built area emits equally, the field is flat over the city and falls off only at the edge, and ***an overlay of a constant is an overlay that shows nothing***. Shops are raised by a tier-1 trade rule at a handful of sites, so they are **point sources with a 1,024 m tent over each**, which is `02 §2.4`'s own shape and the only arrangement in which the map has anywhere for the value to be high. 🔴 **AND FOUR SOURCES ARE NOT 2,400: at `amount = 1` the field rounded to ZERO in every Cell** and the legend read *dark 0 to bright 0, 1,002 Cells read* — the emission had to go up three orders of magnitude before a point source registered at all. ***The number is a picture's and ratifies nothing***, which is what that file is for |
| **F36** | 🔴 **THE LEGEND PRINTED THE STORAGE AND NOT THE NUMBER, AND BOTH FIGURES LOOKED LIKE QUANTITIES.** Pollution is held in **kernel units** — pre-normalised, which is what buys exact superposition — and land value is **Q16.16**. Read straight off the column they came out as **1,955,195** and **17,642,822**; the readings are **298** and **269.20**. ⚠ **Nothing would have failed**: the ramp is a ratio, so the picture was correct and only the axis was nonsense — ***a caption is where a unit error survives***, which is `plans/0012` **Cause 5** arriving in a HUD instead of in prose. `MapLayers.Pollution` and `LandValueDump.Whole` both already did it right, one level away |
| **F37** | ⚠ **THE MUTED CITY WAS BRIGHTER THAN MOST OF THE RAMP, AND AN OVERLAY IS READ FROM ABOVE.** The first base grey was **0.17**, which put the roofs at the top of the picture's contrast range and left the field showing through the gaps between them — ***the thing being measured came second to the thing it was measured over***. At **0.055** the city is a silhouette that still says where the streets and the massing are. 🔴 **The same reading applies to `Unmeasured`**, which must never be confused by eye with a measured zero: *this Cell is clean* and *nothing has ever been read here* are different claims, and on a world with no emitter the whole map is the second one |
| **F38** | ⚠ **THE LAND-VALUE OVERLAY IS THE POLLUTION OVERLAY WITH A LAG, AND THAT IS THE MODEL RATHER THAN THE DRAWING.** Side by side on `pictured.toml` the two pictures are the same picture. Desirability composes pollution at **100%** and noise at 100%, has **no positive term at all** until amenity exists (`adr/0123`), and land value chases it with a tau — so with one emitting kind the field has exactly one input and the second overlay is the first one, smoothed. ✅ **It is worth having anyway and worth saying out loud**: ***two instruments that agree completely are one instrument***, and the day they part is the day a second term landed |
| **F39** | ⚠ **THE OVERLAY IS UNSHADED, AND THAT IS THE HALF THAT IS EASY TO FORGET.** The sun is on the clock now, so a ground plane left lit would multiply the field by the time of day — a Cell reading 200 at noon and 40 at dusk, from a light that is not data. ***An instrument that reads differently at 18:00 than at 12:00 is not an instrument***, so the ground goes `ShadingMode.Unshaded` and nothing in the frame answers to the sun while an overlay is up |
| **F41** | ✅ **SEALING DRAWS THE PAVED-BUT-UNBUILT LATTICE, AND THAT IS `F8` IN A PICTURE.** It is the one of the three overlays that does not look like the other two — hard-edged instead of a plume, and near-uniform over a region that is visibly **larger than the city**. ***The bright area is the road grid, and the road grid runs well past the last Building.*** `SyntheticCity` sizes the paved extent from a Lot capacity and stops subdividing the moment its housing budget is met, so the lattice outruns the built part by design and in reading order (`SyntheticCity.cs:861-862`, a west-to-east south-to-north raster from the lattice's origin corner). **F8 has been in this plan since 01-09 as an arithmetic finding off a Lot count; this is the first time anybody has SEEN it**, and it took no instrument — the overlay was built for pollution and reported this on the way past. ⚠ **It is also the answer to *what is Sealing for***: as a number in a hover it is a Tile count nobody has a use for, and as a map it is where the ground has been taken |
| **F40** | ⚠ **`fouled.toml`'s OWN COMMENT DISAGREES WITH ITS OWN RULE**, found while copying it: the paragraph above `smoke` says *"rate 1024 is twice a Day at this file's own TICKS_PER_DAY"* and the Rule states **`rate = 128`**, which is sixteen times a Day. One of the two moved and the other did not. Routed rather than fixed here (`adr/0073`) — it is that file's number and this row does not own it |
| **F42** | ✅ **THE DRAWING WAS SCRAMBLING THE ONE NUMBER THE CITY HAD STARTED TO STATE.** `Main.Buildings` read `LotTable.Storeys` — [`0053`](0053-the-block.md)'s ladder, `rung + 2` plus a deterministic draw of **one** — and then multiplied the height by **0.55 to 1.85**, which at the ladder's top is plus or minus three storeys. 🔴 **AND `LotRuleset.StoreysOn`'s OWN REMARK IS THE INDICTMENT**: *one storey of variation and no more, because two storeys of jitter would blur the ladder, which is the thing the height is supposed to say.* ⚠ **Two mechanisms both claimed to be what stops a block reading as a wall**, which is `plans/0012` **Cause 1** arriving in geometry rather than in prose — and the shell's copy was continuous and swamped the city's. The multiplier was inherited from the era when height was INFERRED from a kind's `occupants` and there was no storey count to draw. ⚠ **`buildings.gdshader` was the second half and had to move with it**: it FITTED a storey count out of the drawn height, `floor((height - PLINTH) / STOREY)`, so ***the number of window rows on a wall was not the number of storeys the city holds***. **Measured on `platted.toml` at 10,000 Citizens, off the `draw` dump: the drawn counts were 1 to 8 over 542 Buildings — 130 of them one storey tall on a city whose floor is two — against a ladder of five rungs.** ✅ **After: four distinct heights, 7.0, 10.5, 14.0 and 17.5 m, every one an exact multiple of the storey.** ⚠ **The plinth left the storey grid** — the fit had nothing left to absorb — and it stays what it separately is, the mask that keeps an opening off the bottom 0.9 m |
| **F43** | 🔴 **THE DENSITY LADDER IS NEVER CROSSED AT ANY POPULATION, AND THE CAUSE IS A DIVISION BETWEEN TWO UNRELATED QUANTITIES.** `SyntheticCity.BandAt` divides a block's Chebyshev reach by the **paved lattice's half-span**; `PavedTiles` sizes that lattice from `Lots.Rows.Capacity` — a **table's** capacity — while `Subdivide` stops at the **population's** room. ***So the band a block gets is the ratio between a memory sizing and a city sizing***, and the divisor grows exactly as fast as the built radius does. **Measured on `platted.toml`, the file whose whole job is to draw all five patterns in one city: drawn storeys 2, 3, 4, 5 at 10,000 Citizens and 2, 3, 4, 5 at 40,000.** At 10,000 the lattice paves **0–544 Tiles square** and the built part reaches north **158**, never within **114 Tiles** of the centre row; at 40,000 it paves **0–1,056** and reaches **286**. ⚠ **The reading-order walk is only half of it and reversing it proves so**: subdividing outward from the middle against the same divisor gives **5, 6, 7 at 10,000, at 40,000 AND at 160,000** — ***sixteen times the population and the same three rungs, from the other end.*** 🔴 **This is F8 and F41 arriving for the third time**, and F8 said in as many words that it is *the thing to look at before row 4 moves any density number* |
| **F44** | 🔴 **[`0053`](0053-the-block.md) LOOKED, SAW FIVE RINGS, AND THREE COMMITS LATER ITS OWN CAPACITY STEP TOOK TWO OF THEM AWAY.** That plan's *coarse patterns* section records — correctly, at the time — *five concentric rings at 40,000 Citizens … while the core is long slabs*. `Subdivide` counted **Lots** then; `bd81389` made it count **ROOM**, which is a much larger number per block, so the pass stops far sooner. **Measured directly by building the tree at `a9c51ed`, the commit before it: `platted.toml` at 40,000 Citizens built north 2→861 of a 1,056-Tile lattice — 82% across, past the centre row — with 4,801 Building instances. On today's tree the same world reaches north 286, 27%, with 2,416.** ***A change measured on its own terms invalidated an observation recorded three commits earlier, and nothing re-checked it*** — which is F4's shape (*row 4 goes stale on a dozen headers the day it lands*) arriving before row 4 did, and against a **picture** rather than a census figure. ⚠ **`platted.toml`'s and `banded.toml`'s headers both still describe the five-ring city**, and that correction is owed to [`0012`](0012-corpus-audit.md) |
| **F45** | ⚠ **THE FIX WORKS, IS ON A BRANCH, AND IS NOT A VISUALS ROW.** `row4-generator`: `Subdivide` walks Chebyshev shells outward from `BandAt`'s own centre, and `BandAt` gives **each declared band an equal share of the POPULATION** rather than an equal width of lattice — no tuning number, the only figure being the Ruleset's own band count, and the rings come out unequal because a slab block houses an order of magnitude more people than a detached one. **`platted.toml` then draws 2 through 7 at 10,000 Citizens and at 40,000**, and the city is a compact centred town that thins outward. 🔴 **IT COSTS 27 ASSERTION FAILURES AGAINST A BASELINE VERIFIED GREEN, AND TWO OF THEM ARE NOT REPAIRS.** **`BuildingDensityFieldTests`** reports in its own words that the city now has a density **GRADIENT**, which *invalidates the argument that smoothing buys nothing* — `plans/0037` **F8**, a settled decision reopened. **`ArrivalLongRunTests`** trips **`ParkingOccupancyIsConserved` at Tick 65,664**, 1,160 occupied against 1,159 holders: ***one space held by nobody, in a leak this exposed rather than caused***. ⚠ **And the share rule has a hole of its own**: one dense block can consume a whole band's share, so a band can be SKIPPED, which is what `BandAdmissionTests` reports on a two-band world. The other 24 are a golden re-record and fixtures sited on ground that moved. ***A Core change that reopens a decision and surfaces a conservation leak is not discharged by a renderer's row*** ([`adr/0073`](../docs/adr/0073-a-local-workaround-is-not-a-discharge-and-a-finding-about-shared-code-must-reach-it.md)) |

---

## Art style — open, and the constraints that bind it

**Undecided as of 2026-09-01.** What is settled is *the camera and two properties*, decided by the
player on 2026-08-31 and 09-01:

- **A high-level default camera.** *You can meet people; that is not the point of the game.* So
  silhouette, roofline, ground and colour do the work, and façade detail does not.
- **Light follows the clock, and a lit city at night is wanted.**
- **Kinds announce themselves heavily.** A school looks like a school.
- 🔴 **Not diagrammatic.** *This needs to look "city like", not just shallow, drab nothingness.*

⚠ **The declared preference is *vibrant and colourful, but not garish*, and small-cube voxel is under
consideration especially for terrain depth.**

**Four constraints, and they are not preferences.**

| | |
|---|---|
| **Instancing** | One MultiMesh per kind of thing, per-instance colour and transform. ***Variety through instancing is cheap and variety through unique assets is not.*** A kind that announces itself needs **one MultiMesh per appearance family**, each with its own mesh |
| **The Ruleset declares the look** | Otherwise the first `serves = "health"` kind anybody writes is an untextured cube for ever. Same rule as strings: `Core` hands over an id and the shell resolves it through the Ruleset |
| **The overlays** | Pollution and land value are the instrument the pitch rests on. ⚠ **The answer is that an overlay TAKES OVER rather than tints** — the city drops to an unlit base and the data owns all the colour. Two looks, one switch, neither compromised. ✅ **Built at row 7**, and the constraint held: it cost one material swap and no change to any layer's geometry |
| **The night is data** | Windows are occupancy. A dark Building at 22:00 is a tenancy that ended; a block lit at 03:00 is a Shift nobody intended. ***So the richest thing on the list is also the most legible***, which is the argument that changed the author's mind |

**Candidates, none chosen.**

| | |
|---|---|
| **Small-cube voxel** | ✅ **The most distinctive, and terrain depth is where it pays** — a cut shoreline, a cliff, strata, a floodplain with a real edge. Night is nearly free: a window is an emissive voxel. 🔴 **It breaks the instancing architecture**: surface voxels over a 65.5 km map cannot be instances, so it wants greedy meshing into chunk meshes, which is a bespoke renderer against [`adr/0018`](../docs/adr/0018-prefer-off-the-shelf-infrastructure.md) unless an addon is taken. ⚠ **The built area is ~1.4 km across, not 65.5** — the volume is far smaller than the map |
| **Vibrant flat-shaded low-poly** | Hard normals, a controlled saturated palette, a strong key with real shadows, a coloured sky. **Cheapest by a wide margin** — it is what the shell already draws plus a palette, roof meshes, shadows and a sky, and it scales to a million. Least distinctive |
| **Painterly** | Ramp shading, warm grade, ambient occlusion in the block interiors, a paper grain. ⚠ *Vibrant not garish* comes from the **grade** rather than from restraint in the palette. Risk: post-process grain fights the overlays and goes muddy at borough distance |
| **Diorama** | Tilt-shift, a slightly oversaturated toy palette, visible material warmth, props. **The one that most rewards a high-level camera specifically** |

⚠ **A hybrid is the author's recommendation and is not a decision**: **voxel ground, instanced
buildings.** Terrain gets literal depth where the interest is and is a bounded volume; Buildings stay
instanced massing so a million of them still draw, with voxel-scale detail only in the window grid,
which is where the night look lives.

🔴 **No ADR may be written for this while [`0045`](0045-amnesty.md) stands** (standing order 1), and
the decision is *arguable* rather than *measurable* so [`adr/0043`](../docs/adr/0043-a-claim-a-measurement-could-settle-must-not-be-settled-by-argument.md)
does not reach it either. ***It is decided here, in this section, and filed properly when the ratio
is earned.***

### What voxel would cost, filed so the question is not re-opened from scratch

🔴 **IT IS THE ONLY CANDIDATE THAT CHANGES THE ARCHITECTURE, AND THAT IS THE WHOLE OF THE DECISION.**
Everything the shell draws today is **one MultiMesh per kind of thing** with a per-instance transform
and colour, which is why 241 Buildings and 150 gables cost two draw calls and why a million of them
is arguable at all.

⚠ **A voxel ground cannot be instances.** Surface voxels over the map are far past what an instance
buffer holds, so it wants **greedy meshing into chunk meshes** — a different pipeline beside the one
that exists rather than a setting on it. Under
[`adr/0018`](../docs/adr/0018-prefer-off-the-shelf-infrastructure.md) that is a **bespoke component
needing a written exception naming the property no library provides**, unless an addon carries it.

✅ **The mitigating fact, and it is large: the built area is ~1.4 km across, not the map's 65.5 km.**
`plans/0042` **F17** and the `flooded.toml` header both turn on the same number. ***The volume is a
small fraction of what the map size suggests***, so the argument against is about the pipeline and
never about the extent.

⚠ **A hybrid keeps both** — voxel ground, instanced Buildings — because the two questions are
separable: the ground is one surface with depth, and the Buildings are a hundred thousand small
objects. **Nothing decided.**
