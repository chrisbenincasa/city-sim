# 0000 — The board

**Read this first.** A flat, scannable status of everything planned and everything done, and the one
place that orders the three tracks against each other.

It is a *view*, not a source: [`0003`](0003-build-plan.md) owns the slice order and its gates,
[`0002`](0002-open-questions.md) owns **every open question**, `docs/adr/` owns the decisions. When
they disagree, they win. Update this file whenever a task lands.

**Do not write an open question here.** That is how this file grew to 63 of them while the file named
*open questions* held none — and a board that is also the ledger is not a view of anything.

---

## How to read this file

**This is a view, not a source.** [`0003`](0003-build-plan.md) owns the slice order and its gates,
[`0002`](0002-open-questions.md) owns **every open question**, `docs/adr/` owns the decisions,
[`0013`](0013-tick-budget.md) owns what a Tick costs. When they disagree with this file, they win.

**And [`0002`](0002-open-questions.md) §F owns the coverage map** — every design document and all ~~58~~
**80** ADRs, marked 🔴/🟡/🟢, with ⚫ where a supporting claim has been measured false. *(It has stopped
tracking new ADRs three times; on 2026-08-12 it was found missing eleven, and the fix is filed as
[`0012`](0012-corpus-audit.md)'s mechanical check 5 — **generate the ADR column from the directory**,
because only that column is generable and a missing row makes an unassessed decision read as absent.)* **§A–§E are questions
somebody has already framed; §F is where you look for what has never been examined at all.** They are
different questions and the second is the one that has twice produced a surprise.

**Two rules keep it a view rather than a second ledger.** *Do not write an open question here* — that
is how it once held 63 of them while the file named *open questions* held none. And **a cell here is
at most three sentences**: the reasoning belongs to the slice plan, the spike plan or the ADR, and
this file links to it. A view that carries its own history stops being scannable, which defeats the
only thing it is for.

**The long-form board, as it stood before the 2026-08-10 restructure, was frozen at the bottom of this
file and is deleted as of 2026-08-12.** It was 999 lines of unmaintained history under a banner saying
so, and it had become the thing this file's own two rules exist to prevent: a view carrying its own
history stops being scannable. **Git holds it** — `git show db6f19f:plans/0000-board.md`, everything
below the `# Archive` line.

---

## State of play

**What the project is.** A city-builder whose simulation is an ordinary C# library with no game engine
inside it. Godot will be the display layer and has not been started.

**Where it is.** **Phase 2 has started. `06` milestone 5a — the Road Graph — shipped 2026-08-11**, briefed and recorded in [`0020`](0020-the-road-graph.md): Nodes, Segments and a directed Arc adjacency as typed tables in the State Hash, mode masks that make **Severance** real, the per-Segment Epoch, a generator driven by a new `[roads]` Ruleset table, per-mode connectivity components, `--roads`, and a 100,000-Tick run. **The suite is 1,060 green and both golden baselines were re-recorded.** Two ADRs came out of it — [`adr/0071`](../docs/adr/0071-travel-time-is-sub-tick-and-q16-16-is-a-scale-rather-than-a-meaning.md) and [`adr/0072`](../docs/adr/0072-the-mode-mask-is-saved-on-the-arc-and-the-segments-is-derived.md) — and its sharpest finding is that **the spike had `adr/0072` backwards from its own argument**: `Modes.cs` reasons that the mask must be per direction and `RoadGraph.cs` then saves the Segment's, which no measurement could catch because the spike never generates a one-way street. *A benchmark cannot refute a claim about a case it never constructs.*

**Before it.** **Phase 1's code is closed. There is nothing left in the Phase 1 code column at all.** Slices 0–10 are
**all done** — **slice 8, hot reload, closed last**, and with it `adr/0015`'s own acceptance test: *change
a production ratio and see the effect in seconds* reads **0.70 s**, against the 60–120 second warm rebuild
the ADR was written on. **Session N then put four items back on the code track and all four have shipped**
— [`0003`](0003-build-plan.md)'s hash-moving queue, now empty.
[`adr/0063`](../docs/adr/0063-a-wait-list-wakes-on-the-bins-state-and-a-shortfall-is-derived-rather-than-stored.md)
brought `adr/0033`'s satisfiability invariant, specified in three documents and built in none, and the
wake predicate it immediately found broken in the committed golden baseline. **Slice 10 task 11** put
`adr/0059`'s `revisit_ticks` in, and at 1M the shipped Ruleset raises **2,898 Buildings** where it raised
none. And tasks 3 and 4 shipped together as one commit
([`adr/0064`](../docs/adr/0064-a-bins-capacity-is-a-property-of-the-ruleset-in-force-and-an-over-full-bin-drains-rather-than-clamps.md),
[`adr/0065`](../docs/adr/0065-a-bin-holds-a-long-and-unbounded-names-a-ceiling-whose-approach-is-a-defect-rather-than-a-refusal.md)):
a Bin's capacity is **derived from the Ruleset in force** rather than frozen when the Building was raised,
an over-full Bin **drains rather than clamps**, and a Bin's `level` and `capacity` are `long` — because
`Money` is a `long` and a Bin's level was an `int`, so every payment into a Bin **narrowed 64 bits to 32**.
**The queue reopened the same day it emptied and emptied again the next**, with two more from session N task 2
([`adr/0068`](../docs/adr/0068-a-buildings-occupancy-is-declared-by-its-kind-and-an-over-capacity-building-evicts.md),
[`adr/0069`](../docs/adr/0069-placement-is-a-mechanism-of-its-own-and-construction-houses-nobody.md)): a
Building's occupancy is declared by its **kind** and derived from the Ruleset in force, an over-capacity
Building **evicts** rather than draining because occupancy has no consumer, and **placement is a
mechanism of its own** — `World.Place` had exactly **one** caller, at construction, so of `02 §5.2`'s six
steps only step 5 existed and the city could only house somebody by building them a house. **Both
shipped 2026-08-11.** **The defect was a 45% vacancy, not a homelessness figure**: nearly half the
housing stock stood empty while 70% of the population queued outside it, because nothing could move a
Household into a Building that already stood. **That is now 10%.** The corpus called this *five-sixths
homeless* for a slice and that buried the lede — homelessness in `rulesets/minimal.toml` is
`1 − capacity ÷ population` over a file that condemns every dwelling 64 Ticks after raising it **on
purpose**, so 83% became 53% and the rest is arithmetic. Placement drains a Pool to **zero** on a
fixture that does not demolish itself. And the pass needed **three** hash-bearing numbers where its ADR
predicted none, because `adr/0059`'s precedent derives the *sample* and leaves free the duration it is
derived from.
**Session F ran on 2026-08-11** — [`adr/0008`](../docs/adr/0008-walking-is-a-simulated-leg.md), *walking is a simulated Leg* — **and with it milestone 5b's second gate is discharged and the movement track is open.** One sitting, seven decisions, an amendment and three ADRs ([`0074`](../docs/adr/0074-side-of-street-is-a-property-of-the-access-point-not-of-the-graph.md), [`0075`](../docs/adr/0075-a-leg-is-a-plan-and-a-traveller-is-a-cursor.md), [`0076`](../docs/adr/0076-the-trip-fate-set-is-closed-at-four-and-a-fate-names-the-journey.md)); `CONTEXT.md` gained **Address** and **Node**. Record in [`0021`](0021-trips-legs-and-the-pedestrian-layer.md) → *What session F decided*.

**No gate is red anywhere in the corpus**, and **no session gates a slice at all** — F was the last one that did. S4, S0a, S0b, S2 R0–R8 and
**S5** have run — S5 published 2026-08-11, and with it [`0013`](0013-tick-budget.md) holds a **Microscopic tier
row for the first time**; sessions A, B, C, M, D, **F**, eight and nine are closed. **N is the only one
still open**, and what is open in it is task 5's residue — every ADR it produced has shipped.

**The standing rule that came out of that task outranks it, and it is now
[`adr/0070`](../docs/adr/0070-an-unbuilt-mechanism-is-not-a-design-constraint.md).** *An unbuilt mechanism
is not a design constraint.* The third sibling of `adr/0043` (which governs **claims**) and `adr/0052`
(**numbers**), governing the third thing a sitting reasons from: **absences**. Name the mechanism and
classify it — *unbuilt*, *undesigned*, or *refused* — and **only refused is evidence**. **Almost nothing
in this project is built yet**, so *the simulation does not do X* nearly always means nobody has built X,
and reasoning from it is inverted rather than merely weak. The symptom is a question shaped *given X does
not exist, should Y compensate?* — session N spent a sitting on exactly one of those, and the answer was
**build X**.

**What you can run today.**

```
dotnet run --project src/Borough.Headless                                   # build a city, print its tables and a State Hash
dotnet run --project src/Borough.Headless -- --seed 1 --ticks 10000         # step a session and print a hash trace
dotnet run --project src/Borough.Headless -- --layer pollution              # print a Map Layer as an ASCII field
dotnet run --project src/Borough.Headless -- --zones --ruleset rulesets/minimal.toml --ticks 5000   # watch the city thin out
dotnet run --project src/Borough.Headless -- --ruleset rulesets/minimal.toml --reload-at 200 --ruleset rulesets/minimal-tuned.toml --ticks 400   # retune a running city
dotnet run --project src/Borough.Headless -- --help                         # every flag
```

**What works.** Typed tables where every field is declared once as saved-and-hashed or
derived-and-rebuilt. Integer-only arithmetic. A deterministic eight-phase Tick. An Input Log that
replays to identical hashes, and a crash artifact that replays back into its own crash. Map Layers with
diffusion and decay. Build-time analysers that turn the determinism rules into compiler errors. And
**two Rule families** — Bin Rules moving Goods atomically, sleeping on the Bin that stopped them and
walking a fallback chain that ends by recording why; Zone Rules sampling a Lot grid to build and
condemn, with evicted Households keeping their money and waiting in the Unplaced Pool. **A waiter now
wakes on what its Bin holds rather than on what just arrived**, and an end-of-run invariant refuses a
Rule left asleep beside a Bin that has stopped blocking it.

**What does not exist.** Two of the eight Tick phases are empty. There are no jobs, no money, no
traffic and no renderer; Citizens are rows that do nothing. ~~no roads~~ — **roads shipped 2026-08-11**
(`Roads.Nodes` and `Roads.Segments` are in `World._tables`), and ~~no movement~~ is now **half true and
the half matters**: three Movement tables and a real Dijkstra exist and **nothing in the Tick calls
them**, because they are not in `World._tables` and Phase 4 is still an empty method. *Built but
unwired* is a third state this line had no room for, and it is exactly what row 8 closes. There is **no supply
chain**, because a chain between Buildings crosses an ownership boundary, which is the District Pool,
which is a named hole that throws — so the only shape available is a Building producing into and
drawing from its own Bins. ~~And **a Building has no declared occupancy at all**, so a long run settles
**five-sixths homeless**.~~ **Both halves are built** (`adr/0068`, `adr/0069`): a `[[kind]]` declares
`occupants`, and a `[placement]` pass drains the Unplaced Pool into dwellings that already stand, so
**10%** of the housing stock stands empty where 45% did.

**What is missing is step 2b, and it is the only part of placement that is not built.** A Household
takes the first dwelling with room, whatever it is, because the acceptance filter — *affordable? at
least one reachable job in budget?* — needs prices, jobs and a Commute Budget and none exist.

**What is *not* missing is a housing mechanism, and the long run's 53% queueing should not be read as
one.** `rulesets/minimal.toml` gives every dwelling an `upkeep` Rule drawing on a Resource nothing in
the file produces, so every Building is condemnable 64 Ticks after it is raised and the city holds ~60
of ~120 Lots at equilibrium — 180 places for 360 Households. **That is the fixture's arithmetic, stated
in its own header, not a defect.** *Everybody is housed* is a property of a Ruleset's **balance**, and
no Ruleset that models a city exists yet.

### The five numbers to hold in your head

| | Number | What it means |
|---|---|---|
| **The good one** | **8.72 ms a Tick at 1M — 55.9% of the budget at 4×** (S0b) | The **only** Tick figure ever taken from a real running city. Everything else priced is a spike's paper |
| **The sum** | [`0013`](0013-tick-budget.md) reads **≥17.8 ms a Tick** — the same bill as ≥114% at 4×, ≥229% at 8× and ≥57% at 2×, which are one number over three product decisions | *The simulation as priced fits at 2× and does not fit at 4×* — and **which speed is the target is a product decision nobody has argued** |
| **The row that decides it** | **Routing is 9.4–10.5 ms of those 17.8** | Without it the ledger reads 7.3–8.4 ms and fits a 4× rung with room. Its unit came off a synthetic harness; its multiplicand counts **the wrong event** |
| **The correction with a known direction** | A diverting Traveller re-searching costs **134.135 ms a Tick** at target scale — 861.87% of a 15.6 ms budget, up to 2,387.73% across the band. ⚠ **Carry the bill**: that percentage is now *right by cancellation* (`adr/0096` reprices at 62.5 ms, `adr/0094` multiplies routing by 4, and they cancel to 0.4%) | It points **up**, and the cache cannot rescue it (88.5–95.9% hit rate needed). **Session D took the design decision on 2026-08-10** — [`adr/0061`](../docs/adr/0061-a-diversion-rejoins-by-local-descent-and-a-rejoin-is-never-a-search.md): a Rejoin is local descent and **there is no search**. The number is answered rather than reduced; what is now owed is the rejoin success rate on real demand |
| **Scale** | 1M Citizens in **86 MiB** of tables, **177 MB** resident with a Ruleset in force; 100,000 Ticks in 11.75 s | The sizing risk is retired. **One State Hash is 32.47 ms — 2.08 Tick budgets**, so a per-Tick hash is unavailable at target |

**The meta-figure, and it is the one to be uneasy about.** *Every* time a fixture has been replaced by
a real world, the number came in **worse**: the Rule engine's unit cost by **2.8×** (552 ns against a
synthetic 198.3), Trips per Tick by **32%** (85 → 112), the Zone Rule `sample` dimensionally wrong at
scale, and every pre-S0a Tick figure taken over an **empty world**. [`0013`](0013-tick-budget.md) now
states the general form: **a unit cost is a hypothesis until a real world has produced one** — and
routing's has never met a world.

**The pattern has its first counterexample, and it is instructive that it is not really one.** S5's
L5 moved the Lane model **1.50× in the good direction**, which no re-measurement in this corpus had
ever done. But it did not come from a fixture meeting a real world; it came from **the instrument
having a defect** — `IntegerMath.FloorDiv` computing a modulo on every call, so every `Fixed.Div`
was two 64-bit divisions. **So the rule survives and gains a second clause**: a number improves when
the thing being measured was never what the measurer thought, and *that* is the case where the
correction points down. Two of the ledger's measured rows — the Rule engine's **6.4 ms** and S0b's
**8.72 ms** — were taken on that substrate and are now upper bounds by an unknown amount.

**Known problems, none urgent.**

- **Routing does not fit, and R6.3 found the reason is not the one we had.** Starting a trip is cheap
  because a Citizen reuses a route it worked out once; what is expensive is a driver changing its mind
  mid-journey, which the design made routine and then removed the cheap way of serving.
- **The network runs out of routes, not road** — 87.25% of traffic on 1% of the carriageway, 90.87% of
  it empty, at 13% of holding capacity. One free-flow tree per District means one route per (node,
  District) pair in the whole model. A design question, in [`0002`](0002-open-questions.md) §C.
- **The synthetic city fixture and `World`'s table sizing disagree** and nothing checks that they
  agree; Households land at exactly capacity, so the first one the simulation creates grows the table.
- **The governor was the wrong axis to worry about, and S2's re-captures proved it twice.** *Every S2
  and S0a absolute is `powersave`, mis-pinned, or both* was the standing caveat; **co-tenant load
  dominates the governor** where absolutes are fragile — a `powersave` run on an idle machine beat a
  `performance` run on a busy one. **The R8 re-capture is the controlled version of the same
  experiment**: two `performance` captures on *matched* machines (CPU stall 0.13% vs 0.11%) agree on
  **724 of 744 cells within ±2%**, every non-timing figure **bit-identical**, worst case 1.157× —
  against the mismatched R0–R4 pair's **1.77×**. *The variance S2 called run-to-run noise was almost
  entirely machine state.* **No capture filename records load.** S0a still owes its own re-capture and
  inherits the correction.
- Several documents still describe behaviour that later measurement contradicted — see *Owed*, and
  [`0012`](0012-corpus-audit.md) for the corpus-wide sweep's half.

---

## Do these next

**Build. The argument track is not the constraint, and treating it as one is how this project starts
going in circles.** That is a correction the board made to itself after a session in which every
decision generated two or three more — *the design was generating design*. **The standing rule: an
argument session runs when something concrete is blocked on it, and never because it is available.**

The three tracks do not contend — the code track is somebody at a keyboard, the argument track is a
sitting, the spike track is a machine running unattended — but **the code track leads**.

| | Track | Task | Plan | Why this one |
|---|---|---|---|---|
| ~~**1**~~ | ~~code~~ | ~~**Slice 10 task 11 — `revisit_ticks`.**~~ **DONE 2026-08-10 — all five sub-tasks, two baselines re-recorded. → [`0014`](0014-zone-rules-and-the-sweep-family.md) → *Task 11 as built*.** At 1M the shipped Ruleset now raises **2,898 Buildings** where it raised none, and the Tick got **8% cheaper** doing 117× the Lot evaluations. **The finding that outlives it is about the baseline rather than the sampler**: a derived sample of 1 at 132 Lots never lands on a cleared Lot in eight triggers, so the golden session **silently stopped covering the create branch** — every hash moved, every test passed, and the committed trace covered half the mechanism. Session lengthened 256 → 2,048 Ticks, and a test now asserts both branches ran | [`0014`](0014-zone-rules-and-the-sweep-family.md), [`adr/0059`](../docs/adr/0059-a-zone-rules-sample-is-a-revisit-period-so-the-ruleset-states-a-duration.md) | A closed slice shipped a defect and its own tripwire hid it |
| ~~**1b**~~ | ~~code~~ | ~~**Hash-moving queue item 3 — `adr/0064` + `adr/0065`, one commit.**~~ **DONE 2026-08-10 — capacity is `Rows.Derived<long>` and rebuilt at load and at every swap, `level` and the whole write path are `long`, the end-of-run check is `Invariant.BinCapacityMatchesItsDeclaration` (id 29), one baseline re-recorded. → [`0018`](0018-session-n-the-bin-the-pool-and-the-economy.md) → *Tasks 3 and 4's implementation record*.** **With it the code track is empty and the queue is closed.** ⚠ **The finding that outlives it is an ADR being wrong about the code**: `adr/0064` recorded a live defect on the ground that the loader refused nothing for a duplicate `(kind, Resource)`. It has refused it since slice 7 task 8 — and that refusal was the **one guard in the loader with no test**, so the suite you read to find out what the loader refuses did not name it. A fact with **no copy at all**, re-derived wrongly from the shape of its absence | [`0018`](0018-session-n-the-bin-the-pool-and-the-economy.md), [`0003`](0003-build-plan.md) → *The hash-moving queue* | A guard with no test is invisible to the next reader, including the one deciding it does not exist |
| ~~**1c**~~ | ~~code~~ | ~~**Hash-moving queue items 4 and 5 — `adr/0068` then `adr/0069`.**~~ **DONE 2026-08-11 — both, two commits, both baselines re-recorded, and the queue is empty again.** ⚠ **Its stated acceptance was wrong in both halves.** The five-sixths equilibrium does **not** close: 83% homeless becomes **53%**, and the residue is `rulesets/minimal.toml` demolishing its whole housing stock on purpose. What the pass fixes is **vacancy** — 45% of the stock empty while 70% queued, now **10%** — which is what `PlacementLongRunTests` asserts, because *everybody is housed* is a property of a Ruleset's balance and the shipped one declines to have one. And it needed **three** hash-bearing numbers where the ADR predicted none. | [`0018`](0018-session-n-the-bin-the-pool-and-the-economy.md) → *What shipping task 2 found*, [`0003`](0003-build-plan.md) → *The hash-moving queue* | **The board's own rule, tested and held**: the argument track moved work into the code track, and the code track then refuted two of the argument's predictions. |
| ~~**1d**~~ | ~~code~~ | ~~**Hash-moving queue item 6 — `SyntheticCity` paves what it populates.**~~ **DONE 2026-08-13 — one commit, all three golden baselines re-recorded, and `adr/0089`'s map flip is unblocked.** `RoadGenerator.LayInto` takes an extent; `SyntheticCity` derives it from `World`'s own 225 Lots per 1,000 Citizens. The golden world goes **16,641 Nodes / 33,671 Segments → 36 / ~60**; at 1M it asks for a 4,800-Tile lattice, **8.6% of a 512-Cell map**. ⚠ **Its stated blocker was never true** — the generator lays nothing *at world creation*, only when `SyntheticCity` runs, so `adr/0021` was not violated where two documents said it was (`adr/0093`). **Three consequences nobody predicted**: `arterial_count` goes to **0** in both shipped Rulesets, because an Arterial is a road nothing in the simulation can build; the golden fixture goes to **4,000 Citizens** so its commands fit a 10×10 lattice; and two long-run tests were found **asserting a distribution property from a single draw** and now sweep five seeds | [`0003`](0003-build-plan.md) → *The hash-moving queue* | A test that draws once cannot tell an outlier from a regression |
| ~~**2**~~ | ~~spike~~ | ~~**S2 R7's tail.**~~ **DONE 2026-08-11 — every item R7 owed that is available is closed.** The second `performance` capture, the R0–R4 re-verification, the three R8 direction sentences, tripwire row 5 re-filed to Phase 2, the in-flight band corrected, and the unbacked figures now printed as tables by the harness. The owed list was **stale in four of seven entries** and is reconciled. **Three findings outlive it**: two captures are an error bar only if the machine is the same twice — **co-tenant load beats the governor** past L3; the in-flight band is a range over a **product** read as a sensitivity to one factor, so **111,000 means three different cities**; and R8's damping ratio and its fire rate **do not share a sign** under `adr/0047`. **Closed out the same day by a canonical R8 re-capture**, which gives the 92.28% damping ratio and the 12 MiB L3 artefacts rather than prose, and by amending [`adr/0047`](../docs/adr/0047-routing-never-keys-on-the-district.md): its free-diversion figure is a **range — 3.18–3.45%** — published as a point estimate off a single capture | [`0010`](0010-s2-routing.md) → *R7* | ⚠ **And the fourth outlives all three: the block on deleting the harness cleared without anyone noticing.** R7's *not available* rests on two named gates — R6's invalidation half, which is session **M**'s, and the question R6.3 put in front of it — and **both closed in other documents**: M shipped the contract as an amendment to `adr/0012`, and session **D** task 1 closed the diversion question into `adr/0061`. Three live documents still say S2 cannot close. **`0012` *Cause 1* on the gate rather than on the fact** |
| ~~**3**~~ | ~~argument~~ | ~~**Session D — `03 §5`, the traffic model.**~~ **DONE 2026-08-10 — all five tasks, in one sitting. → *Done / Sessions*.** *(Original row kept below the strike because it is the record of why a session was booked against a number.)* The first session ever booked against a **number** rather than against a document being ungrilled. **The brief is [`0017`](0017-session-d-the-traffic-model.md)**, and its typing pass is recorded there: eleven claims typed measurable, **one spike emitted (S5)**, two `adr/0007` defects filed to [`0012`](0012-corpus-audit.md). *(An earlier revision of this row read IN FLIGHT and was wrong: the parallel session in progress is **slice 8**, not D. Corrected rather than silently edited, because a board that reports a session as running is how two sittings end up on one document.)* | [`0002`](0002-open-questions.md) §A and §C | **The rule was applied, not suspended.** *An argument session runs when something concrete is blocked on it* — and what is blocked is [`0013`](0013-tick-budget.md)'s **dominant row**: routing is 60–67 of the ledger's 114 points at 4×, its multiplicand counts the wrong event, and **the only thing that can replace it is Trip generation, which is milestone 5b, which D gates.** So D is on the critical path to unguessing the one row that decides whether the simulation fits. **Book it as more than one sitting**; it now runs fully beside code, since the half that wanted S2's numbers has them |
| ~~**3**~~ | ~~spike~~ | ~~**S5 — the Lane kernel.**~~ **RUN 2026-08-10, all four rounds twice plus a BenchmarkDotNet cross-check; PUBLISHED 2026-08-11 into [`spike-results`](../docs/spike-results.md) → *S5*, [`0013`](0013-tick-budget.md), [`adr/0016`](../docs/adr/0016-the-lane-is-the-entity-not-the-car.md) and [`0002`](0002-open-questions.md).** **~325,000–330,000 Vehicles a Tick a core** with Overlaps, **~4,510–4,580 Microscopic Segments in 15.6 ms**, and `0013` has the Microscopic row it had **never** held. **T3 fired and stands** — its *"constant of a `memcpy`"* is **17–29× a bare walk**. **T1 fired and was withdrawn hours later by L5**, which found `IntegerMath.FloorDiv` evaluating a modulo on every call: **every `Fixed.Div` in the project was two 64-bit divisions**. Correcting it is **bit-identical** — 1,060 tests green, no golden baseline moved — worth **1.50×**, and it takes the Lane model to **~533,000–570,000 Vehicles a Tick a core**, *above* the 400,000 the corpus had treated as unreachable. **The gap was never `adr/0003`'s arithmetic.** The §D2 row this opened retired the day it was written. **Owes the canonical `performance` capture** — no verdict turns on it now — and **what the correction is worth at every other division site**, which S5 cannot speak for | [`0019`](0019-s5-lane-kernel.md), [`spike-results`](../docs/spike-results.md) → *S5* | **The rarest thing in the corpus, and it stayed that way**: a spike that blocked on nothing, contended with nothing, and came back with the direction of surprise pointing **down** for the first time — every absolute is a `powersave` lower bound, so the one number that disappointed can only improve. **And it is a negative sighting of scatter ≈1.5**, the first: this kernel is arithmetic-bound, so there is nothing for scatter to move |
| **4** | spike | ⚠ **STILL BLOCKED 2026-08-11, on a NEW gate — do not delete `spikes/S2.Routing/`.** **The 5a gate is discharged**: the port is done, nothing in `src/` or `tests/` compiles against the harness, and the only references left are two doc-comments citing paths. **But another session is doing further research inside it**, so it is live work, and that is the gate now. *Previously:* ✅ **HOLD DISCHARGED 2026-08-11 — the port is done and nothing reads the harness, so this is now waiting only on somebody saying go.** Milestone 5a shipped; nothing in `src/` or `tests/` compiles against `spikes/S2.Routing/`, and the only remaining references are two doc-comments citing files by path. It is **51 tracked C# files and 29,719 lines** — 92 files and 42,914 lines counting the `results/` reports — and `Borough.slnx` still lists the project — a deletion that size is taken deliberately rather than as a consequence of a green suite. *Original row:* ⚠ **ON HOLD 2026-08-11 — do not delete `spikes/S2.Routing/` yet.** `Graph/` is the **reference implementation of milestone 5a**: a directed CSR Road Graph, mode mask on the arc, the saved/derived split marked per column, `adr/0014`'s generator with real severance, and `Matrix/Connectivity.cs` — the component test the District Pool's membership test needs. The deletion moves to the **end of the Road Graph slice**, where the closing condition becomes *the port is done and nothing reads the harness* rather than *the report is written*. See [`0010`](0010-s2-routing.md) → *R7*. *Original row:* **Close S2 — delete `spikes/S2.Routing/` and record the deleting commit's parent.** R7's last act, and the **only** thing between S2 and closed. Both gates cleared without either clearance reaching S2's plan, so read [`0010`](0010-s2-routing.md) → *R7* and satisfy yourself before removing 33,000 lines | [`0010`](0010-s2-routing.md) → *R7*, [`spike-results`](../docs/spike-results.md) → *S2* | **It became available by two other documents closing**, not by any work on S2: session **M** shipped the invalidation contract into `adr/0012`, and session **D** task 1 closed R6.3's diversion question into `adr/0061`. Same discipline as S4 — whose task 11, deleting `spikes/S4.Kernels/`, **is also still open** |
| **6** | code | ✅ **DONE 2026-08-11 — 5a-bis, all seven tasks. 1,073 tests green, all three golden baselines re-recorded.** Three ADRs: [`0077`](../docs/adr/0077-a-road-edit-is-one-segment-and-the-player-lays-streets-only.md) *a road edit is one Segment and the player lays Streets only*, [`0078`](../docs/adr/0078-frontage-is-derived-on-the-epoch-and-a-lots-width-is-the-segments-own-building-count.md) *frontage is derived on the Epoch*, [`0079`](../docs/adr/0079-a-building-outlives-its-frontage-and-an-address-that-has-none-is-a-hole-the-trip-model-reports.md) *a Building outlives its frontage*. **The slice's sharpest finding is about the re-record, not about Lots.** Zoning a Tile now zones the **block** it falls in, and eight of the golden session's eleven `zone` commands named Tiles 0–31 — *one* block, and one the populator had already carved. A straight re-record would have turned eight commands into no-ops and **retired the verb from the baseline while producing a full set of freshly correct hashes**. That is slice 10 task 11's finding on its **second outing in three slices**, so it is a test now rather than a paragraph: `GoldenSessionCoverageTests` asserts what the session *reaches*, which no hash test structurally can. Three more outlive it. **A guard that covers one of two identical files is worse than no guard** — the ruleset-hash check covered `minimal.toml` and not its twin, so `TunedRulesetHash` was a literal nothing held to a file and had been wrong since the file was last edited; the catalogue pairs a *stated* hash with a *loaded* one, so nothing noticed. **A derived structure that caches a Ruleset value reads as *absent* rather than as *stale* before its first rebuild**, and absent is the state every guard is written against. And the 100,000-Tick run found the lay/bulldoze cycle **closes but is not synchronous** — a Building on the bulldozed face survives, so its Lot's freeing waits on the Zone Rules; *a test that demands synchrony is testing the Zone Rule's cadence while claiming to test the subdivider* — plus a real defect, a Lot vacated by demolition while unfronted was never freed, because re-subdivision runs on a **road edit** and a Lot can be vacated on any Tick. **Three of the brief's five recommendations were refused, each differently, and the sitting's finding is that a brief is evidence about what its author could see rather than a specification.** Decision 1: *"one grid-snapped Street **Tile**"* rests on two accurate `adr/0014` quotations and a graph that does not exist — `RoadGenerator` puts nodes only at intersections `block_tiles` apart, so a Street Segment is a whole **block face**. ***A quotation can be accurate and its conclusion false***, which is `adr/0070` on a **presence** rather than an absence. Decision 4: *"declines through `adr/0053`'s existing machinery… it needs no new mechanism"* — `Condemn` is keyed on a starving **Rule Instance** and a bulldozed Street starves nothing, so ***citing a mechanism is not checking what it is keyed on***. Decision 3 was closed by **session F** hours earlier and the brief's *"it does not contend with session F"* reads as independence. And decision 5 predicted *"two or more"* hash-bearing numbers: it is **one**, and it was already written down — `CONTEXT.md` → Address's **five Buildings per Segment**, whose 33,024 Segments against *"~30,000"* and 165,120 Lots against `World`'s independently-chosen 225,000 are **two figures that never met agreeing within a quarter**. Depth is deleted, not chosen. ⚠ The brief also **contradicts itself**: task 7's *every Lot has frontage* is false under its own decision 4, corrected rather than deleted | [`0022`](0022-the-lot-subdivider-and-build-road.md), [`0020`](0020-the-road-graph.md), `02 §2.2` | `02`'s admission is discharged: *every Building is on the Road Graph by construction* is now true **by construction** rather than for want of a Road Graph |
| ~~**5**~~ | ~~argument~~ | ~~**Session F — `adr/0008`, *walking is a simulated Leg*.**~~ **DONE 2026-08-11 — one sitting, all seven decisions, and it closed no measurable claim. → [`0021`](0021-trips-legs-and-the-pedestrian-layer.md) → *What session F decided*.** **5b's second gate is discharged, both its gates are now clear, and the whole movement track is unblocked.** [`adr/0008`](../docs/adr/0008-walking-is-a-simulated-leg.md) amended in place — four consequences and the revisit trigger — plus **three ADRs**: [`0074`](../docs/adr/0074-side-of-street-is-a-property-of-the-access-point-not-of-the-graph.md) *side of street is a property of the Access Point*, [`0075`](../docs/adr/0075-a-leg-is-a-plan-and-a-traveller-is-a-cursor.md) *a Leg is a plan and a Traveller is a cursor*, [`0076`](../docs/adr/0076-the-trip-fate-set-is-closed-at-four-and-a-fate-names-the-journey.md) *the Trip Fate set is closed at four*. `CONTEXT.md` gained **Address** and **Node** and had six entries amended; `0002` §D gained two rows and had one corrected; six defects went to [`0012`](0012-corpus-audit.md), one paid in the sitting. ⚠ **Four findings outlive it.** The brief was **wrong about the amendment's size in both directions at once** — *"sidewalk edges… **and** crossing edges"* is two claims in one breath, the first refused and the second **correct and already built by 5a**, so *an `and` in a consequence is two consequences*. **A revisit trigger can be spent before it is written**: *one pedestrian edge per block face* was not made expensive by `adr/0072`, it was **already what the graph is**, so the ADR's only stated lever never existed. **A placeholder whose value sits inside the range of legitimate answers cannot announce itself** — a zero-length parking walk is what a Building with a garage genuinely produces — **and this one pays the player for the shortage**, since a vehicle Access Point reachable as a fallback from an exhausted Parking Shed makes a full car park cost *less* than an empty one. And **side of street was nearly surrendered by collapsing two questions into one**: whether it is *modelled* is independent of whether it is *in the graph*, and `adr/0009`'s deferred table had asked for it years of documents ago. *(Original row kept below the strike, because it is the record of a gate that was nearly struck for the wrong reason.)* **PROMOTED 2026-08-11, and it is now the only thing between here and the next code.** Brief: [`0021`](0021-trips-legs-and-the-pedestrian-layer.md) → *Session F's brief*. **F gates 5b, which is the whole movement track** — `0002` §F: *"makes 5b **the irreversible milestone**, so it is owed **before** the Leg model is built."* ⚠ **5b has two gates and D cleared only one.** D's blockquote below says *"milestone 5b, which D gates"*, and `CLAUDE.md` repeats it; the gate table lists **D and F**, and F is unstruck. That is `0012` *Cause 1* on the gate rather than the fact, and it is the same shape [`0020`](0020-the-road-graph.md) warns about for row 4 above — *a deletion blocked twice for unrelated reasons is the row that gets struck for the wrong one.* **One sitting, not more**, because **session D task 0 already ran F's typing pass**: §B-16 and §B-17 are its output and F may close neither. **The finding that makes F obviously real work**: `adr/0008` says the network needs *"sidewalk edges alongside street edges"*, which is the structure [`adr/0072`](../docs/adr/0072-the-mode-mask-is-saved-on-the-arc-and-the-segments-is-derived.md) **rejects by name** — it predates the mode mask and was never amended, and nobody noticed because nobody has built a Leg | [`0021`](0021-trips-legs-and-the-pedestrian-layer.md), [`0002`](0002-open-questions.md) §F | **The standing rule applied rather than suspended**: an argument session runs when something concrete is blocked on it. What is blocked is the **whole code track**, and behind it [`0013`](0013-tick-budget.md)'s two broken movement rows — routing's multiplicand counts the wrong event, the Lane model has none at all, and *"routing needs 5b's Trip generation to fix a multiplicand, and the Lane model needs 5b's stress counts to acquire one"* |
| **7** | code | ✅ **DONE 2026-08-11 — the Severance sweep, run before 5b starts, and it overturned the claim 5b's demonstration was going to rest on.** 240 `[roads]` configurations against an Arterial-free control. **The shipped Ruleset severs nothing** — 0.0% at every `foot_crossing_every` in `1..16` and every Arterial count up to 32, and zero stranded walkable nodes on seven of eight seeds — so `0002` §D1's *"the shipped 32/8 sits well inside the regime where the dial works"* is **measured false**, and S2 R0.5's contradicting number was right all along. **The struck claim generalised from two observations differing in two variables** (512-with-two against 256-with-eight) and attributed the difference to the wrong one: severance is monotone **increasing** in block size, because the key states a **ratio** and what reconnects a city is an **absolute count of crossings kept** — 306 at block 32, 16 at block 512, dial fixed. **Three findings outrank the correction.** `--roads` **announced Severance over a city that has none for the whole of 5a**, because its verdict compared component *counts* and 65 of the shipped world's 66 foot components are Arterial junctions nobody can walk to — `RoadSeveranceTests` diagnosed exactly this on day one and left the `Walkable` filter in the test file, so ***a predicate discovered in a test and left there is a correction with a blast radius of one call site***. **`foot_paths_per_thousand_blocks` is a second and stronger dial** (2.0% against 43.6% at the shipped lattice) that no document names as one. And **every Severance figure ever recorded here was a single draw**, because `--roads` refused `--seed` — the polyline comes from the world key, twelve seeds span 0.0%–23.5% at a nearby rung, and ***a generator whose output cannot be varied cannot be characterised***; the guard that stopped it being varied was written to refuse something else and caught this by category. Shipped: `RoadConnectivity.StrandedOnFoot`, `--roads --seed`, `rulesets/severance.toml` (characterised over eight seeds, floor 32.3%), two tests, and corrections to `0002` §D1, `0020`, `0021` task 7, `CLAUDE.md` and `RoadFixtures`. **1,076 green.** ⚠ **What it does *not* settle**: all of it measures **disconnection**, and the half that decides whether a Building declines is **detour**, which needs a shortest path — and **nothing in `Borough.Core` searches the graph**, which is 5b's first unowned decision | [`0020`](0020-the-road-graph.md) → *the 2026-08-11 amendment*, [`0002`](0002-open-questions.md) §D1 | **The measurable was routed to a machine rather than to a sitting** (`adr/0043`): *does the shipped Ruleset sever anything* names its refuting number and the instrument that produces it, so no argument was entitled to close it. It had been closed by argument, in a brief, and the argument was wrong |
| ~~**8**~~ | ~~code~~ | ✅ **DONE 2026-08-12 — 5b task 5, and with it milestone 5b. Phase 4 runs, behind `CommandKind.Trip`.** Shipped: the verb, with the destination packed as a signed **block delta** into `Command.Zone` on `ConnectPayload`'s precedent — the log codec is text and a sixth field would bump the version, costing every log ever written; `TripEngine` advancing the Traveller cursor and releasing what has ended, in **one sweep** so there is exactly one place that reads a Fate and one that frees a row; a **fourth Census metric family**, four `TripCounter` flows drained on reading; `Invariant.TripHasAFate` at the release site and `Invariant.SegmentVolumeIsConserved` at end of run, both with a test that writes the violation; and the three Movement tables joining `World._tables`. **All three baselines re-recorded. 1,136 green** against 5a's 1,060. ⚠ **The one thing it owed and could not build is volume, and the expected reason was not the binding one.** This row said *"nearly vacuous… `adr/0041` increments on vehicular Legs only, and 5b has none"* — true, and it predicts that adding a vehicle fixes it. **It does not.** `adr/0041`'s own amendment needs *"a **next Segment** every Tick"*, and [`adr/0075`](../docs/adr/0075-a-leg-is-a-plan-and-a-traveller-is-a-cursor.md) gives a Leg **a cost and no path** with no next-hop table anywhere: ***a decision that removes a representation defers every decision that reads it***, and neither ADR says so. Volume waits on a **path source** ([`0010`](0010-s2-routing.md) decision 11). `adr/0041` amended, filed to [`0012`](0012-corpus-audit.md). ⚠ Second finding, smaller and live: **the State Hash version byte's rule had been decided twice in silence** — the clock bumped `World.HashSeed`, 5a's two tables and 5b's three did not, and the distinction (*existing state re-composed* against *new state, which is a design change under `05 §4`*) was written nowhere. Now a comment on the constant | [`0021`](0021-trips-legs-and-the-pedestrian-layer.md) → *Task 5's findings* | **It closed milestone 5b**, whose remit shrank on 2026-08-12 rather than whose blockers cleared. Tasks 4, 6 and 8 left for 5b-bis because each measures a **distribution** and no generator exists |
| **9** | code | ✅ **DONE 2026-08-13 — 5b-bis, all eight tasks, 1,276 green, three baselines re-recorded in tasks 2, 4 and 5 rather than at the end.** Brief: [`0023`](0023-jobs-and-the-commute.md). ⚠ **The milestone-level finding is that four of its eight tasks found something about an *instrument* rather than about jobs** — task 3's crossing-cost derivation does not exist, task 6's cost histogram cannot ratify the number it is about, task 7's minute formatter had been dropping the sub-Tick fraction in **every duration it ever printed**, and task 8's peak test was **measuring its own sample size**. A missing derivation, a censored distribution, a lossy conversion and a small-sample maximum: four mechanisms, one consequence, which is that a number came back and could not settle what it was taken to settle. ***`adr/0043` types a claim by whether a machine could produce the number, and every one of these produced it.*** **Task 8, the 100,000-Tick run**: the collection half closes — `TripTable`, `LegTable` and `TravellerTable` joined `World._tables` in 5b and had never met a long run, and at 10,000 Citizens over 100,000 Ticks all three are flat on **slots** (22 / 145 / 22 / 145, with an identical high-water mark at 50,000). **The Commute Budget is binding hard**: `jobs beyond budget` never reaches zero and **4,561 of 10,000 hold a job against 9,608 posts declared**. `trips no route` and `trips stranded` are **0 throughout**, so two of `adr/0076`'s four Fates are unexercised by any run. `adr/0081`'s **no-wage bias** held for the whole milestone and stays attached. **Tasks 1–7 as built:** **Task 1, the spatial index** — `Space/BuildingResidency.cs`, a Cell → Buildings reverse index, `(derived AND rebuilt)`, with a hot query that is allocation-free and string-free and a test that a rebuild reproduces it **exactly** across a recycled slot. No baseline moved; `cell_next` is derived. **Task 2, `[[building]] jobs`** — `adr/0068`'s rule on a second axis, plus the **worker list** `CitizenTable` has been saying since slice 10 does not exist, two invariants (**38**, **39**) and eighteen tests. ⚠ **Its finding is that the key exposed a live defect nothing could previously report**: `SyntheticCity` gave every Citizen a Workplace on a `(i * 7)` stride, which was 1,000 jobs no Ruleset granted, and the first adoption after a ceiling existed dismissed all of them. ***A quantity nothing can count cannot be contradicted*** — `adr/0070` from underneath, and *a mechanism's first appearance is also the first audit of every fixture writing its state by hand*. **The stride is deleted here rather than in task 4**, so the three baselines re-recorded **once** and task 4 now only adds. `06` milestone 5b-bis — jobs, the commute, and the first Trip generator.** New milestone, created 2026-08-12 by the §A sitting ([`adr/0081`](../docs/adr/0081-the-commute-is-the-first-trip-generator-and-a-job-is-taken-by-satisficing-on-distance.md)). **Its first task is not the generator**: there is **no query from a place to the Buildings near it** anywhere in `Borough.Core`, and all three candidate generators needed one. Then a `[[building]] jobs` key, a job-assignment pass replacing `SyntheticCity`'s `(i * 7) % buildings` stride on `CitizenTable.Workplace`, the commute generator itself, and 5b's transferred tasks 4, 6 and 8. **Task 3, the `[trips]` Ruleset table** — built the same day, with `TravelTime.FromSeconds`/`FromMinutes` on `Speed.FromKilometresPerHour`'s precedent and `adr/0082`'s denomination, and both live call sites that had been passing `TravelTime.Zero` now read it or **refuse**. The **crossing cost is 30 s**; ⚠ **the derivation was looked for and there is none** — no signals and no Segment width, so both candidates rest on unbuilt mechanisms (`adr/0070`) — and *a search for a derivation that fails is a result that has to be written down*. `--trips` has already run half its ratifier: the term moves the 1-block band's **p90 detour 140% → 153%** and **no other band at all**, which is `adr/0074`'s blast-radius claim measured. The **Commute Budget stays unset**, as `[trips]`' one optional key, because ***a value that means "unset" has to be outside the range of legitimate answers*** and every minute count is a legitimate Budget — so the omission states a city with **no ceiling**, which is also the only city whose cost distribution is uncensored and therefore a **precondition of the ratifier** rather than a convenience. **Task 4, the assignment pass** — `EmploymentEngine` in Tick phase 6 behind placement, a `[jobs]` Ruleset table, a **four-counter** `JobCounter` Census family, and the **Commute Budget set at 20 minutes**, read off the distribution task 3 said would have to exist first. A Citizen with no Workplace draws candidates from a box around home and takes the first with a free slot it can **walk to inside the Budget** — `adr/0017` satisficing against a real filter for the first time, since placement's copy scores every candidate identically. **The box is derived from the Budget and there is no radius key**, so the loader **refuses `[jobs]` in a Ruleset with no `commute_budget_minutes`**: one number doing one thing, against two that can contradict each other in silence. ⚠ **Its load-bearing finding is that the Cell-uniform draw is right on paper and unusable here** — the shipped Rulesets hold ~1,200 Buildings across 16,384 Cells, so drawing land rather than rows finds an employer **one occasion in four hundred**, and ***an argument about what a draw means is independent of whether the draw ever hits anything***. Two more: **a Budget chosen against the map is not thereby exercised by every world on it** — 20 min is inert on the 1,000-Citizen golden fixture and binding at 10,000, slice 10 task 11 a fourth time, now a test in both directions; and `jobs` landed on the **dwelling** kind rather than a workplace kind, because a second kind needs a second `[[zone_rule]]` and a second decline Rule or the city fills with offices. All three baselines re-recorded. **Task 5, the commute generator and the daily occasion** — `CommuteEngine` in Tick phase 4 ahead of `TripEngine.Advance`, `TripPurpose.Commute`, and **`CommuteRoster`**, a `(derived AND rebuilt)` partition of the population by departure phase. **A Citizen with a Workplace and a home walks to work once a Day, and it is the first Trip in this project nobody asked for** — `TripEngine` needed **no change**, which was `adr/0080`'s stated bet and is now a measurement. **The occasion is a *phase*, not a schedule**, and the argument is two constants side by side: a commute recurs every Day, `EventWheel.Size` **is** a Day, so an armed Citizen never leaves its bucket and ***a bucketing on a constant is derivable rather than scheduled*** — no saved column, no re-arm, no generalisation of the Wheel. The phase is drawn from the **id** rather than the slot, because `SyntheticCity`'s slot order is a fixed stride through the Building table and a city that departed by slot would leave in a **spatial wave**. ⚠ **Its finding is that `commute_peak_factor` was going to be a fifth free hash-bearing number and is a restatement of a measured one**: under a uniform window of `W` Ticks the departure rate is `TICKS_PER_DAY ÷ W` times the daily average, so the **peak multiplier and the window are one quantity seen from two sides** — S2 R7 measured the multiplier (2–3×) and nothing has ever measured a window, so the file states the evidenced side and `JobRuleset.CommuteWindow` derives the other (`adr/0059` a **fourth** time). **3 implies an eight-hour window**, which is exactly what 3× means; the **shape** is what nobody has measured, and there is **no `peak_offset`** because nothing in the simulation asks what time of day it is. Two smaller: **the overlap guard is unnecessary because of a refusal written for another reason** — `[jobs]` cannot load without a Commute Budget, so an in-flight commute is bounded in minutes against a 24-hour Day; and **`TripPurpose.Commanded` was scheduled for deletion and is kept**, since `adr/0080` demotes it to *a test affordance rather than the only door* and ***its own rule became checkable on the day it stopped being vacuous***. The golden session now contains Trips at last — task 3 shipped the Trip model wholly outside the baseline — though at **2,048 Ticks against a 2,731-Tick window** nobody in it departs twice. Both session baselines re-recorded; `world-hash.txt` did not move. **Task 6, the Trip Census family and the cost distribution** — `TripCostBucket`, a **seventh Census family** and the corpus's first histogram: seven bands of clock minutes, filled at Trip *creation* so a Trip refused for its length is in the distribution with the cost that refused it, and the buckets therefore sum to Trips **made** where the Fates sum to Trips **ended**. ⚠ **Two of the task's premises were false and finding that out is the task.** *The four flows are already read* — they were not: 5b wired `TripCounter` through the Census and printed it **nowhere**, so for a whole milestone the only reader of a Trip Fate was the test suite, which is ***a consumer no operator has***. That is `adr/0064`'s *a guard with no test* on a second axis, and both are **a fact with no copy at all** rather than two that drifted; `CensusReportTests` is the guard, and it asserts a **family** appears rather than a number. And `TripFate.ExceededCommuteBudget` became reachable in **task 4**, not here — reached in an ordinary run by a mechanism nothing predicted, the **placement pass moving a Household after the job was accepted**. ⚠ **The second finding is that this distribution cannot ratify the number it is about**: a commute exists only where the Budget already admitted one, so lowering the Budget **collapses** the histogram into its shortest band instead of growing a tail — twenty commutes at a one-minute Budget, every one under a minute. ***A distribution censored by a number is not evidence about that number***, which makes task 3's refusal to set a Budget before `--trips` ran a measurement rather than an argument. The ladder is deliberately **not** denominated in the Budget (***a ruler must not move with the thing it measures***) and its edges are an instrument's resolution rather than `adr/0052` numbers. **No baseline moved** — the Census folds into nothing. **Task 7, something to look at** — `--commute`, the **seventh runner mode** and the **second that steps the world** (`--zones` is the first, for the same reason: employment happens over time, so unlike `--roads` and `--trips` this picture has a real *before*). It prints **where people work against where they live, by block**, and the quantity is a **balance rather than a count** — a grid of worker counts is a grid of population, which `--zones` already draws; what is new is which way people move in the morning. The before frame is the control and is the strongest thing in the output: at Tick 0 nobody is employed anywhere, so every block exports its whole population and none takes anybody in. Refuses a Ruleset with no `[jobs]` for a sharper reason than the usual one — *a grid of universal export is what this dump prints on purpose in its first frame*, so printing it in the second would make a broken pass and a file that grants no work indistinguishable. ⚠ **Its finding is a defect in an instrument nobody was auditing**: `--trips`' minute formatter **dropped the sub-Tick fraction before converting**, so every duration it has ever printed was short by up to one Tick (10.546875 s in-world) — which surfaced only because this dump prints the Commute Budget beside the file's own `20` and it came out **19.9**. ***A defect that only shows on a value you happen to know is a defect that hides in every value you do not***, and on `--trips`' 2.5-minute band — the one the crossing cost's ratifier is read off — it is **7%**. Fixed at the source per `adr/0073` — **and the fix broke a test in the way that test's own doc comment warns about**, since `20.0 min` contains the substring `0.0 min` that `No_band_reports_a_walk_of_no_time_at_all` forbids, three paragraphs below a note in the same file saying *parsed rather than substring-matched, because the substring lies*: ***a rule written down beside the code it governs is not thereby applied to the code next to it***. And the picture makes task 4's `jobs`-on-`dwelling` choice **visible rather than argued**: 222 blocks of 228 within a quarter of parity, because every Building has the same posts and the same occupants, so **this city has no land use at all** and the commute is a shuffle rather than a flow. **It also paid a debt task 5 left**: the commute generator's cost, owed to [`0013`](0013-tick-budget.md) under `adr/0073` and two days late — **~0.52 ms in a departure Tick at 100,000 Citizens, ~5.2 ms against a 15.6 ms budget at 1M, on one Tick in three, and it does not fall away as the city settles**. Isolated by a Ruleset with `[[building]] jobs = 0`, which loads and makes the pass sample without ever routing; the by-product is **~32.5 µs a walk search in a real world**, the first such figure the corpus has that is not a microbenchmark over a warm graph — ***a unit cost is a hypothesis until a real world has produced one***, a fourth time | [`0023`](0023-jobs-and-the-commute.md) | **Its named risk**: that every number 5b was to produce is taken against a fabricated origin-destination draw and lands in [`0013`](0013-tick-budget.md) and [`0002`](0002-open-questions.md) as measured fact. **S2 R4 already ran that experiment** — 18.52% detour on a uniform draw against **128.82%** on a local one, *"a different city"* |

~~**The argument track has two promoted rows and the rest is a menu**~~ — **it has none, as of
2026-08-11. Both promoted rows have run, and nothing in the argument track gates a slice.** See
*Open tracks*; the standing rule now holds for **everything** in it: **take from the menu when something
concrete is waiting, and leave it alone otherwise.**

**⚠ The code track is EMPTY as of 2026-08-13.** 5b-bis closed that morning, all eight tasks, and ~~nothing
in Phase 2 as sequenced is both unblocked and unbuilt~~ ⚠ **that clause is FALSE and was false when it was
written — corrected 2026-08-13 by an audit run beside item 6. Milestone 5c is both unblocked and unbuilt**,
and has been for two days: all three of its named gates were discharged elsewhere and **none of the
closures reached a gate board** — see the *Blocked* table below, where the blanket row is now split
per-milestone as its own text had been asking since 2026-08-12. So there is a **fourth candidate and it
is a whole milestone rather than a queue item**: `06` **5c**, statistical resolution and the travel-time
matrix, which also carries 5b's unpaid `adr/0041` volume debt that 7a's Stress reads. *It is listed last
rather than first on purpose: 5c moves every State Hash, and so do items 6 and 7, so it wants the tree to
itself.* **What is next is therefore a choice rather than a
queue**, and the ~~three~~ ~~four~~ **four candidates were, of which item 6 and the map flip have since shipped**:
~~[`0003`](0003-build-plan.md)'s hash-moving queue **item 7** — the
`TICKS_PER_DAY` flip, which is mechanical and should land before more Rulesets are authored, because
Goods quantities rescale ×4;~~ **✅ SHIPPED WHOLE 2026-08-13 — the clock half and the Goods rescale, as two commits;** [`adr/0095`](../docs/adr/0095-a-commute-budget-is-three-rungs-and-only-the-last-one-refuses.md)'s
**three rungs**, which is Ruleset content plus one comparison and is what makes 5b-bis's *binding hard*
reading interpretable; and ~~**[`0003`](0003-build-plan.md) queue item 6** — scoping `SyntheticCity`'s paving to the area it
populates, which is what [`adr/0089`](../docs/adr/0089-the-map-is-sized-by-how-many-commutes-fit-across-it.md)'s
map flip waits on.~~ ⚠ **That item is *mechanical*, and an earlier draft of this paragraph called it an
argument**: it named `plans/0002` **ledger #2**, *open map or progressive unlock*, as undecided when
[`adr/0090`](../docs/adr/0090-the-generator-makes-land-and-the-player-makes-every-road.md) had closed it
as **refused** the day before — by this same session. `plans/0012` **Cause 2**, and the sixth
`adr/0093` sighting in four days.

✅ **ITEM 7'S CLOCK HALF SHIPPED 2026-08-13 TOO — `Ticks.PerDay` and `EventWheel.Size` are 2048.**
All three golden baselines re-recorded. ⚠ **It was billed as "one line each" and
[`adr/0094`](../docs/adr/0094-a-day-is-2048-ticks-because-ticks-per-day-is-a-sampling-rate-and-not-a-length-of-life.md)'s
own rescaling inventory turned out wrong in two places, both of which would have shipped in silence**:
it says the Goods quantities are *the only rescaling*, and there were **three**, two moving **down** ×4
while the Goods move **up** ×4. `Speed.PerKilometrePerHour` was a **literal**, so a walk would have run
at **1.25 km/h** with nothing failing to compile; `revisit_ticks` was **actively misclassified by that
ADR's own table** and is struck from it with the user in the room. ***The name of a quantity is not its
denomination.*** Full record in [`0003`](0003-build-plan.md) → *The hash-moving queue* and the ADR's
*What building it found*.

✅ **AND THE GOODS ×4 RESCALE SHIPPED AS THE SECOND COMMIT — it was two rescales rather than one.**
The **Bin capacities** had to move with the Goods (`sundries` 12 → **48**, `repairs` 1 → **4**) and
nothing predicted it. ***A buffer is denominated in transactions, not in goods.*** In in-world time the
old and new pairs are identical — 22.5 minutes of supply either way — so measured in Goods nothing was
wrong; measured in **firings held**, every Bin in the game shrank from four to one, and a Bin that must
be *exactly full* for one `consume` to succeed is a knife edge. `adr/0094` considered dividing every
`rate` by four and rejected it **on cost**, and resizing the buffers is what the side it took owes.
⚠ **It costs something real**: a dwelling holds **90 in-world minutes** of sundries where it held 22.5,
so a city coasts four times as long through a supply failure before reporting it.

⚠ **The knife edge exposed a live defect, filed unfixed as [`0003`](0003-build-plan.md) queue item 8.**
A waiter whose **own** requirement falls is never re-checked: `adr/0063` made the wake predicate read
**live** state — the requirement is derived from a Readout on every call — and the only thing that calls
`World.Drain` is a write to the **Bin**. ***A live predicate with an event-driven trigger is only correct
if every input to the predicate is an input to the trigger***, and occupancy is not. Observed on four
Bins at once, stable from Tick 512 to 4096: queue **depth 1**, level 12, headroom 0, requirement 12,
`BinStillBlocks` **false**. It is filed rather than fixed because its two candidate repairs are a design
question — issue a wake on a Readout write, an edge `02 §5` does not have, or sweep the wait lists on a
cadence, which is `adr/0033`'s forbidden move.

✅ **ITEM 6 SHIPPED 2026-08-13, and it produced a fifth candidate by discharging a gate.**
`RoadGenerator.LayInto` takes an extent and `SyntheticCity` derives it from the Lot count the population
asks for; the golden fixture's world goes from **16,641 Nodes and 33,671 Segments to 36 and ~60**, and
at 1M the derivation asks for a **4,800-Tile lattice — 8.6% of a 512-Cell map**. ✅ **And the flip it
gated went in the same day: `CellGrid.WorldCells` is 512**, so `adr/0089` is built and there is no fifth
candidate. ⚠ **The flip moved no State Hash**, where three documents predicted every one would — `05 §4`
tests whether a change moves *this* city, and a map size is a bound on the cities that are *reachable*,
so a design change came back reading as an optimisation. It is neutral **because item 6 landed first**.
It cost **11.6 MB at 1M** against the ~4 MB `adr/0089` accounted for (a fifth map-scaled structure,
`StreetGrid`'s index), and it broke three fixtures that were laying at map extent — including
`rulesets/severance.toml`, which stranded **0%** of pedestrians and had **stopped demonstrating Severance
with no number in it changed**. Close-out in [`0003`](0003-build-plan.md) → *The hash-moving queue*. Two things it left
behind: **`arterial_count` is 0 in both shipped Rulesets**, because an Arterial is a player tool
`adr/0077` refuses in `Connect`, `adr/0090` keeps out of the generator, and `adr/0014` grants no
frontage — a road nothing in the simulation can build, sitting in the one structure the player does not
author, and 5a's sweep had already measured those eight severing 0.0%. And **the golden fixture is
4,000 Citizens**, which the session's commands had to be re-authored for — and which made the shipped
Commute Budget start refusing walks, so the committed baseline now reaches a branch it could not,
**acquired by accident**.

⚠ **A note on how this line got stale, because it is the fifth sighting of `adr/0093` in four days.**
On 2026-08-13, an hour after 5b-bis closed in this same tree, a sitting read *this paragraph* to answer
*what is next* and reported 5b-bis task 8 as the remaining work. `git log` would have said otherwise in
one command. ***A description of the build is where to look and never what you found*** applies to the
board itself, and the board is the document most likely to be read instead of the build — which is what
its own opening paragraph means by *a view, never a source*.

*Original line, kept because the strike is the record:* **So the code track is what is left, and as of
2026-08-12 it holds one row: 5b-bis.** 5a-bis shipped
2026-08-11 (row 6), and **milestone 5b closed on 2026-08-12** — its remit shrank rather than its
blockers clearing, since the §A sitting found that **no milestone in Phase 2 ever produced a Trip
destination**, so tasks 4, 6 and 8 left for a new milestone. Row 8 was what was left of 5b, Phase 4
behind a command, and it is done. **Row 9 is 5b-bis**, which builds the generator those tasks were
waiting for, and its **first task is not the generator** — it is the place-to-Buildings query all
three candidates needed and nobody had named.

*Original paragraph, kept because it is the record of a slice that looked like one row and was two:*
It shipped 2026-08-11
(row 6), which leaves **5b** — Trips, Legs and the pedestrian layer, unblocked by session F the same
afternoon — as the only code row. [`0021`](0021-trips-legs-and-the-pedestrian-layer.md) task 1 asked for
exactly this order: 5a-bis produces **frontage**, and an Access Point is downstream of frontage, so 5b
now inherits **real Access Points** — an `Address` on every Lot, derived and rebuilt on the Epoch —
instead of inventing them nearest-Segment-by-construction.

> ### ⚠ Picking this up cold on or after 2026-08-11 — read this box first
>
> **5b is DONE as of 2026-08-12** — tasks 1, 2, 3, 5 and 7, with 4, 6 and 8 moved to 5b-bis. *(This
> box read "IN FLIGHT, not waiting" while the slice was running; kept because the merge warning below
> it is still live for anyone touching `Space/Address.cs`.)* Tasks **1, 2 and 3 shipped** (`18544ee`…`8195a44`) from a worktree at
> `.claude/worktrees/5b-trips-and-legs`, running in parallel with the row 7 sweep below — so two
> sessions were writing that afternoon and **both landed on `main`**, at **1,100 green**.
> `src/Borough.Core/Movement/` now holds `TripTable`, `LegTable`, `TravellerTable`, `TripFate`,
> `TripPurpose`, and **`WalkRouting` — a real Dijkstra over a binary heap**. `plans/0021` →
> *What tasks 1–3 built* is the record and is authoritative over this box.
>
> ⚠ **Both sessions wrote `Space/Address.cs` that afternoon, with different encodings, and the merge is
> worth reading before touching either.** 5a-bis's slot-based type is the one that survived; a Leg's
> endpoints stay `HandleColumn` and resolve to an `Address` at the read, which is where a bulldozed
> Segment becomes `adr/0079`'s named absence. *A value type's representation is a property of its
> storage class, not of the concept* — `adr/0074` names the triple and never its encoding, which is why
> two careful readers took different answers on the same day, **and closing that gap in the ADR is still
> open**. The same merge corrected a sentence in `CONTEXT.md` → Access Point and in `0021` task 1: **a
> saved `Handle` does not dangle** (a freed slot bumps its generation — `Rows.cs:326`), so the reason an
> Access Point is derived is `adr/0064`-style **staleness**, not danglingness. The conclusion never
> moved; the general rule the old wording implied would have forbidden saving a Trip at all.
>
> **Task 1 closed 2026-08-11 and added no columns** — a Building's Access Point resolves through its
> Lot, which already derives one, so the task moved **no State Hash and no baseline**. Decision 3's row
> in [`0021`](0021-trips-legs-and-the-pedestrian-layer.md) still read *two saved columns* against its
> own task body's `adr/0078` correction, which is `0012` *Cause 1* inside one document. ~~**1,103
> green.**~~ **The suite is 1,122 green as of 2026-08-12**, after task 7 and the §B benchmarks.
>
> **`--trips` shipped 2026-08-11 and *detour* has a number for the first time.** `--roads` measures
> disconnection and says the larger half is detour; that half now reads **p90 140% → 306%** and
> **p99 663% → 913%** between `minimal.toml` and `severance.toml`, while the **median is 100% of the
> grid ideal in both**. ***Severance is a tail, not a median***, which makes the Commute Budget's
> being a **percentile** a measured requirement rather than a preference.
>
> ~~**What is left**: **task 4 the generator**, task 5 volume and Phase 4, task 6 the Census family and
> the Commute Budget, task 8 the long run.~~ **What is left is task 5 alone** — tasks 4, 6 and 8 went to
> **5b-bis** on 2026-08-12, and task 7 shipped. ~~**No `[trips]` table exists**, so the crossing cost is a
> required parameter nobody has chosen — and `--trips` did not choose it, passing zero and saying so.~~
> **The table was created the same day by 5b-bis task 3**: the crossing cost is `30 s` in all three
> shipped Rulesets, chosen against `0002` §D's band with its named ratifier half-run, and the Commute
> Budget is **still unset — stated as an omitted key that means *no ceiling***, because it is a
> percentile of a distribution 5b-bis task 8 has yet to produce (`adr/0052`).
>
> **⚠ The slice is being run out of order on purpose: §B before §A.** Task 4 is blocked (below) and its
> §B sibling is not, so the walk-search cost and the walk-cost *distribution* are being measured first —
> because **§B's answer can change what §A may choose.** Reasoning in
> [`0021`](0021-trips-legs-and-the-pedestrian-layer.md) → *Task 1, built*.
>
> **⚠ 5b IS CLOSED (2026-08-12), AND ITS REMIT SHRANK RATHER THAN ITS BLOCKERS CLEARING.** The §A
> sitting moved **tasks 4, 6 and 8 out of 5b** into a new milestone — each measures a *distribution*,
> and no generator exists or was ever scheduled to. **What was left of 5b was task 5**, which shipped
> behind a command the same day. `06`'s *the irreversible one* risk was retired by `adr/0075` and
> tasks 1–3, not by the generator. **The next milestone is 5b-bis.**
>
> **Two things blocked task 4, and both are now settled.** Both were rows in
> [`0002`](0002-open-questions.md) rather than prose here, because an open question is never written
> on the board:
>
> - ~~**§A — the generator's destination set does not exist.**~~ **CLOSED 2026-08-12 by a sitting, and
>   it closed by being VOID AS POSED.** All seven generators the corpus names sit in `06`'s
>   *Mechanisms with no milestone*, and Phase 2 as sequenced **never produces a place a person would
>   go** — so no reordering unblocks it and `adr/0070`'s answer is *build X*, which nothing was
>   scheduled to do. **`06` gains milestone 5b-bis** (jobs and the commute), **5b closes on tasks 1, 2,
>   3, 5 and 7**, and **Phase 4 ships now** behind a `CommandKind.Trip` on `Populate`'s precedent.
>   [`adr/0080`](../docs/adr/0080-phase-4-does-not-wait-on-a-trip-generator-and-a-trip-is-entered-by-command.md),
>   [`adr/0081`](../docs/adr/0081-the-commute-is-the-first-trip-generator-and-a-job-is-taken-by-satisficing-on-distance.md);
>   record in [`0021`](0021-trips-legs-and-the-pedestrian-layer.md) → *The §A sitting ran 2026-08-12*.
> - ~~**§B — the walk search has no `0013` row**~~ **MEASURED 2026-08-11, and it half-closes: a walk
>   search has no unit cost.** 86.2 ns at 128 m to 38.4 µs at 4.10 km — a **446× span** — because the
>   search settles a *disc*, at a flat 35–40 ns a settled node. **The lever is the walk-length
>   distribution, not the route count**, and no document states one; that half is **§A's**. The
>   **severed** case is **14.9 ns flat**, the cheapest query in the model. `0013` has the row.
>
> ~~**⚠ A third row joined §A on 2026-08-12**~~ **CLOSED 2026-08-12, in a sitting run beside 5b-bis, into
> [`adr/0082`](../docs/adr/0082-the-behavioural-clock-is-global-and-car-following-sub-steps-inside-it.md)
> — *the behavioural clock is global and car-following sub-steps inside it*.** **A Tick is 10.546875 s of
> in-world time, a Day is 24 in-world hours, `TICKS_PER_DAY = 8192` stands, and the Commute Budget is
> denominated at 5.6889 Ticks per clock minute** — which is all 5b-bis needed. `adr/0019` is **amended,
> not superseded**; **no State Hash moved and no baseline was re-recorded**, which is what made it safe to
> run in parallel. **It is two clocks, not the three this box counted**: `adr/0019`'s 0.2326 s and
> `02 §1.2`'s 0.144 s are within 1.6× and both car-following, and the calendar is 45–73× from both. The
> **global fine clock is refused on measurement** — 371,420 Ticks an in-world Day, so a Tick must cost
> 0.192 ms against S0a's **0.112 ms empty** and S0b's **8.72 ms with work**, **108× short** — and
> `adr/0019`'s whole derivation predates any Tick figure taken over a city. ⚠ **The sharpest finding is
> one the box did not contain**: `adr/0019` has *one* exchange-rate row where there are **two
> Ticks→seconds rates**, the host's (free) and the in-world one (derived), which is why it believed its
> headline ratio invariant — **it is not, and it moves 4.3×**: *5.9% of a life spent driving* is **1.4%**,
> so the corpus has assumed **~4× the standing traffic** the shipped numbers produce. Re-derivation filed
> to [`0013`](0013-tick-budget.md) and 5b-bis rather than applied. Two more: ***a degree of freedom is
> spent by the first document that uses it, and nothing announces the spending*** — `adr/0070` read
> backwards, reasoning from a **freedom** rather than an absence, and no edit made `adr/0019` wrong; and
> **the argument that survived was the one listed last**, since of its three reasons for declining 4096
> visual honesty falls with a 65×-adrift column and queue fidelity moves to the sub-step ratio. **One new
> hash-bearing number**, the sub-step ratio (`0002` §D2), band **21–45**, ratifier **S5** — and it is the
> largest lever on the Microscopic Cap, taking it from ~569,000 to **~12,600–27,100** against a demand
> side R2 puts near 186,600. *Original box below, kept because it is the record of a question framed
> correctly and counted wrongly.*
>
> **⚠ ORIGINAL: found while §B was being read back: *how long is a Tick*, and
> the shipped code answers it 73× away from two design documents.** `Speed.FromKilometresPerHour`
> converts every authored `*_speed_kph` using *a Day is 86,400 s over 8192 Ticks*; `adr/0019` says there
> are **no seconds in the library** and derives `TICKS_PER_DAY` **from** car-following at 4.2 m/Tick;
> `02 §1.2` states ~0.5 Tile/Tick. **Nothing shipped is wrong and this is not an outage**: `--trips`
> reports a median walk of 1.4 min at 128 m and 18.3 min at 2,048 m, which is 5 km/h to the digit, so
> **the code is right and the documents are not** — the reverse of `0012`'s usual direction. **It does
> not block 5b's remaining task** — a walk Leg needs the
> number, not the argument — **but the Commute Budget is denominated in whichever wins**, so it
> is owed before **5b-bis** and not before 5b task 5 *(it read "before task 6, not task 4" until
> 2026-08-12, when the §A sitting moved both of those tasks into 5b-bis — the dependency is unchanged
> and only its address moved)*. The likely shape is **the behavioural clock globally
> with the Lane kernel sub-stepping inside phase 4**, which costs the Microscopic Cap ~35× and is S5's to
> price. Row in [`0002`](0002-open-questions.md) §A; the document corrections in
> [`0012`](0012-corpus-audit.md), whose load-bearing one is that **`02 §1.2`'s own derived table prices
> the calendar at 168.75× and the vehicles at 2.6×** — the Cities: Skylines mismatch [`01`](../docs/01-player-experience.md)
> diagnoses at 112×, committed in our own table three documents away from the paragraph naming it.
>
> **An audit of 5b's unsettled decisions ran the same afternoon and is now mostly superseded by tasks
> 1–3** — the search, the Fate set, the Purpose enum and the Leg structure all exist. **The two rows
> above are what survived it**, and they survived because they are the parts tasks 1–3 deliberately
> stopped short of. *Recorded because a superseded audit that is not marked superseded is read as
> current, which is `0012` Cause 1 arriving through a stale finding rather than a stale status.*

**Row 7 ran before 5b rather than inside it, and that ordering is the point.** 5b's stated payoff is a
Severance demonstration; the Ruleset it would have demonstrated on severs nothing, and the instrument it
would have trusted said otherwise. **A slice that had started on schedule would have found this while
holding a keyboard**, with a half-built Trip model to explain it against. *An audit before a slice is
worth a finding during one* — and what makes it affordable is that the question was **measurable**, so
running it cost forty minutes rather than a sitting.

---

## Done

Slice status is [`0003`](0003-build-plan.md)'s ledger, which is the source; spike numbers are
[`spike-results`](../docs/spike-results.md)'s. This list says what each thing **was for** and links to
its record. Findings live in the plan that produced them.

### Slices

| | Slice | Record | The finding that reached past it |
|---|---|---|---|
| 0 | Scaffolding — four projects, build config, three reflection guards, CI | [`dev-environment`](../docs/dev-environment.md) | — |
| 1 | **S4** — the kernel benchmark | [`0004`](0004-s4-kernel-benchmark.md) | No tripwire fired. *Task 11 — delete `spikes/S4.Kernels/` — is still open* |
| 2 | The arithmetic substrate | [`0005`](0005-arithmetic-substrate.md) | `adr/0038`, and an amendment to `adr/0003`'s normative hash |
| 3 | The analysers | [`0006`](0006-analysers-and-lints.md) | Twelve diagnostics; `adr/0036`'s rule-7 exception axis |
| 4 | Typed tables and the field declaration | [`0007`](0007-typed-tables.md) | `BOR0901`, and the project's first State Hash |
| 5 | The Tick, the Input Log and replay | [`0008`](0008-tick-and-replay.md) | **`Borough.Formats`, the fifth project** (`adr/0039`). Its costing of the invariant tiers is worth three orders of magnitude |
| 6 | **Map Layers** | [`0009`](0009-map-layers.md) | **`adr/0044`** — the sixth claim in the corpus measured false, and the first outside S2 — which then got its own second half wrong by argument and **withdrew rather than amended**. *Citing an ADR is not applying it* |
| 7 | **The Rule engine — Bins and Bin Rules** | [`0011`](0011-rule-engine-bins-and-rules.md) | `adr/0049`, `adr/0050`. `02 §4.3`'s worked example **destroyed money for six slices**, because the transcription into the loader's own fixture dropped the money line — a green suite agreeing with the code instead of the document, and the first of **four** such |
| 8 | **Hot reload — the Ruleset as a thing that happens to a running world** | [`0015`](0015-hot-reload-and-the-ruleset-as-a-thing-that-changes.md) | **`adr/0015`'s acceptance test, run: 0.70 s** against the 60–120 s warm rebuild it was written on — and **it could not have been run through a recorded log at all**, because a log names a Ruleset by content hash and *editing the file is the loop*. A declaration's identity turned out to be its **name**, not its id, so *every removal the degradations exist for is also a reordering*; **derelict needed no flag**; and the provenance trail's cap is **world-creation-fixed on a self-referential argument** |
| 9 | **The Event Wheel** — the fine wheel only | [`0016`](0016-the-event-wheel.md), `adr/0056` | **776 tests, no baseline moved** — hash-neutral by construction, its own acceptance test. The end-of-run tier had been stamping every violation **Tick 0** in both 100,000-Tick acceptance runs |
| 10 | **Zone Rules — the second Rule family** | [`0014`](0014-zone-rules-and-the-sweep-family.md) | `adr/0053`–`0055`, and **`adr/0059` in task 11, which reopened the closed slice**. **Discharged `adr/0006`'s collection half for the first time.** The growth cycle **cannot be entered from a standing start**; the city settles **five-sixths homeless**; `02 §5.7`'s *constant cost regardless of Zone size* is false in the letter and true in the substance — and the *pacing* half of the same bullet was false outright, because a sample stated as an absolute count makes a mechanism's time constant the size of the city |
| **5a** | **The Road Graph — the first Phase 2 slice** | [`0020`](0020-the-road-graph.md), `adr/0071`, `adr/0072` | **The spike had the mode mask backwards from its own argument, and no measurement it ran could have caught it** — it never generates a one-way street. Also: ~~*Severance is a property of the grid's fineness relative to the barrier*, so the crossing dial does nothing until there is enough Arterial per unit of grid~~ **⚠ MEASURED FALSE 2026-08-11 and inverted — see row 7 of *Do these next***: severance is monotone **increasing** in block size, so the shipped 32-Tile lattice is the **dead** end and the **coarse** end is where the dial bites. The slice generalised from two observations differing in two variables; and a **wholly-derived table cannot join `World._tables`**, because `Rows.Fold` folds the allocator's scalars before consulting any column's disposition, so it would hash its own rebuild count |
| **5a-bis** | **The Lot subdivider and the road editor** | [`0022`](0022-the-lot-subdivider-and-build-road.md), `adr/0077`–`0079` | **The sharpest finding is about the re-record, not about Lots.** Zoning a Tile now zones the **block**, and eight of the golden session's eleven `zone` commands named one block — a straight re-record would have **retired the verb from the baseline while producing a full set of freshly correct hashes**. Second sighting in three slices, so it is `GoldenSessionCoverageTests` now rather than a paragraph. Also: **a guard that covers one of two identical files is worse than no guard**, and **a brief is evidence about what its author could see rather than a specification** — three of its five recommendations were refused, each differently |
| **5b** | **Trips, Legs and the pedestrian layer** — ⚠ **partial**, and **re-scoped rather than finished** | [`0021`](0021-trips-legs-and-the-pedestrian-layer.md), `adr/0074`–`0076`, `0080` | Tasks **1, 2, 3 and 7** shipped 2026-08-11; **task 5 is row 8 above** and tasks 4, 6 and 8 left for **5b-bis** on 2026-08-12. **`06`'s *irreversible one* risk was retired by `adr/0075` and tasks 1–3, not by the generator** — a single-Leg model is not expressible in the three-way split. Two findings: **a walk search has no unit cost** (86.2 ns to 38.4 µs, a 446× span — the lever is the walk-length *distribution*, which no document states), and ***Severance is a tail, not a median*** (p90 140%→306% detour, median 100% in both Rulesets), which makes the Commute Budget's being a **percentile** a measured requirement |

### Spikes

| | What it settled | Record |
|---|---|---|
| **S4** | The kernel benchmark; no tripwire row fired | [`spike-results`](../docs/spike-results.md) |
| **S0a** | **The sizing half of the Phase 1 gate** — 86 MiB for 1M rows. Found what it was not looking for: **run mode had never had a city in it**, so every Tick figure the corpus held had been taken over an empty world. It also **split S0 in half** | [`spike-results`](../docs/spike-results.md) → *S0a* |
| **S0b** | **The Tick with work in it: 8.72 ms at 1M, 55.9% at 4×.** Split the way S0 did — three clauses of four, with routing, pollution decay and `05 §5` role 3 unreachable for want of **content** rather than machinery. Its largest finding is not the price: **a Zone Rule's `sample` is absolute where the thing it paces is relative to the size of the city** | [`spike-results`](../docs/spike-results.md) → *S0b* |
| **S2** | **Retired the risk it existed for** — *pathfinding is slow at a load nobody measured, on a map already committed to*. The map survives, the path source is chosen, the cluster is **8**, and R8 closed the congestion loop. **R0–R8 are done and R7's tail closed 2026-08-11**; the one act left is **deleting the 33,000-line harness**, and R7 found on the same day that the two gates holding it — session **M** and R6.3's question — **have both cleared**, so it is available and is row **4** above. Run order, per-round verdicts and this spike's instructions to its own later rounds are all in its plan | [`0010`](0010-s2-routing.md), [`spike-results`](../docs/spike-results.md) → *S2* |
| **S5** | **The Microscopic tier's price, which the corpus had transplanted from somebody else's float engine — and, in the end, a defect in our own.** **~533,000–570,000 Vehicles a Tick a core**, ~7,400–7,900 Segments in 15.6 ms, break-even promotion residency **1 Tick**. `adr/0016`'s structure survives whole **and so does its 400,000**, once `IntegerMath.FloorDiv` stops computing a modulo nobody asked for — **every `Fixed.Div` in the project was two divisions**, and the fix is bit-identical and worth **1.50×**. The *`memcpy` constant* is still false, at 17–29× a bare walk. Also found the **arrival Tick is a demotion discard `03` invariant 3 does not enumerate**, and that the clever Overlap cursor beats the naive scan by only 3–7%. Owes its `performance` capture | [`0019`](0019-s5-lane-kernel.md), [`spike-results`](../docs/spike-results.md) → *S5* |
| **S1 / S3** | **Not run.** Track B, Godot only, no gate. They are the empirical inputs to session **L** — a rendering ceiling and a UI-cost figure. Size them from `spike-results` and the 1M target; `06`'s specifications were stale by an order of magnitude and are struck | — |

**One caveat travels with every S2 figure and belongs to all of them.** Everything R1–R5 published ran
on a **frozen cost basis** — a route was invalidated because a road was *bulldozed*, never because one
got *busy*. R8 closed that loop for itself; **quote nothing from R1–R5 as a statement about a congested
city.**

### Sessions

| | Produced | The thing it changed outside itself |
|---|---|---|
| **A** — `adr/0015`, hot reload | [`adr/0048`](../docs/adr/0048-the-ruleset-is-validated-where-it-is-parsed-and-only-integers-and-strings-cross-into-the-core.md) | **Cleared two slice gates rather than one.** Named **Tomlyn**, put the validator in `Borough.Formats`, and found `adr/0003`'s owed exception **was never owed** |
| **B** — `02 §4` residue | `adr/0045` | A fallback chain is a source ladder over one Bin. Found the corpus's **own worked example polled for ever**. Published a depth cap of 5 and **withdrew it** |
| **C** — `02 §7` + `adr/0006` | [`adr/0056`](../docs/adr/0056-the-event-wheel-is-two-levels-ticks-and-days.md) | The Wheel's period is **exactly one Day** while `adr/0011` schedules Life Stages in **Days**, so **every Life Stage transition the design specified was unrepresentable** on the wheel it was specified to run on |
| **M** — the route cache's invalidation contract | An amendment to [`adr/0012`](../docs/adr/0012-routing-intent-lives-in-the-agent.md) | *Never wrong about a removal, boundedly wrong about an addition*, bounded **at use**. **What it changed outside itself is not the contract**: a Habit belongs to the **Citizen**, not the Traveller, which sets the store to the population and means R5.5.4's rotation was never evidence about it |
| **eight** | `adr/0036`, `adr/0037`, `adr/0002` rebuilt | The core's language; one live world state; a boundary sized against an *inspector* rather than a renderer |
| **nine** — `06-roadmap.md` | [`adr/0042`](../docs/adr/0042-a-planning-document-cites-and-a-design-document-owns.md) | **A planning document cites, a design document owns.** `06` is since [`0012`](0012-corpus-audit.md)'s **control case** — the only large document that came back clean, because it stores no status |
| **D** — `03 §5`, the traffic model | [`adr/0060`](../docs/adr/0060-a-habit-route-is-a-small-set-of-variants-and-which-one-you-take-is-who-you-are.md), [`adr/0061`](../docs/adr/0061-a-diversion-rejoins-by-local-descent-and-a-rejoin-is-never-a-search.md), [`adr/0062`](../docs/adr/0062-the-microscopic-cap-counts-vehicles-and-nothing-is-ever-evicted.md), an amendment to `adr/0046`, and **S5** | **The wall came down in one sitting, and the two largest things it produced were not on the brief.** A Habit is now **`k` variants** — which makes `adr/0046`'s own Provider-List row true, and is the structural answer to *the network runs out of routes, not road*. And **the first ratification this corpus has withdrawn**: R8 forbade carrying its fire rate to a multi-candidate scheme, and D chose one three hours earlier. The 861.87% is answered by **removing the search**, not cheapening it. Its typing pass — run **before** anything was argued — emitted S5, found `adr/0007` stating invariant 6 inverted, and found `0013` holding **no row for the Microscopic tier at all** |
| **N** — `02 §4`–`§5` and `04`, the Bin, the Pool and the economy | [`adr/0063`](../docs/adr/0063-a-wait-list-wakes-on-the-bins-state-and-a-shortfall-is-derived-rather-than-stored.md), [`adr/0064`](../docs/adr/0064-a-bins-capacity-is-a-property-of-the-ruleset-in-force-and-an-over-full-bin-drains-rather-than-clamps.md), [`adr/0065`](../docs/adr/0065-a-bin-holds-a-long-and-unbounded-names-a-ceiling-whose-approach-is-a-defect-rather-than-a-refusal.md) | **Booked against four defects found by running code, and the first two it argued both turned out to be one shape**: a Ruleset number copied onto a row at creation and never re-derived, so the edit reaches everywhere except where it matters. `0063` deleted `shortfall` and **found its defect live in the committed golden baseline within minutes** of building `adr/0033`'s invariant; `0064` deleted the saved capacity on the same argument, four hours later, against an objection that nearly reversed it — *reload is design-time and rare* — which the **patch** answers and the reload does not. **`0065` then found the corpus holding two widths for one quantity**: `Money` is a `long`, a Bin's level was an `int`, and `adr/0031` puts money in a Bin — so every payment narrowed 64 bits to 32, and the semantic question task 4 was booked to answer turned out to be unanswerable at the old width. **All three have shipped**, `0064` and `0065` as one commit — whose own finding is that `0064` recorded a live defect that a two-slice-old loader refusal had already closed, because that refusal was the one guard in `RulesetLoader` with no test. **`0066` then came out from under task 5**, which was arguing `04 §6` and found that the **Provider List is the only per-entity collection in the design ever modelled as an inline array** — against a rule `05 §4` has always stated — so S0a's *21% of the entire world* and its *Households are the largest table* both price a structure the project forbids, and are suspended pending §B. **`0067` then settled `04 §6` itself** — a shopping attempt is a **Trip**, one provider per occasion — which puts the economy's central chain into **5b's** budget column rather than inventing one, and bounds a feedback loop that amplified the failure path by the Provider List's length. ~~Still running~~ **Still open, and the distinction is the point: every ADR it produced has shipped — `0063`–`0070` — so nothing in the *code* track waits on it.** What is open is task 5's residue in `04 §6` |
| **F** — `adr/0008`, the Leg model and the pedestrian layer | An amendment to [`adr/0008`](../docs/adr/0008-walking-is-a-simulated-leg.md), plus [`adr/0074`](../docs/adr/0074-side-of-street-is-a-property-of-the-access-point-not-of-the-graph.md), [`adr/0075`](../docs/adr/0075-a-leg-is-a-plan-and-a-traveller-is-a-cursor.md), [`adr/0076`](../docs/adr/0076-the-trip-fate-set-is-closed-at-four-and-a-fate-names-the-journey.md) | **The last session that gated a slice, and the amendment it was booked for turned out to be wrong in both directions at once.** `adr/0008` asks for *"sidewalk edges alongside street edges, **and** crossing edges at junctions"* — the brief proposed replacing the sentence, which would have deleted the **correct** half: crossings are real foot-only Segments, they are what **Severance** turns on, and **5a already built them**. *An `and` in a consequence is two consequences.* **Three more outlive it.** A **revisit trigger can be spent before it is written** — *one pedestrian edge per block face* was already what the graph is, so the ADR's only lever never existed, which no later diligence could have caught. **A placeholder whose value sits inside the range of legitimate answers cannot announce itself**, and this one *paid the player for the shortage*: a zero-length parking walk is what a garage genuinely produces, so a vehicle Access Point reachable as a fallback from an exhausted Parking Shed makes a full car park cost **less** than an empty one — the prohibition is now written, because F was the last document before the code. And **side of street was nearly surrendered by collapsing two questions**: whether it is *modelled* is independent of whether it is *in the graph*, and `adr/0009`'s deferred table had already asked for it. It also **refused** a hash-bearing number rather than opening one, and closed **neither** measurable claim it was forbidden to touch |
| **H** — `adr/0009`, parking is modelled supply | [`adr/0083`](../docs/adr/0083-a-sheds-use-is-the-arrival-query-and-a-stale-shed-is-wrong-by-a-bounded-walk.md), [`adr/0084`](../docs/adr/0084-parking-occupancy-is-two-checks-and-an-invariant-over-absent-state-cannot-be-written.md), amendments in place to `adr/0009` and `05 §3`, plus **mechanical check 7** | **The first session run purely as parallel work — nothing was blocked on it — and it closed a second session's remainder as a side effect.** `adr/0009` itself **survives untouched**: what moved is one revisit trigger and one consequence. **Two findings outlive it.** The question that had blocked the Epoch rung for two sessions — *"what nobody has typed is what a shed's **use** is — a parking search, or a Trip arrival"* — is a **false alternative**: `adr/0009` gives the shed exactly one caller, a release does not consult it, and *a parking search* is refused by the ADR's own title, so **both readings are the same event**. *Nobody could type it because there was nothing to choose between.* What falls out is the **rate**, which is what R5.6 could observe and not explain: a shed's use rate is the arrival rate where a route's is Trip starts **plus every diversion** — R6.3's 1,269.51 a Tick. And ***a vacuously-satisfied invariant needs its state to exist — zero is a value, undefined is not***, which is the **missing third case** beside slice 5 task 7's *withhold a vacuous assertion* and 5b's *ship one on purpose*: parking has no columns at all, so both sides of the sum would be undefined and the check would be a comment wearing a test's clothes. ⚠ **And the thing that should not have needed a session**: the shed radius is hash-bearing, has been named as such since `adr/0009`, and had **no ratifier from the day it was written** — `adr/0052` was simply not being applied to this ADR |
| **J** — `05 §7` format half, map size, Outside Connection layout | [`adr/0085`](../docs/adr/0085-nothing-on-this-map-is-far-away-so-a-settlement-is-made-by-a-gap.md), [`adr/0086`](../docs/adr/0086-a-save-has-no-schema-of-its-own-and-the-field-declaration-is-the-format.md), [`adr/0087`](../docs/adr/0087-a-save-is-copied-at-save-cadence-not-read-from-a-past-that-no-longer-exists.md), [`adr/0088`](../docs/adr/0088-the-price-of-a-far-hinterland-is-paid-in-your-own-traffic.md), plus amendments in place to `05 §7`, `adr/0020` and three `CONTEXT.md` entries → [`0024`](0024-session-j-the-save-the-map-and-the-outside.md) | **The second session run purely as parallel work, and it reversed nothing while replacing the reason for everything.** All three items kept their conclusion and lost the ground it stood on — the map keeps 4096² **on density alone**, async saving keeps *no hitch* on a copy amortised over the autosave interval instead of a Past `adr/0037` deleted, and edge choice keeps its stakes with **congestion** in place of *longer hauls*. ***A decision given several grounds is load-bearing on whichever ones survive, and nothing recomputes that when one falls.*** **Its sharpest finding is that the corpus answered its own question first**: S2 R1.5 swept the Commute Budget to compare two algorithms and its Settlement-count column reads **1 of 121 Districts at every rung from 40 Ticks up**, which refutes ledger #1's *far Settlements are genuinely separate* — ***a measurement answers every question its numbers bear on, not only the one it was run for.*** **Two more.** ***The obligation a deletion creates is a re-derivation, not a retraction*** — the mirror of 5b's `0075`/`0041` finding, and the half that would have deleted a correct decision; and `05 §3` **already held the replacement** four sections from the paragraph contradicting it, which is `plans/0012` *Cause 1* **inside one file**, a granularity the audit does not operate at. And **the section blocking a milestone was empty**: `05 §7`'s format half had been decided by slice 4's field declaration and was holding a stale copy of it |
| **Q** — the reach-failure memory, and a gate audit that ran first | [`adr/0097`](../docs/adr/0097-a-reach-failure-is-counted-on-the-citizen-and-a-stock-failure-is-not-remembered-at-all.md), amendments in place to [`adr/0017`](../docs/adr/0017-agents-satisfice-they-never-optimise.md), [`adr/0067`](../docs/adr/0067-a-shopping-attempt-is-a-trip-and-a-household-tries-one-provider-per-occasion.md) and [`0013`](0013-tick-budget.md), plus **mechanical check 8** | **Run 2026-08-13 beside `0003` queue item 6, and its two halves are unrelated except that the first paid for the second.** It opened as *how coarse a routing destination may be* and that question was **already closed by `adr/0047`**, which had listed the entries it closed **by number** — `plans/0010` applied it, `0002` and this board did not, and **five stale rows** were left: §A's row, three of five bullets in §C's routing cluster, and session **I**, which was serving as **milestone 5c's gate**. ⚠ **5c has been unblocked for two days**, and the *Blocked* blanket row is now **split per-milestone** as its own text had demanded since the fourth sighting. The sharpest half is that `adr/0047` **retyped** the survivor from *arguable* onto a **measurable and unset** object (the routing partition's size) that no ledger held — so the stale `A` row invited a session to close by argument exactly what `adr/0043` forbids. ***A closure that retypes what survives it writes two obligations, and only the first one looks like one.*** **Then the sitting proper**: `adr/0047`'s owed rule was **one sentence covering two failures** — an option fails because it is *full* or because it is *unreachable* — and [`adr/0067`](../docs/adr/0067-a-shopping-attempt-is-a-trip-and-a-household-tries-one-provider-per-occasion.md) had answered the **stock** half on 2026-08-10, **three days after the debt was filed**, with a cursor nobody connected to it. The **reachability** half is `adr/0097`: counted on the Citizen, reset on employment, read by nothing, consumer named as 9a's Departure and deliberately undesigned. **The provider case is void under `adr/0070`.** ⚠ **`adr/0047`'s loop is not reachable in the build** — the job pass keys its candidate draw on the **Tick**, so the defect is live as a **recurring search bill** rather than as a loop, and the loop needs 5c. **Two things the build settled that no document had**: `TryEmploy` already splits the cheap failure from the expensive one, so *a memory buys a skipped search* decided which one gets remembered; and **`02 §9` was already violated** — *every aggregate must name its constituents*, and `jobs beyond budget` had no entity reference behind it. ⚠ **Its most-repeated finding is about the sitting's own errors, and there were three.** It told the user `CitationTests` checks that links resolve (**it does not** — it regex-matches `adr/\d{4}` and never opens a target); it wrote *"nothing in the corpus owns this"* from a `grep` truncated by `| head` when **four documents owned it correctly**; and it proposed routing a cost `0013` had held since 5b-bis. **All three are `adr/0093` — a conclusion reported from an unopened or truncated read** — and the first produced **mechanical check 8**, link resolution, measured before proposing at **4,064 links and one dead in the live corpus** | **What it changed outside itself is that the argument track stopped being the constraint**: 5c is a whole milestone, unblocked, and nobody knew |
| **—** | [`adr/0043`](../docs/adr/0043-a-claim-a-measurement-could-settle-must-not-be-settled-by-argument.md), [`adr/0052`](../docs/adr/0052-a-hash-bearing-number-is-chosen-with-a-named-ratifier-or-not-at-all.md) | The two process rules that have paid for themselves: six claims measured false, and a number **deleted** rather than added (`adr/0059`) |

---

## Open tracks

### The argument track — what stands between here and Phase 2

**Phase 2 is not blocked by code** — **except that its first milestone is not blocked at all.**
**5a is exempt from this paragraph as of 2026-08-11**: no session below names it, its risk is retired
by `adr/0014` + `0040` + `0047`, and it is scoped in [`0020`](0020-the-road-graph.md). ~~Every *other* milestone (`06` 5b–10) waits on a design written from research and never argued, on one
spike, or on a number nobody has chosen.~~ ⚠ **As of 2026-08-12 that is 5c, 6 and 7a.** 5a-bis, 5b and
5b-bis shipped; **8 cleared with session H and 10 cleared with session J**, both on 2026-08-12, and
neither was blocked by code at any point. What waits is one design written from research and never
argued (`03 §5`'s residue), three ADRs, and S2. `0002`'s readiness review
states the shape plainly: *Phase 2's wall is one large item, not many small ones.*

**None of these contends with the code track.** This is a **menu, not a queue** — take from it when
something concrete is waiting. Ordered by what it unblocks.

| | Session | What is missing | Unblocks |
|---|---|---|---|
| ~~**D**~~ | ~~**`03 §5`** — the traffic model~~ **RUN 2026-08-10, all five tasks** → [`0017`](0017-session-d-the-traffic-model.md). **What it left behind is a dependency rather than a hole**: four questions about the Microscopic tier's shape now route to `03 §5.1`'s acceptance suite, which is milestone 6's work. **And it named its own successor** — `03 §2`, the Citizen model, as a separate sitting | ~~**The wall.**~~ The most detailed unargued design in the project, now carrying transit vehicles. More than one sitting. **It is also where the 861.87% diversion figure lands**, and where `0002` §F's rebuild found the corpus's remaining 🔴 is essentially **one cluster** rather than many gaps | milestones 5b, 5c, 6, 7a |
| **E** | `adr/0005` + `adr/0007` — fidelity | One session, not two: `0007` moved Fidelity from person to **place**, and `0005`'s tiers are what it moved | milestones 7a, 7b |
| ~~**F**~~ | ~~`adr/0008` — walking is a simulated Leg~~ | **RUN 2026-08-11, one sitting, all seven decisions** → [`0021`](0021-trips-legs-and-the-pedestrian-layer.md) → *What session F decided*. **Milestone 5b is unblocked.** An amendment and three ADRs; `CONTEXT.md` gained **Address** and **Node**. **It closed neither §B-16 nor §B-17**, which is the constraint it was booked under, and it **refused** a hash-bearing number rather than opening one — the per-mode Commute Budget weight — so §D got smaller by an argument, which is `adr/0059`'s direction. *Original row:* **PROMOTED 2026-08-11 — see *Do these next* row 5.** Written from research. It is what makes 5b *the irreversible milestone*. **One sitting** — D task 0 already ran its typing pass | ~~milestone 5b~~ **cleared** |
| **G** | `adr/0016` — the lane is the entity | Carries the order-of-magnitude claim the whole microscopic tier rests on | milestone 6 |
| ~~**H**~~ | ~~`adr/0009` — parking is modelled supply~~ | **RUN 2026-08-12, one sitting, beside 5b-bis** → [`adr/0083`](../docs/adr/0083-a-sheds-use-is-the-arrival-query-and-a-stale-shed-is-wrong-by-a-bounded-walk.md), [`adr/0084`](../docs/adr/0084-parking-occupancy-is-two-checks-and-an-invariant-over-absent-state-cannot-be-written.md), plus two amendments in place to `adr/0009` and the `05 §3` correction it was owed. **The decision survives untouched and only its consequences moved.** The occupancy leak is **specified** — two invariants, neither per-Tick — the shed's Epoch rung is settled at per-Segment-by-paths with **no staleness parameter**, and the radius has a **named ratifier** for the first time since `adr/0009` was written. **It also discharged session M's remainder**, below. *Original row:* Its `adr/0006`-class occupancy leak is named and needs the invariant specified with it | ~~milestone 8~~ **cleared** |
| ~~**I**~~ | ~~`adr/0012` — routing intent lives in the agent~~ | ✅ **DISCHARGED — and it was discharged before this row was read, struck 2026-08-13 by an audit run beside `0003` queue item 6.** The row reads *"owes an amendment, runs after S2 R6"*. **The amendment is in the file**: [`adr/0012`](../docs/adr/0012-routing-intent-lives-in-the-agent.md) lines 47–63 carry R5 and R6 — the induced error as an **absolute**, 0.86–0.94 Ticks flat across the entire origin-destination family; the coarse key's benefit unconfirmed and forbidden to cite; **eviction at fixed capacity, four-way LRU on the high bits**, conflict misses 20.0% → 3.8% — and then session **M**'s invalidation contract beneath them. `adr/0047` had recorded the state precisely (*"`adr/0012`'s owed amendment is **half discharged**… the **eviction policy** remains owed to R6"*), R6 ran, and the second half landed. **Nothing was left owing and no document said so.** ⚠ **The session that never needed running is the finding**: this row is what the *Blocked* table cites as 5c's gate, so a discharged amendment was holding a milestone for two days. *A gate is discharged by the work, and struck by somebody — and only the first of those happens on its own.* | ~~milestone 5c~~ ✅ **cleared** |
| ~~**J**~~ | ~~`05 §7` format half, map size, Outside Connection layout~~ | **RUN 2026-08-12, one sitting, beside 5b-bis** → [`0024`](0024-session-j-the-save-the-map-and-the-outside.md). Four ADRs, [`0085`](../docs/adr/0085-nothing-on-this-map-is-far-away-so-a-settlement-is-made-by-a-gap.md)–[`0088`](../docs/adr/0088-the-price-of-a-far-hinterland-is-paid-in-your-own-traffic.md), and **all three items produced a conclusion that stood on a ground that did not.** The map stays at 4096² **on density alone** — its corner-to-corner ground is **1.4–2.7% of a Day and not 141%**, and ⚠ **S2 R1.5 had already measured the consequence in a column nobody read**: one Settlement holding all 121 Districts at every Budget rung from 40 Ticks up, against a 30-minute Budget of 171. A Settlement is made by a **missing edge**, never a distance. `05 §7`'s format half was **already decided by the per-field declaration** and the section was holding a stale transcription of it, wrong in four of five lines; and *Outside Connection layout* had **no subsystem in it** — it is an ordinary Building, complete since 5a-bis. **Milestone 10 is unblocked and smaller than `06` described it**. ⚠ **THEN RE-OPENED ON ITS MAP-SIZE HALF THE SAME DAY, with the user in the room, into [`adr/0089`](../docs/adr/0089-the-map-is-sized-by-how-many-commutes-fit-across-it.md)** — *the map is sized by how many commutes fit across it*, at **512 Cells, 16384² Tiles, 65.5 km**. `adr/0085` is superseded on its decision. **The correction was the frame, not the arithmetic**: whether the city is one blob or several towns is the **player's** decision, and at 16.4 km the player has no lever but refusing to build a road — the units error had deleted a degree of freedom, and `adr/0085` spent a sitting hunting a replacement mechanism instead of noticing the map was too small. Density does not bind because `adr/0021` makes unbuilt ground free: the **developed** density is unchanged and a 1M city occupies **270 km², the whole of the old map**. ⚠ ~~**The constant is NOT flipped** — see hash-moving queue **item 6** in [`0003`](0003-build-plan.md) — because `RoadGenerator` paves the whole map, which is `adr/0021`'s own claim being false in one place, and its repair is `0002` **ledger #2**~~ **The constant is still not flipped and is no longer gated: item 6 shipped 2026-08-13**, so it is a one-line commit waiting on nothing. And the gate's stated ground was **never true** — the generator lays nothing at world creation, only when `SyntheticCity` runs, which `CommandKind.Populate` is the only door to (`adr/0090`) | ~~milestone 10~~ **cleared** |
| **P** — the player model | `01 §1` (the core loop), `§3` (the first ten minutes) and `§4` (two hours, and twenty) | ✅ **DONE 2026-08-13** → [`0025`](0025-the-player-model.md). **Twenty-four decisions, seven ADRs ([`0090`](../docs/adr/0090-the-generator-makes-land-and-the-player-makes-every-road.md)–[`0096`](../docs/adr/0096-the-microscopic-cap-derives-from-the-design-speeds-budget-and-not-from-the-top-rungs.md)), and `01 §2`, `§4`, `§5` and `§6` amended rather than merely cited. Every section of `01-player-experience.md` has now been examined.** ⚠ **`§4` was never on the plan and closed last.** Its two markers were unsupportable for opposite reasons: the two-hour one rests on a Commute Budget that was a **step function**, and the twenty-hour one rests on Replacement Rate inside **140 Days** — no generation at any Life Stage length. So **`TICKS_PER_DAY` goes 8192 → 2048** ([`adr/0094`](../docs/adr/0094-a-day-is-2048-ticks-because-ticks-per-day-is-a-sampling-rate-and-not-a-length-of-life.md), 562 Days in a campaign) and the **Commute Budget becomes three rungs** ([`adr/0095`](../docs/adr/0095-a-commute-budget-is-three-rungs-and-only-the-last-one-refuses.md), fast 20 / moderate 40 / unsavoury 50, only the ceiling refusing). ⚠ **The clock change is `adr/0019`'s explicitly forbidden move and `adr/0082` is what made it legal**: that ADR derived a Tick's duration **from** the constant, so a commute is 1.39% of a Day at both values and *"twice the vehicles"* cancels — and `adr/0082` never re-derived what stood on the premise it deleted, leaving a **revisit trigger pointing backwards**. Third sighting in two days of ***the obligation a deletion creates is a re-derivation***. It costs **×4 on routing** (`0013` → ~305% at 4×, ~76% at 1×). ⚠ **A second claimed cost — the Microscopic Cap ÷4 — was published and withdrawn the same day** by [`adr/0096`](../docs/adr/0096-the-microscopic-cap-derives-from-the-design-speeds-budget-and-not-from-the-top-rungs.md), which is the session's sharpest finding and is about a **number**, not a mechanism: the gap was quoted against 186,624, S2 R2's synthetic **fleet**, which `plans/0013` labels *not a stressed count* **in the cell** and warns about **in the paragraph below the table** — and it was borrowed as a denominator a day after that paragraph was read. The gap is real and is **7.3×** at the design speed, not 27–58×: 186,624 is 2,592 stressed Segments × 72 Vehicles and is an **upper bound**, and that clause is what was left behind. ***A caveat attached to a number does not travel with it*** — now [`plans/0012`](0012-corpus-audit.md) **Cause 5**, with **check 6 built and green** (`DisqualifierTests`), which **caught the correction being wrong on its first run**: `plans/0013`'s compressed *not a stressed count* was read as *a whole fleet*, so the caveat travelled without *its* context too — Cause 4's sibling with the polarity reversed (there the source sentence is wrong; here it is right and was left behind), and the first cause in that ledger that its **existing** machinery could check, since both ends of a travelling number are documents. The supply side was also priced at 15.6 ms while the clock moved, which double-counts; at the **design speed** it is **~25,400 Vehicles on one core, identical to what the old clock gave at its own top rung**. `0096` fixes the **basis** — the Cap derives from 1×, because pricing at 4× chooses `03 §3.9`'s *simulation* degradation for every player permanently to avoid its *hardware* one on one machine — and records two owed things rather than designing anything: a **fallback tier below Microscopic** (foreseen, undesigned under `adr/0070`) and a **2- and 4-thread** Lane kernel measurement (S5, the largest unclaimed multiple on the supply side). `plans/0003` gains **item 7**, ungated and not urgent. **The governability problem is untouched and stays untouched** — a **playtest** question that must not be grilled. *The row below is the session's own record and narrates in the order things were decided; it was pinned mid-way and resumed.* ~~⚠ **PINNED MID-SESSION 2026-08-12**~~. **Ten decisions taken and written, one question open.** `plans/0002`'s coverage map marked both sections *never touched*, and everything downstream was built on them unread. Decided: the map **starts empty** and the player builds every metre; a founding balance granted automatically, *enough to start and not enough to win*; **1× is the design speed** and 4× is a speedup, which recomputes `0013`'s ledger from an existential ≥114% to **~28%**; **no feedback-latency rule, because response latency *is* the demand signal** — what a cadence bounds is **perception, not response**; a vacant Lot's reason **recomputed on the click and never recorded**; the **hour markers are expectations, not a script**; and the ground carries resources, so building over Woodland forfeits the Timber and Woodland is cleared by **zoning Extraction**, not by a tool. **Its finding is one finding, seen five times: the answer was already written down somewhere nobody had read.** `01 §2` already held *when a zoned Lot stays empty, that is information*; `CONTEXT.md` already held four of the six vacancy reasons and `§1` was one edit from becoming a second, disagreeing copy; `PlacementEngine.cs:72` had reached perception-versus-response from the Census side; and `adr/0022`'s macro-arc turned out **inert at 16.4 km**, missed by `adr/0089`'s sweep because ***a claim about distance is not always a claim that says so***. **Clearing closed 2026-08-12 into [`adr/0091`](../docs/adr/0091-clearing-land-is-bought-rather-than-taken-and-demolish-is-the-sixth-verb.md): `01 §2` is now **six verbs**.** `Demolish` takes over the bulldoze `Connect` has carried unnamed since `adr/0077`, and clearing is **bought** — market value off the land value Layer, paid to the displaced, so it is a transfer under `adr/0024` and adds no authored number. *No act yields empty ground except the one that pays full price.* **The five-times finding became seven**: `CONTEXT.md` → Derelict had asserted the verb since `adr/0057` (*"it stands until the player clears it"*), and `01 §6` had already made the District-scale lever a `Govern` **Policy**. ⚠ **Its largest finding is `02 §5.9` contradicting itself twelve lines apart** — abandonment leaves the shell **standing**, and the build took the half that leaves contagion with no carrier. **`Observe` and `§3` step 1 closed the same day into [`adr/0092`](../docs/adr/0092-the-region-view-is-the-map-from-far-away-and-a-trajectory-names-the-place-it-is-reported-at.md).** The **region view is a zoom level of one map**, not a second screen — `01` had never mentioned it and five other places had, including `00-vision.md`, so it was a view with **no owner**. And **`01 §6`'s *expandable by District* was a decision it could not have made**: at 16.4 km the map is one Settlement, so the alternative had no instances; it becomes **Settlement → District**, each trajectory reported first at the level its mechanism makes. `§3` step 1 becomes **read the edges**, so the first verb used is `Inspect` — the step `§5.6` had already called *the opening's real reconnaissance*. ***A decision taken when the alternative had no instances is re-decided rather than defended***, and `adr/0089`'s consequence list missed this one too. **✅ Both sections done, and the parked road write-up is written too.** [`adr/0090`](../docs/adr/0090-the-generator-makes-land-and-the-player-makes-every-road.md) — the generator makes land, the player makes every road, the map is **open** and `adr/0011`'s damper argument is **given up rather than answered**; the `adr/0077` amendment makes the edit unit a **run** of Segments (**63, 8.06 km, Input Log still version 1**); `plans/0002` **ledger #2 closes as refused**. ⚠ **Its finding is that `adr/0089`'s stated blocker is false**: `RoadGenerator` has one production call site, `SyntheticCity`, reached only by `Populate` — a player's world has had **no roads since 5a** — so the flip to 512 waits on a mechanical fix in a spike verb, not on a design question. `CellGrid.cs`'s *"at world creation"* is where the claim came from, the **third** decision in three days taken from a sentence about the code. And the session's own finding got named: [`adr/0093`](../docs/adr/0093-a-description-of-the-build-is-where-to-look-and-never-what-you-found.md), ***a description of the build is where to look, and never what you found*** — the **fourth inference sibling** after `0043` (claims), `0052` (numbers) and `0070` (absences), governing **what the build does**. **Four sightings in three days and all four wrong about the *trigger***; the repair is on the **writing** side — *name a symbol, never a time*. `plans/0012` **Cause 4**, and the only cause this corpus cannot check, since all three mechanical checks are document-to-document. **✅ SESSION P DONE** | — |
| **K2** | `06`'s Phase 2 **ordering** only | K1 is done. What remains is re-deriving the sequence and placing the **seventeen mechanisms `06` lists as having no milestone**. **Last** — ~~D–J~~ **E, G and I** move what it sequences; D, F, H and J have run | Planning Phase 2 at all |
| **L** | **A presentation design** | **It does not exist.** Every other phase is backed by a design document; rendering has none. **Write it first, then grill it** | Phase 3. Blocked on **S1** and **S3** |
| ~~**M**~~ | ~~*Run.* The remainder is the **shed half**~~ **FULLY CLOSED 2026-08-12 by session H**, into [`adr/0083`](../docs/adr/0083-a-sheds-use-is-the-arrival-query-and-a-stale-shed-is-wrong-by-a-bounded-walk.md). **The answer is no — it is one *shape* with two bounds — and this row had the right answer with the wrong reason.** The neighbourhood/path distinction is real and is why the witness had to be measured separately, but it is **not what decides it**: the shed's addition error is bounded by the radius and already priced by the Commute Budget, where a route's is unbounded, so the shed takes `adr/0012`'s shape and **none of its parameters** — no `T`, no rotation, no proximity wake. The blocker was `0002`'s *nobody has typed what a shed's use is*, which turned out to be a **false alternative**: `adr/0009` gives the shed exactly one caller | — | ~~R6's remainder~~ **cleared** |

> **D is promoted, and it is the first session booked against a number rather than against a document
> being ungrilled.** *(Decided 2026-08-10; the row is in* Do these next *above.)* The standing rule is
> **run a session when something concrete is blocked on it** — and this is an application of that rule,
> not an exception to it. What is blocked is [`0013`](0013-tick-budget.md)'s **dominant row**: routing
> carries 60–67 of the ledger's 114 points at 4×, R6.3 found its multiplicand counts **the wrong
> event**, and the only thing that can replace it is Trip generation — milestone **5b**, which D gates.
> **So the question *does the simulation fit* cannot be answered without D**, and the correction with a
> known direction points up.
>
> **What it must not become.** The board has been here before — *the design was generating design* —
> and the guard is that D is booked to produce **the traffic model as a design**, not to produce more
> sessions. Everything else in this table stays a menu. **Two things D should be handed on day one**:
> session M's diversion policy, which is settled *in principle* (**rejoin the Habit Route**) and has
> never been made a design; and R8's *the network runs out of routes, not road*, which is `plans/0010`
> **decision 11 on a different axis** and is the largest thing S2 produced that a spike cannot close.

### Not arguable, and worth being explicit about why

The **Microscopic Cap**'s value needs a built traffic model. **S2** is measurement — argument cannot
close it. And `0002` names a set of **playtest questions wearing design-question clothing** that the
argument track must not drift into: health, recreation, Service variants, car ownership, and
`01-player §1/§3/§4`. The governability problem — *268 km² of individually-placed service Buildings* —
**is not answerable by argument.** Somebody has to try placing them.

⚠ **Session P grilled all three sections and left that set intact**, which is the thing to carry: what a
grilling closes is the **design** question, and the playtest question underneath it survives unchanged.
`§4` closed on 2026-08-13 and produced **two more entries for this list** — `TICKS_PER_DAY = 2048` and a
Household's life in Days, both hash-bearing, both with **playtest** as the only named ratifier and
refuting numbers written in both directions. *An examined section is not thereby a settled one.*

### Two audits the corpus has assigned itself and not run

- **Type every claim `arguable` or `measurable` (`adr/0043`), across every ADR and design section —
  not only the ungrilled ones.** Two of the claims measured false sat in 🟢 rows of the coverage map.
  **That map is [`0002`](0002-open-questions.md) §F** — promoted out of the archive and rebuilt on
  2026-08-10, all **58** ADRs and every design document, with the blanket rows split because *a status
  whose granularity is coarser than the claims it covers cannot be checked*. **§F marks the three
  suspects ⚠** — **`adr/0016`**, **`adr/0009`**, **`adr/0008`** — each reads as decided, each carries a
  quantitative claim, and none has a number. **Type them before grilling them**: if the refuting number
  and the machine can be named, a session must not close them.
  *Rebuilding the map found the larger defect: it had **stopped at `adr/0043`**, so `0038`–`0059` —
  every decision the building has produced — did not appear in it at all.*
- **Re-check every 🔴-blocked debt for the shape `adr/0003`'s had** — a gate whose stated reason covers
  only *part* of what it blocks, leaving a runnable remainder parked behind a session it does not need.
  **Two data points, not one**: `adr/0003`'s owed validation, and `06`'s K1. The diagnostic is written
  down; the sweep has never been run, and it may move work out of the argument track and into code.

---

## Owed — documentation debt, none of it blocking

**Open items only.** Discharged ones are in the archive below, with the reasoning that made each
worth recording. **The corpus-wide sweep's debts are [`0012`](0012-corpus-audit.md), not here** — a debt
in two ledgers is the defect `0012` exists to diagnose. **S2's instructions to its own later rounds
moved to [`0010`](0010-s2-routing.md)** → *Findings that change a later round*.

| | What is wrong | Owner |
|---|---|---|
| **`05 §9`** | Owed the State Hash's cost. It records the full-world double buffer deleted at *"8–15 ms at 1M"*; **one State Hash is 32.47 ms, 2–4× worse**, and the performance strategy does not mention it. What is missing is the statement that **a per-Tick hash is not available at the target** | slice work |
| **`05`** | Strike the ~400k Trips/Day figure — known wrong, still standing in the authoritative document | — |
| **`05 §3`** | Parking Shed invalidation needs the *when you pay / what survives* correction `CONTEXT.md` → Epoch has taken | — |
| **`03 §3.8`** | ~~Third clause of the joint rewrite, **deliberately open**: *force-promotion must stand on its own second argument or go* is a **decision**, not a transcription~~ **RETYPED by session D task 3: it is *measurable*, not a decision.** Run `03 §5.1`'s **spillback** scenario with the mechanism disabled and see whether the upstream Segment blocks. **No session may close it** | ~~a session~~ milestone 6's acceptance suite |
| **Two section numbers, corpus-wide** | **`02 §6` is *Goods movement*; the sentence is `02 §5.2` step 6.** **`03 §3.6` is the junction blind spot; the attribution sentence is `03 §3.8`.** Wrong in four documents each. Same shape as the *"Zone"* row: a quotation copied forward instead of checked | a sweep |
| **"Zone" for the matrix's granularity** | It is the **District**. `05 §422` and `references.md §2` both say *"zone-to-zone travel-time matrix"*, and a corrected quote is a broken one — so this is a sweep, not a one-line fix | a sweep |
| **`adr/0012` and two other filenames use "Agent"** | Banned outright by `CONTEXT.md`. 33 occurrences across 22 files | a sweep |
| ~~**`spike-results`**~~ | ~~The 37k–111k in-flight band conflates duration sensitivity with peaking; re-derive on both axes~~ **DONE 2026-08-11.** The two axes enter as a **product**, so **111,000 appears three times** on the corrected surface and the real top is **334,000 — 3.0×** the published ceiling. It also unseated the reconciliation built on the band: `adr/0019`'s ≈120,000 is the provisional duration at a 2.16× peak, not a cross-town artefact | ~~R7~~ |
| ~~**`Heuristic.cs`'s hoist has no marker**~~ | ~~`adr/0073` requires a local workaround to name what it works around and where the finding went~~ **DONE 2026-08-11**, the day the ADR shipped. The remark now carries the corrected diagnosis (the cost was **not** routing on time), the fact that **the hoist is a workaround for a defect that no longer exists**, and the instruction not to remove it on the note alone — *re-run the ladder*. **The ~2 parts in 10,000 of tightness it gives up is now buyable back**, and whether one division beats the multiply is a measurement, which is the only honest way to leave it | ~~S2~~ |
| **`plans/0002`'s S0 specification** | Names four clauses as one spike. Split into S0a and S0b everywhere else; the ledger entry still reads as one item | — |
| **S0a's capture is `powersave`** | Every absolute is an **upper bound**; ratios unaffected. ~~Rides with R7's re-capture~~ — **it does not ride with it**: `routing-run.sh` captures S2's harness only, and S0a is `Borough.Headless`. **S2 R7 has since found the governor is the smaller axis** at memory-bound rungs, so this re-capture must record **load** as well, or it will reproduce the defect it exists to fix | S0a |
| **The synthetic fixture and `World`'s sizing derivation disagree**, and nothing checks that they do | `World` allocates 225 Lots and 150 Buildings per 1,000 Citizens; `SyntheticCity` builds **120** of each; **Households land at exactly capacity**, so the first one the simulation creates reallocates the table | slice 7's, because the right ratio is a design question and a fixture is not where it gets settled |
| **The end-of-run tier allocates on the Large Object Heap at scale** | ~544 KB at 100k Citizens, ~5.4 MB extrapolated at 1M. Once per run, after the trace is written, so it perturbs nothing today | Fix with a scratch buffer **when a real 1M city shows it**, not on an extrapolation |

---

## Blocked

**There is no red gate left.** [`0003`](0003-build-plan.md)'s gate board is the source. What remains
below is Phase 2 and Phase 3 — everything `0003` does not own.

| | Blocked on | Which is |
|---|---|---|
| ~~**Phase 2 milestones 5a–10**~~ → **5a is NOT blocked, 2026-08-11.** Split, because the row was hiding the one milestone nothing gates. **No session in the table above names 5a**: D→5b/5c/6/7a, F→5b, G→6, H→8, I→5c, J→10. 5a's own risk — *geometry leaks in and the graph stops being uniform* — is retired by `adr/0014` + `0040` + `0047`, and **K2 re-derives Phase 2's *ordering*, which is the one thing 5a's position cannot move in** since 5b–10 all run on the graph. Scoped in [`0020`](0020-the-road-graph.md). ⚠ **SPLIT AGAIN 2026-08-12, and the same way**: **5a-bis, 5b and 5b-bis are not blocked either.** D and F have both run, so nothing in the argument track names 5b; 5a-bis shipped, 5b shipped four of its five tasks, and 5b-bis was created by the §A sitting with no gate on it. ~~**The row that remains blocks 5c–10**~~ ⚠ **NARROWED A FOURTH TIME 2026-08-12 by session J, and by the same mistake again: it blocks 5c, 6 and 7a.** **Milestone 10 is cleared** — J was its only gate and J has run. This row has now been split four times, every time because it was a blanket over milestones with different gates, and *a blanket row is a status whose granularity is coarser than the claims it covers*, which is `0002` §F's own rule applied to a gate. **Four sightings of one defect is not a recurring mistake, it is a structure that should not exist**: the row should be per-milestone or it should not be a row | ⚠ **FIFTH SIGHTING 2026-08-13, and this time the row's own prescription is applied rather than restated** — it is **split into the three rows below** and this one is retired as a blanket. *Five narrowings, each correct and each leaving the structure that produced the next one, is the corpus's clearest case of a **finding written down instead of acted on**: the sentence to the left has said what to do since 2026-08-12.* | — |
| **Milestone 5c** — statistical resolution and the travel-time matrix | ✅ **NOT BLOCKED as of 2026-08-13**, and it has not been for some time. Its three named gates are all discharged and **not one of them was discharged by this audit** — each closed elsewhere and none of the closures reached a gate board. **Session I** owed an amendment to `adr/0012` that is in the file (see the struck row above). **`03 §5`** was session **D**, which ran 2026-08-10, all five tasks. **S2** is R0–R8 complete; `plans/0020` records *"routing, travel-time matrix, route cache — 5c, and S2 has measured all of it"*, and the only act left in that spike is deleting its harness, which gates nothing. A fourth gate `adr/0047` names — *"`06` still has player-drawn versus automatically derived Districts open, blocking milestone 5c"* — was discharged **twice over**: `02 §2.1` settled the question as ***both***, and the ADR severed routing's dependency on it regardless | ⚠ **What it inherits is a first task, not a gate.** The **routing partition's size** is *measurable and unset* (`adr/0047`), now [`0002`](0002-open-questions.md) §D2; `adr/0052` requires a named ratifier and never a ratification first, and the partition is `(derived AND rebuilt)`, so it can be chosen when it is built. **5c also carries 5b's unpaid `adr/0041` volume debt**, which 7a's Stress reads |
| **Milestone 6** — lane-as-entity traffic | 🔴 `adr/0016` — *the lane is the entity* — written from research and never grilled. ⚠ **Partly discharged by measurement rather than by argument**: S5 has since measured the order-of-magnitude claim the milestone's whole risk rests on, and `0002` §F marks this ADR one of three *type-before-you-grill* suspects — so a session **may not close** whatever part of it turns out measurable | session **G** |
| **Milestone 7a** — Stress-driven Fidelity with hysteresis | 🔴 `adr/0005` + `adr/0007` — fidelity. One session and not two: `0007` moved Fidelity from person to **place**, and `0005`'s tiers are what it moved. ⚠ It also needs **Segment volume**, which is 5c's unpaid `adr/0041` debt — *a 5c that ships a matrix and no per-Segment next hop leaves 7a with nothing to threshold* | session **E**, then 5c |
| **Planning Phase 2 at all** | S0 must have run — it has — and `06`'s ordering must be re-derived | session **K2** |
| **Phase 3** | 🔴 **a presentation design that does not exist** | session **L**, itself blocked on **S1** and **S3** |

**Closing S2 was on this list until 2026-08-11 and is not any more.** Its gate — R6's invalidation
half, session **M**'s — closed when M closed, and the question R6.3 put in front of it closed into
`adr/0061`. Neither clearance reached S2's own plan. It is row **4** of *Do these next*.

**Phase 3 is undesigned, not unplanned**, and the distinction matters: it describes an absence rather
than a choice. Rendering has never been designed, never been argued, and has no document to argue —
every other phase is backed by `02`, `03`, `04` or `05`. Worse, the interface it would build on was
**re-argued to serve something else**: `adr/0002` was rebuilt around hot and cold query flavours on the
finding that it had *"assumed a renderer because rendering is what an engine boundary is usually
for"*, when the actual consumer is an inspector. **The chain is S1 + S3 → L → Phase 3 is plannable.**

---
