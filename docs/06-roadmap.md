# 06 — Roadmap

> Vocabulary in [`CONTEXT.md`](../CONTEXT.md). Pillars in [`00-vision.md`](00-vision.md). The world model in [`02-simulation-model.md`](02-simulation-model.md), movement in [`03-agent-architecture.md`](03-agent-architecture.md).
>
> **What this document is authoritative over: the phase model, the four rules below, and the risk each milestone retires.** Nothing else. It sequences work; it does not describe the simulation. Every claim here about what the city *does* is a citation, never a restatement — the rule is [`adr/0042`](adr/0042-a-planning-document-cites-and-a-design-document-owns.md), and the reason is that this document had accumulated eleven false claims by copying eleven originals that later moved.
>
> **Phase 0 and Phase 1 order and contents are owned by [`plans/0003-build-plan.md`](../plans/0003-build-plan.md).** Live status is owned by [`plans/0000-board.md`](../plans/0000-board.md). Phase 2's ordering is **provisional** and awaits re-derivation. Phase 3 has no plan and cannot have one yet; see below.

---

## How this roadmap works

Four rules govern everything below. The first three are pacing decisions; the fourth is a review criterion. These four are this document's own, and [`0003`](../plans/0003-build-plan.md) inherits them unchanged.

**1. No dates. Pure dependency ordering.** The developer works somewhere between sustained evenings-and-weekends and casual exploratory, and that rate is not predictable a month out. A dated plan would be wrong immediately and would then be quietly ignored, which is worse than having no plan. What *is* stable is what depends on what, so that is all this document commits to.

**2. Every slice leaves the project in a working, runnable state.** There will be gaps of weeks. The project must be re-enterable cold — check out, build, run, see something happen. A milestone that ends with the build broken, or with a half-migrated data format, has failed regardless of how much of it is written. In practice this means each slice ends with a test that passes and a thing you can watch. What *runnable* means concretely is the definition-of-done list in [`CLAUDE.md`](../CLAUDE.md), refined per slice by [`0003 §Definition of done`](../plans/0003-build-plan.md).

**3. Slices are sized for one or two sittings wherever possible.** A milestone that cannot be finished in a session tends not to get finished — it gets 60% done, then sits for three weeks, and re-entry costs more than the remaining work. Where a milestone genuinely cannot be compressed to that size, [`0003`](../plans/0003-build-plan.md) splits it into numbered slices, each independently completable and each leaving the build green.

**4. Every milestone names the specific risk it retires.** This is a visible field, not prose, and it is a filter: a milestone that cannot name a risk is either not necessary yet or is not understood well enough to start. "Risk retired" means *after this, we know something we did not know, or a class of failure is now structurally impossible.* Its corollary is the stopping rule: **once the risk is retired, the milestone is done, whatever else remains undone in it.**

### What a milestone row is

**A name and a risk.** Nothing else. There is no contents column, because a contents column is a description of the simulation and this document does not own one — the mechanisms live in [`CONTEXT.md`](../CONTEXT.md), `00`–`05`, and the ADRs, and a copy of them here is a copy that goes stale without anybody noticing. That is not a hypothetical: `rate` survived here for months after [`adr/0033`](adr/0033-two-rule-families-scheduled-and-swept.md) replaced polling with subscription, and *"travel time `distance / speed`"* survived alongside a tolerance test against a volume-delay function no milestone built.

---

## Phase 0 — the spikes

Throwaway code answering questions with numeric answers. Results are recorded in [`spike-results.md`](spike-results.md) and the harness is then deleted.

**The framing this section used to carry — *"three spikes, before committing to Godot"* — is spent.** [`adr/0001`](adr/0001-godot-and-csharp.md) was grilled and confirmed, and [`adr/0036`](adr/0036-the-cores-language-is-a-separate-decision-and-it-is-csharp.md) took the core's language out of it, so there is no longer a commitment for S1 and S3 to gate. There are five spikes, not three, and two of them never had anything to do with Godot. Order run and order remaining are both [`0003`](../plans/0003-build-plan.md)'s.

| # | Spike | Risk retired |
|---|---|---|
| **S4** | Kernel benchmark | Three claims the corpus rests on with no arithmetic behind them: `adr/0036`'s GC-pause assertion, `adr/0003`'s *"`checked` is cheap"*, and ledger #29's block-copy cost. Settled **before the first line of core exists** |
| **S2** | Routing | The single most consequential open technical question in the design — and the one that collapsed Cities: Skylines 2. Planned in [`0010`](../plans/0010-s2-routing.md) |
| **S0** | Synthetic 1M-Citizen city | That the 1M target is a hope rather than a spec, and every system sized against it is built on an unvalidated assumption |
| **S1** | Chunked `MultiMeshInstance3D` at city scale | That the per-Chunk MultiMesh split does not deliver acceptable draw counts and culling at city scale |
| **S3** | One data panel with a live multi-series graph | That Godot's `Control` UI cannot carry the Evidence drill-down, which is the load-bearing reason the engine was chosen at all |

**S1 and S3 now feed Phase 3's design rather than gating an engine decision** — see *Phase 3* below. Their specifications in this document were stale by roughly an order of magnitude and have been struck rather than restated; `0003` and [`spike-results.md`](spike-results.md) carry the live figures.

---

## Phase 1 — the foundation

No graphics. Everything is `Borough.Core`, `Borough.Tests`, `Borough.Headless`, `Borough.Formats` and `Borough.Analysers`, and everything is verifiable from a terminal. The boundary in [`adr/0002`](adr/0002-simulation-is-an-engine-agnostic-library.md) is enforced from the first commit by the CI reference check, not retrofitted once it has already been violated.

| # | Milestone | Risk retired |
|---|---|---|
| **1** | Tick and determinism harness | Determinism, which is close to impossible to retrofit. Every later debugging tool is downstream of this one |
| **2** | Typed tables and handles | That an ECS or a naïve object graph gets baked in, that save/load and deterministic iteration order become hard later, and that a full-world double buffer gets baked in and quietly cancels the Event Wheel |
| **3a** | Rule engine — Bins and Rules | That the economy is not conserved and failures are silently partial |
| **3b** | Rule engine — hot reload path | **The Citybound failure.** A simulation that is slow to tune is a simulation nobody tunes, and it then becomes unbalanceable |
| **3c** | Map Layers and Zone Rules | That growth cost scales with Zone size rather than staying constant |
| **4** | Event Wheel | That cost scales with *number of Citizens* rather than *number of Citizens with something happening now* |

~~**One assertion in this section is inherited and has never been argued.** *3b must not slip behind 3c.*~~ **RETIRED by the `adr/0015` session.** It was one unargued claim counted twice — this document gave no reasoning, and [`adr/0015`](adr/0015-all-tuning-data-is-hot-reloadable.md) grounded itself by citing this document straight back. **Slice 6 then falsified it in practice and cost nothing doing so.** Slice 6 is 3c's Layers half, it shipped first, and it introduced real Ruleset content — the diffusion cadence and Layer rates, which [`adr/0044`](adr/0044-the-map-layer-diffusion-cadence-is-the-designers-number-not-the-profilers.md) classifies as hot-reloadable data — with no Ruleset file in existence. It was free because `LayerRuleset` arrives as a **constructor argument rather than a `const`**, which is `adr/0015`'s own no-`const` rule doing the work the ordering was a proxy for.

**What replaces it is checkable where the ordering never was:** `LayerRuleset` is the standing test case, so **slice 8 is not done until the Layer cadence and rates load from a file.** The general form: what protects against the Citybound failure is the no-`const` rule, not the milestone sequence.

---

## Phase 2 — the simulation proper

**✅ RE-DERIVED 2026-08-16 by session K.** The ordering below is now a derivation rather than a record. What it replaces was written before conserved Money, Hinterlands, Office, the labour system, transit and every Service existed, and it survived only because *"a block nobody enforces is not a block, it is a document disagreeing with the build"* — 5a through 5c shipped underneath a paragraph saying Phase 2 could not be planned.

> **What K had that its predecessors did not.** The **S0** half of the old block was discharged 2026-08-14: S0a and S0b have both run, so the tables hold 1M in 86 MiB and a Tick with work in it is **8.72 ms at 1M**. The **input** half was discharged 2026-08-15 by the corpus sweep that took the inventory below from eighteen rows to thirty-nine. What is **not** discharged is sessions **E** and **G**, which move the traffic and fidelity rows — so **20 through 23 are sequenced provisionally and everything above them is not**. See *What is parked, and why* below.

**Numbering follows [`PROCESS.md`](../PROCESS.md) → *Numbering*: the integer is the position, a shipped milestone's number is frozen, and inserting renumbers the unshipped tail.** Session K's insertion moved 126 references; renumbering the shipped head would have moved 282, which is why 5a–5c keep names that no longer sort.

| # | Milestone | Risk retired |
|---|---|---|
| **5a** | Road Graph and Streets | That geometry leaks into the simulation and the routing graph stops being uniform |
| **5a-bis** | The Lot subdivider and the road editor | That a design document's precondition is a hypothesis about the build. *Every Building is on the Road Graph by construction* was true because there **was** no Road Graph; frontage is enforced now, so [`adr/0014`](adr/0014-grid-streets-with-freeform-arterials.md)'s asymmetry and `CONTEXT.md` → Frontage's deletion of the utility network are standing on something. Secondarily [`adr/0012`](adr/0012-routing-intent-lives-in-the-agent.md)'s: an invalidation contract no player has ever driven is a contract nobody has tested |
| **5b** ✅ | Trips, Legs and the pedestrian layer — **DONE 2026-08-12** ([`plans/0021`](../plans/0021-trips-legs-and-the-pedestrian-layer.md)), tasks 1, 2, 3, 5 and 7 | **The irreversible one.** A single-Leg Trip model propagates into Lot valuation, cost functions and every balance constant, and is what Citybound could never undo. **⚠ RETIRED 2026-08-12 by [`adr/0075`](adr/0075-a-leg-is-a-plan-and-a-traveller-is-a-cursor.md) and tasks 1–3**, which built the three-way split with an intrusive Leg list and eager Leg creation — a single-Leg model is not expressible in that structure. What remained of the milestone was **measurement**, which is not what makes a milestone irreversible, and it moved to 5b-bis ([`adr/0080`](adr/0080-phase-4-does-not-wait-on-a-trip-generator-and-a-trip-is-entered-by-command.md)). **⚠ The milestone shipped owing one consequence it could not build**: [`adr/0041`](adr/0041-volume-is-attributed-by-the-traveller-not-the-district-pair.md)'s Segment volume needs *a next Segment every Tick* and `adr/0075` gives a Leg a cost and no path, so **attribution waits on a path source — which is 5c**, ~~not on vehicles~~. ⚠ **THAT LAST CLAUSE IS WRONG, corrected 2026-08-14 while scoping 5c** ([`plans/0026`](../plans/0026-statistical-resolution-and-the-travel-time-matrix.md)): `03 §3.3` increments volume when a **vehicular** Traveller enters, and `WorldInvariants.TrafficIsConserved` counts only Travellers whose Leg mode is not `Foot`, so **a path source is necessary and not sufficient** — nothing in the build creates a non-`Foot` Leg. 5c therefore builds the **vehicular Leg at Statistical resolution** as well, and without it 5c would ship a matrix and a next hop and still leave 22 with nothing to threshold. ***A precondition stated in the singular is a precondition nobody has finished counting***, and this is the third sighting in three milestones. The columns and the conservation invariant exist; nothing increments them |
| **5b-bis** ✅ | Jobs, the commute, and the first Trip generator — **DONE 2026-08-13** ([`plans/0023`](../plans/0023-jobs-and-the-commute.md)), all eight tasks | **That every number milestone 5b was to produce is taken against a fabricated origin-destination draw and lands in [`0013`](../plans/0013-tick-budget.md) and [`0002`](../plans/0002-open-questions.md) as measured fact.** S2 R4 already ran that experiment: a uniform draw put a District-granular route's detour at 18.52% where a local-trip draw puts it at **128.82%**, *"which under `05 §4` is a different city"*. 5b's tasks 4, 6 and 8 each measure a **distribution** — peak pedestrian density, the Commute Budget percentile, the walk-search multiplicand — so each is void without a generator, and **this document had scheduled none**: every generator the corpus names sits in *Mechanisms with no milestone* below. [`adr/0080`](adr/0080-phase-4-does-not-wait-on-a-trip-generator-and-a-trip-is-entered-by-command.md), [`adr/0081`](adr/0081-the-commute-is-the-first-trip-generator-and-a-job-is-taken-by-satisficing-on-distance.md) |
| **5c** ✅ | Statistical resolution and the travel-time matrix — **DONE 2026-08-16** ([`plans/0026`](../plans/0026-statistical-resolution-and-the-travel-time-matrix.md)), **all eight tasks**: the routing partition, the matrix, the path source, the route cache, the **vehicular Leg at Statistical resolution**, the volume-delay function, a runner mode and the long run. ⚠ **Task 8 was the named ratifier for four hash-bearing numbers and could not fire** — a generated lattice grows with the population it serves, so BPR is only ever evaluated where it is nearly flat; all four rows now name a **world** instead, and `ConnectedCityCongestionTests` is it. **Task 8 discharged its other job**, `adr/0006`'s acceptance run, over 49 whole Days. ⚠ **Task 7 added a fourth shipped Ruleset**, `rulesets/congested.toml`, because no existing file states `[traffic]` or `[households]` and therefore **nothing in the repository could ratify either** | That routing intent leaks into the world ([`adr/0012`](adr/0012-routing-intent-lives-in-the-agent.md)) — the GlassBox failure. **⚠ It also inherits 5b's one unpaid consequence**: [`adr/0041`](adr/0041-volume-is-attributed-by-the-traveller-not-the-district-pair.md) needs *a next Segment every Tick* and nothing supplies one, so **Segment volume is not attributed until this milestone's path source exists** ([`plans/0010`](../plans/0010-s2-routing.md) decision 11). Volume is what 22's Stress reads, so a 5c that ships a matrix and no per-Segment next hop leaves 22 with nothing to threshold |
| **6** ✅ | Evidence — the accumulators — **DONE 2026-08-17** ([`plans/0028`](../plans/0028-evidence-the-accumulators.md)), **all seven tasks** — the seventh added while scoping task 4, and **every owed decision closed**: the condemnation trail, the abandonment reason, [`adr/0097`](adr/0097-a-reach-failure-is-counted-on-the-citizen-and-a-stock-failure-is-not-remembered-at-all.md)'s reach-failure count, the assembler, `--evidence` with a fifth Ruleset, the long run, and a Trip's Fate outliving its Trip. ⚠ **Scoping it subtracted a task and routed it to 17**: `CONTEXT.md` names **three** sources of Failure Pressure and only one is built, so attributing Trip failures to a Building is a **decline** decision — ***Evidence reports pressure and does not produce it***. ⚠ **Task 7 found the brief wrong about its own shape**, and the correction is the milestone's sharpest sentence: it was scoped as task 2 verbatim — copy the fact into a trail before the row is freed — and task 2's subject is the **Building**, which is destroyed, where a Trip's is the **Citizen**, who outlives the journey by design. ***What is freed is not always the subject, and it is the subject that decides the shape***, and the two are indistinguishable **from the freeing site**. ⚠ **It also found `TripFate.Stranded` is written by no site in the build** — declared, documented, counted in the Census, produced by nothing: ***a count that reads zero because nothing exercises it and one that reads zero because nothing writes it are different facts, and a Census cannot tell them apart*** | That Evidence is retrofitted. [`02 §9`](02-simulation-model.md) prices it *"cheap if designed in and expensive if retrofitted"*, and every milestone below adds mechanisms that would each have to be reopened to emit into it. ⚠ **Its position is argued from cost of delay rather than from a dependency**, which is unique in this table, and the reason is what the 2026-08-15 sweep found: [`01 §6`](01-player-experience.md) says Evidence is *"scheduled early in `06-roadmap.md`"*, [`00-vision`](00-vision.md) says it is *"scheduled as a foundational feature"*, and this document scheduled it nowhere — ***each named the other as owner***. **The drill-down graph is Phase 3** and is named there |
| **7** | Parking *(was 8)* | That parking is abstracted into a District average, losing the specific diagnosis — and that occupancy leaks, which is an [`adr/0006`](adr/0006-no-collection-grows-with-elapsed-time.md)-class permanent capacity loss. **Two invariants, not one, and neither is per-Tick** ([`adr/0084`](adr/0084-parking-occupancy-is-two-checks-and-an-invariant-over-absent-state-cannot-be-written.md)): a write-site `O(1)` release check and an end-of-run conservation sum. It also owes the **shed radius**, whose ratifier is this milestone's own walk-Leg distribution as occupancy approaches 1 ([`adr/0083`](adr/0083-a-sheds-use-is-the-arrival-query-and-a-stale-shed-is-wrong-by-a-bounded-walk.md)). ⚠ **It is here rather than beside the traffic rows because it is ungated and because [`adr/0008`](adr/0008-walking-is-a-simulated-leg.md)'s car commute is three Legs of which two are zero-length until it lands** — so every congestion figure taken before it is taken on a journey missing both ends |
| **8** | Save/load *(was 10)* — 🟢 **IN FLIGHT 2026-08-17, [`plans/0030`](../plans/0030-save-load.md)**, nine tasks of which **1–7 have shipped**, and ✅ **all five open decisions are closed**, each by the task it blocked. ⚠ **`05 §4` invariant 6 has machinery for the first time in the project's life** — *run N, save, reload, run M*, green over seven cases and two Rulesets — and **the milestone's named risk is measured rather than argued**. ⚠ ***"The end of phase 7" and "the end of the Tick" are different instants***: the copy goes after `World.Advance`, one statement later than [`adr/0087`](adr/0087-a-save-is-copied-at-save-cadence-not-read-from-a-past-that-no-longer-exists.md) says, because `Advance` increments the Tick and the Tick has been saved state since [`adr/0058`](adr/0058-the-tick-is-state-so-the-world-holds-it-and-the-hash-folds-it.md) — a copy before it reloads into a world that re-runs that Tick. ⚠ **That ADR's hash clause is not buildable as written**, since a fold over the copy's bytes is not the State Hash, so **D4's conclusion survives and one of its two reasons falls**; whether a save carries a verified hash is the one question this milestone added. ⚠ **Task 7's findings are about the instruments**: ***a structural test over one fixture measures the fixture's content as much as the structure*** — `adr/0086`'s owed column-set test covered 170 of 187 columns silently, because **`layer_cell` stands at zero rows under all four shipped Rulesets** — and ***a test that allocates heavily is not a local decision in a suite that runs in parallel and asserts on allocation***. ⚠ **The last of them was settled by a memory objection rather than by any of the three options the brief listed**: the save is **streamed** rather than assembled — `Core` writes through an `ISaveSink` and reads through an `ISaveSource`, handing each column's own contiguous storage over — so **nothing proportional to the file is allocated at either end**. The largest single hand-over at 1,000,000 Citizens is **7.63 MiB against a 131.33 MiB file**, which matters because [`adr/0087`](adr/0087-a-save-is-copied-at-save-cadence-not-read-from-a-past-that-no-longer-exists.md) already spends a copy of the world at save time and a staged file would have put a second body of the same order beside it. ***A format with no authored layout can be streamed, because there is nothing to lay out.*** **Two were settled on review the same day** — `Disposition` gains a **third value** for scratch that nothing rebuilds (D3), and the save write is **synchronous in this milestone** with the seam drawn around the **hash** rather than around the write (D4) — and **two more by task 4**: all five fields on the `Simulation` **dissolve** rather than joining the World, three of them answered in their own doc-comments (D5); and there is **no generator version and no world seed** in the header (D6, [`adr/0111`](adr/0111-a-save-that-re-derives-nothing-needs-neither-a-seed-nor-a-generator-version.md)), which amends [`adr/0086`](adr/0086-a-save-has-no-schema-of-its-own-and-the-field-declaration-is-the-format.md)'s table of three and this row's own owed item. ⚠ **The two are one requirement**: a seed is consumed only by something that regenerates from it, no load path calls a generator, and because `adr/0021` **pins** the number, ***a placeholder inverts the guard*** — an absent version is refused by the format version and `generator_version = 1` agrees with itself and loads a pre-terrain save. What replaces the third row is a **class**: the world-creation values that live in the binary rather than in a table, four of which are built and **each of which says *baked into the save* in its own file** with nowhere to be baked into. ⚠ **The live exposure is the Input Log's**, since a replay re-runs the generator from `log.Seed` on every read — ***a version number guards the artefact that re-derives, not the one that restores*** | ~~**Unsaved state**~~ — **the risk moved 2026-08-12, and the milestone got smaller** ([`adr/0086`](adr/0086-a-save-has-no-schema-of-its-own-and-the-field-declaration-is-the-format.md), [`adr/0087`](adr/0087-a-save-is-copied-at-save-cadence-not-read-from-a-past-that-no-longer-exists.md)). The per-field declaration plus `BOR0901` make *state omitted from the file* unrepresentable, so what the save→reload→compare-hash test now finds is **a derived column that does not rebuild to the value it had** — a class 5a-bis has already sighted once. The file needs no authored layout, the snapshot is a copy at the Phase 7 boundary, and the residual risk is the rebuild rather than the write. ⚠ **It is third rather than sixteenth because that residual risk is a property of every milestone below it**: fifteen rows each add derived columns, and shipping the test first makes it their regression harness instead of one late audit of all of them. ⚠ **AMENDED 2026-08-17 while scoping it: *the milestone got smaller* is true of the format and false of the build.** `Rows` has **no restore path at all** — the four allocator scalars are private with no accessors and no setters, and `AllocateSlot`/`FreeSlot` are `private protected` (`Rows.cs:279-320`), so nothing outside the class hierarchy can place a row in a chosen slot. ***A format decision that removes an authored layout removes work from the writer and none from the reader.*** ⚠ **And the named risk had a cheaper instrument all along**: `World.RebuildDerived()` (`World.cs:888`) and `Rows.FoldAll` (`Rows.cs:204`) measure *a derived column that does not rebuild to the value it had* **directly and with no save format**, where the Factorio test measures it only after M Ticks have carried it into saved state. Coverage today is **2 of 28** derived columns (`StateHashTests.cs:92`). ***A risk named after the test that would catch it gets scheduled behind that test*** |
| **9** | Goods between Buildings — the District Pool | That the Rule engine ships a scope it refuses. `RuleEngine.cs:803` throws on `Scope.Pool` **by name**, so [`02 §4.3`](02-simulation-model.md)'s own worked example — a bakery drawing from the District Pool — is unloadable, and no chain in [`04`](04-economy-and-goods.md) can cross the ownership boundary it names. ⚠ **Found 2026-08-16 by session K and listed in no inventory anywhere**, which is the **fourth** blind spot in the table below; the `04 §6` sitting had already hit it and it still did not reach the list. It is the only root with a consumer **already in the build** |
| **10** | Conserved Money and the treasury | That the economy is not conserved and the city has no balance sheet ([`adr/0024`](adr/0024-money-is-conserved-and-the-city-has-a-balance-of-payments.md), [`adr/0031`](adr/0031-one-resource-abstraction-and-depth-not-count.md), `CONTEXT.md` → Money). ⚠ **`HouseholdTable.Money` and `.Savings` are saved columns whose only writers are tests, fixtures and the golden builder** — declared and produced by nothing — and `MoneyIsRepresentable` is an **overflow** check, not a conservation one. Carries the Household balance sheet, Upkeep ([`adr/0035`](adr/0035-infrastructure-is-priced-by-what-it-consumes.md)) and Policy's spend ([`adr/0033`](adr/0033-two-rule-families-scheduled-and-swept.md)) |
| **11** | The land value target and the composed Layers | That six decisions read a field nothing sets ([`02 §2.4`](02-simulation-model.md)). ⚠ **`MapLayers.SetLandValueTarget` has only test callers**; the Layer, its drift and its cadence all shipped in 3c, so this is a **producer** for a structure already in the tree rather than a subsystem. The Fertility and Desirability holes beside it close in the same row ([`adr/0022`](adr/0022-land-is-a-stock-the-city-spends.md), [`adr/0034`](adr/0034-fields-are-sorted-by-source-geometry.md)) |
| **12** | The Provider List and the shopping occasion | That the design has **one** Trip generator and every Commute Budget rung and congestion figure in the corpus is calibrated against it. Six of the seven generators the corpus names are downstream of this one structure ([`04 §6`](04-economy-and-goods.md), [`adr/0032`](adr/0032-services-are-delivered-by-trips-not-by-coverage.md), [`adr/0067`](adr/0067-a-shopping-attempt-is-a-trip-and-a-household-tries-one-provider-per-occasion.md)) |
| **13** | The price surface | That growth is paced by a sample rather than cleared by a market. [`02 §5.2`](02-simulation-model.md) names step 2b as *"the one part of step 2 that does not exist"* (`02 §5.5`, `02 §5.6`, [`04 §4`](04-economy-and-goods.md)) |
| **14** | Hinterlands and arrival through the gate | That nothing says where Households come from, and that no price in the design has an anchor ([`adr/0023`](adr/0023-immigration-arrives-through-the-gate.md), [`adr/0020`](adr/0020-one-live-world-and-settlements-are-derived.md), [`adr/0088`](adr/0088-the-price-of-a-far-hinterland-is-paid-in-your-own-traffic.md); `CONTEXT.md` → Hinterland). ⚠ **This is *not* the old milestone 10** — see *Retired numbering* |
| **15** | Attended services, wages and Skill Tiers | That a Service is coverage rather than a Trip somebody makes ([`adr/0032`](adr/0032-services-are-delivered-by-trips-not-by-coverage.md)), and that a job has no wage ([`adr/0026`](adr/0026-wages-are-posted-locally-and-never-cleared.md), [`adr/0104`](adr/0104-a-skill-tier-is-earned-by-attendance-and-the-credential-stays-a-wall.md)) |
| **16** | The residential choice model | That the most leaned-on absence in the corpus stays absent. **Four decisions rest on it** and it gates **19** outright ([`02 §5.4`](02-simulation-model.md)) |
| **17** | Decline, Demolish and cleared land | That standing abandoned stock has no sink, which is [`adr/0006`](adr/0006-no-collection-grows-with-elapsed-time.md)-class (`02 §5.9`, [`adr/0053`](adr/0053-failure-pressure-is-a-duration-not-a-tally.md), [`adr/0091`](adr/0091-clearing-land-is-bought-rather-than-taken-and-demolish-is-the-sixth-verb.md), `CONTEXT.md` → Derelict). ⚠ **It also owns the two remaining sources of Failure Pressure and the window they are read over, routed here 2026-08-16 while scoping milestone 6** ([`plans/0028`](../plans/0028-evidence-the-accumulators.md)). `CONTEXT.md` names **three** sources — Trips failing, Rules reaching a reporting terminal, conditions below tolerance — and **only the second is built**: `starved_since` is started solely by `Blocking.Supply` (`RuleEngine.Stop:598-607`). So `CONTEXT.md:203`'s specimen sentence, *"abandoned: 74% of work trips exceeded commute budget over 30 days"* — the corpus's own model of what `LEGIBLE CAUSE` means — is unproducible, and ***in only one of its five clauses***: the destination Building and the verdict are live parameters twelve lines apart inside `TripEngine.Start`, so the **ratio is two integers** and the **window** is the whole gap ([`plans/0012`](../plans/0012-corpus-audit.md)). ⚠ **The window has two derivations and both are refused**: a decaying average is the tally `adr/0053` deleted, and reusing `CondemnAfter × rule.Rate` is the welding [`adr/0079`](adr/0079-a-building-outlives-its-frontage-and-an-address-that-has-none-is-a-hole-the-trip-model-reports.md) forbids by name. It belongs here rather than with Evidence because ***Evidence reports pressure and does not produce it***, and because this row already carries a **sibling window question** — `01 §6`'s sustained-detection duration, below |
| **18** | Needs and the coarse Day wheel | That every Day countdown in the design runs on a wheel whose period is exactly one Day ([`adr/0056`](adr/0056-the-event-wheel-is-two-levels-ticks-and-days.md), [`adr/0010`](adr/0010-one-clock-and-demographics-by-sorting.md), [`adr/0102`](adr/0102-a-housed-departure-is-a-comparison-the-household-re-runs-not-a-threshold-it-crosses.md)–[`adr/0104`](adr/0104-a-skill-tier-is-earned-by-attendance-and-the-credential-stays-a-wall.md)) |
| **19** | Households, the Unplaced Pool and Departure *(was 9a)* | That growth is driven by a global demand scalar. The Pool with per-Household reasons *is* the demand signal, and it is a diagnosis rather than a bar chart |
| **20** | Life Stages and self-generation *(was 9b)* | That the city has no internal demographic engine, so every dwelling change comes from immigration and the housing market has no churn of its own. ⚠ **Gated on [`adr/0011`](adr/0011-household-life-stages-and-self-generating-population.md)'s ⚫** — Life Stages scheduled in Days against a fine wheel whose period is one Day — which **18** is what repairs |
| **21** | Lane-as-entity traffic *(was 6)* | That the vehicle becomes the entity, which costs a spatial index, cache locality, and roughly an order of magnitude ([`adr/0016`](adr/0016-the-lane-is-the-entity-not-the-car.md)). ⚠ **Position provisional — session G moves it** |
| **22** | Stress-driven Fidelity with hysteresis *(was 7a)* | That fidelity depends on the camera, which makes observation change outcomes and destroys replay. ✅ **Position DERIVED 2026-08-16 by session E** ([`plans/0029`](../plans/0029-session-e-fidelity.md) Q4): it is **21 + 1** and has no other freedom — fidelity is a ladder between two tiers, so a Segment cannot be promoted before there is something to promote it into. Its acceptance criteria are `03 §4`'s invariants and §5.1's **hysteresis** scenario, and invariant 3's enumeration — *the* reason it could not be scoped — was written the same day ([`adr/0107`](adr/0107-a-demotion-discards-the-cursor-and-nothing-it-discards-has-to-be-invented.md)) |
| **23** | The rotating Audit *(was 7b)* | The known blind spot — junctions that fail at *low* volume through turning conflicts, which V/C structurally cannot see. ✅ **Position DERIVED 2026-08-16 by session E**: it is **22 + 1** — the audit **promotes**, so it needs 22's promotion machinery and the Cap-ordered admission it now ranks last in ([`adr/0108`](adr/0108-repeated-divergence-is-a-bug-report-and-the-queue-decides-how-long-a-promotion-lasts.md)). ⚠ **It is smaller than this row implies**: it builds the rotation, the comparison, the record and the admission, and **no escalation machinery at all**. **Merging it into 22 was considered and refused** on rule 4 — the two retire *different* risks, and a merged row would retire the camera risk and be declared done with the blind spot uncovered |
| **24** | Terrain, Shocks and the Intensity Dial | That the ground under a standing city cannot change, so [`01 §5.2`](01-player-experience.md)'s *"only instrument that can measure redundancy"* does not exist and the Dial's third sub-dial has nothing to scale (`01 §5.3`–`01 §5.6`, [`adr/0021`](adr/0021-the-map-is-bounded-procedural-and-terrain-never-enters-a-tick.md)). ⚠ **Appended rather than placed**: it depends on nothing in the spine and nothing in the spine depends on it, so its position is the weakest claim in this table |

**Milestone 5b is the one to protect if the phase runs long**, and it is the one whose value is least visible while it is being built. The payoff is **Severance**, argued in [`adr/0014`](adr/0014-grid-streets-with-freeform-arterials.md) and [`03 §3.7`](03-agent-architecture.md).

### Retired numbering

**Kept for ever, so a citation written before 2026-08-16 still resolves.** [`PROCESS.md`](../PROCESS.md) → *Numbering* requires this table on every renumber. **A number is never re-used for different work**, which is why the table reads in both directions.

| Was | Is now | Milestone |
|---|---|---|
| **6** | **21** | Lane-as-entity traffic |
| **7a** | **22** | Stress-driven Fidelity with hysteresis |
| **7b** | **23** | The rotating Audit |
| **8** | **7** | Parking |
| **9a** | **19** | Households, the Unplaced Pool and Departure |
| **9b** | **20** | Life Stages and self-generation |
| **10** | **8** | Save/load |

⚠ **Old milestone 10 was read as two different things and one of them was never a milestone.** This table said **Save/load**; [`plans/0000`](../plans/0000-board.md)'s *Blocked* row said *"10 — the Outside"* and [`plans/0002`](../plans/0002-open-questions.md) §D2 routed an Outside Connection's throughput ceiling to *"`06` milestone 10"*. Session **J** closed both halves of `05 §7` — the save format **and** the Outside layout — and the two collapsed onto one number. **The Outside was never scheduled**: it is now **14**, and it had a row in the inventory below the whole time. `plans/0012` *Cause 1*.

### The dependency graph

**The rows below the milestone table are a flat list and the mechanisms are a graph.** The 2026-08-15 sweep said so and recorded no edges; ***a re-derivation reading that table top to bottom would sequence it wrongly***, because a flat list presents forty independent choices where there are six roots and the rest consequences. The edges are written here because this document owns the sequence and they *are* the sequence's warrant. **Each edge cites the document that owns it — no edge below asserts what a mechanism does.**

**The six roots.** Nothing in the inventory precedes any of them.

| Root | Milestone | Why it is a root |
|---|---|---|
| **Evidence — the accumulators** | 6 | ✅ **Split and placed: the accumulators are this row and the drill-down graph is Phase 3**, named there. ⚠ **This cell said *milestone 21* until 2026-08-16** — which the retired-numbering table above makes **lane-as-entity traffic**, so the roots table contradicted its own **#** column. *A renumber's mapping applies to citations written before it and not to the row it is renumbering into*, and this is the second and third sighting of that trap rather than [`plans/0000`](../plans/0000-board.md)'s *one live* one. The each-names-the-other defect below is why 6 is third rather than sixteenth. Depends on nothing; everything below it would have to be reopened to emit into it ([`02 §9`](02-simulation-model.md))|
| **The District Pool** | 9 | Needs road connectivity, which shipped in 5a (`RuleEngine.cs:771`) |
| **Conserved Money** | 10 | [`adr/0024`](adr/0024-money-is-conserved-and-the-city-has-a-balance-of-payments.md) rests on nothing unbuilt |
| **The land value target** | 11 | The Layer, its drift and its cadence shipped in 3c |
| **The Provider List** | 12 | [`adr/0067`](adr/0067-a-shopping-attempt-is-a-trip-and-a-household-tries-one-provider-per-occasion.md) needs Trips, which shipped in 5b |
| **The coarse Day wheel** | 18 | ✅ **Placed: 18.** [`adr/0056`](adr/0056-the-event-wheel-is-two-levels-ticks-and-days.md); the fine wheel shipped in milestone 4|

**The edges.** Read *A → B* as *B cannot start before A*.

| Edge | Owned by |
|---|---|
| District Pool → the Provider List | A Provider with no stock is untestable; [`04 §6`](04-economy-and-goods.md) steps 1, 2 and 5 |
| District Pool → the price surface | [`04 §4`](04-economy-and-goods.md) |
| Money → the price surface | [`02 §5.5`](02-simulation-model.md) |
| Money → the Hinterland | [`adr/0088`](adr/0088-the-price-of-a-far-hinterland-is-paid-in-your-own-traffic.md)'s price offset |
| Money → Upkeep, Policy, private capital | [`adr/0035`](adr/0035-infrastructure-is-priced-by-what-it-consumes.md), [`adr/0033`](adr/0033-two-rule-families-scheduled-and-swept.md), [`adr/0059`](adr/0059-a-zone-rules-sample-is-a-revisit-period-so-the-ruleset-states-a-duration.md) |
| Land value → the price surface | `02 §5.6`'s initial prices |
| Land value → decline and contagion | `02 §5.9`'s damper |
| Land value → `Demolish` | [`adr/0091`](adr/0091-clearing-land-is-bought-rather-than-taken-and-demolish-is-the-sixth-verb.md)'s compulsory-purchase price |
| Provider List → Attended services | `06`'s own third blind-spot note |
| Provider List → Needs | [`adr/0102`](adr/0102-a-housed-departure-is-a-comparison-the-household-re-runs-not-a-threshold-it-crosses.md)–[`adr/0104`](adr/0104-a-skill-tier-is-earned-by-attendance-and-the-credential-stays-a-wall.md) |
| Provider List → a second Trip generator | [`adr/0032`](adr/0032-services-are-delivered-by-trips-not-by-coverage.md) |
| Price surface → density as a cap | [`adr/0025`](adr/0025-density-is-a-cap-and-it-trades-land-for-materials.md); it never binds until a price clears a Lot |
| Price surface → commercial and industrial placement | `02 §5.2` steps 1 and 3 |
| Attended services → Office, wages and Skill Tiers | [`adr/0104`](adr/0104-a-skill-tier-is-earned-by-attendance-and-the-credential-stays-a-wall.md) — a tier is earned by attendance |
| Attended services → Crime and the Dispatched half | [`adr/0030`](adr/0030-crime-is-an-incident-with-no-perpetrator.md) |
| Office and wages → agglomeration | `02 §5.5` |
| Price surface, Hinterland, Money → the residential choice model | [`02 §5.4`](02-simulation-model.md); the Hinterland is *"an ordinary row"* |
| Choice model, Hinterland, Money, Needs → **19** | [`plans/0000`](../plans/0000-board.md)'s 2026-08-15 assessment of 19 |
| **19**, the Day wheel, Attended services, Taste → **20** | [`adr/0011`](adr/0011-household-life-stages-and-self-generating-population.md), [`adr/0027`](adr/0027-preference-is-drawn-per-household-and-persists-for-life.md), [`adr/0104`](adr/0104-a-skill-tier-is-earned-by-attendance-and-the-credential-stays-a-wall.md) |
| Hinterland, District Pool → Shipments | [`04 §3`](04-economy-and-goods.md) |
| Evidence → trajectory detection | `01 §6`, and its indicators come from **10**, **15**, **19** and **20** |
| **21 → 22** | Fidelity is a ladder between two tiers ([`adr/0007`](adr/0007-stress-driven-simulation-detail.md), [`adr/0005`](adr/0005-two-fidelity-tiers.md)) — a Segment cannot be promoted before there is something to promote it into |
| **22 → 23** | The audit **promotes** on divergence (`03 §3.5`) and ranks last in the Cap-ordered admission ([`adr/0108`](adr/0108-repeated-divergence-is-a-bug-report-and-the-queue-decides-how-long-a-promotion-lasts.md)), so it needs 22's machinery |
| **7 → the traffic cluster's *numbers*** | This document's own **7** row: `adr/0008`'s car commute is three Legs of which two are zero-length until parking lands, so *"every congestion figure taken before it is taken on a journey missing both ends"* |
| **12 → the traffic cluster's *numbers*** | This document's own **12** row: the design has **one** Trip generator and *"every Commute Budget rung and congestion figure in the corpus is calibrated against it"*. S2 R4 measured what the draw is worth — 18.52% detour uniform against **128.82%** local, *"which under `05 §4` is a different city"* |

> ⚠ **The last four were added 2026-08-16 by session E, and two of them this document already stated in
> its own milestone rows.** The **7** and **12** edges are quoted above from those rows verbatim; the
> graph did not carry them, while its own preamble says *"a flat list presents forty independent choices
> where there are six roots and the rest consequences"*. ***A dependency stated in a row's prose is not
> a dependency the graph knows about***, and the graph is what this document calls *the sequence's
> warrant*. **All four are already satisfied by the existing order**, so nothing moves — which is the
> point: they convert a position that happened to be right into one that is derived.

### What is parked, and why

**The traffic and fidelity cluster is sequenced provisionally and session K may not settle it.** Sessions **E** ([`adr/0005`](adr/0005-two-fidelity-tiers.md) + [`adr/0007`](adr/0007-stress-driven-simulation-detail.md)) and **G** ([`adr/0016`](adr/0016-the-lane-is-the-entity-not-the-car.md)) move what these rows contain, so **20 through 23 hold positions rather than places**, and three mechanisms in the inventory below stay unnumbered inside the cluster: **the driver model**, **mode choice** and **Transit**.

> ✅ **SESSION E RAN 2026-08-16 and its half is discharged: 22 and 23 hold *places*, and they are 21's**
> ([`plans/0029`](../plans/0029-session-e-fidelity.md) Q4). ⚠ **This paragraph marked three rows
> provisional and gave them two owners, when the three are one chain with one degree of freedom** —
> fidelity is a ladder between two tiers, so 22 cannot precede 21; the audit promotes, so 23 cannot
> precede 22. **Two of the three markings were never questions**, and E's freedom over 22 and 23 turns
> out to be **none**. ***A shared blocker written as several independent waits reads as several
> choices***, which is [`plans/0002`](../plans/0002-open-questions.md) §D2's *four rows wait on one
> mechanism under four names*, arriving here on positions instead of numbers. **The cluster's position
> is 21's alone, and 21's is session G's.** The three unnumbered mechanisms stay unnumbered.

⚠ **Say the consequence out loud, because 21 and 22 are being sequenced around a loop nothing closes.** [`adr/0046`](adr/0046-a-driver-routes-on-habit-sight-and-temperament-never-on-current-cost.md) refuses by name the configuration 5c shipped: congestion is priced **on entry to a Segment** (`TripEngine.cs:581`) and routes are computed on **free flow** (`TravelTimeMatrix`). **Congestion is therefore a cost paid and never a cost avoided**, and the traffic model has no feedback term at all. The mechanism that would supply one is the **driver model** row below — and four unset [`plans/0002`](../plans/0002-open-questions.md) §D2 numbers (Temperament, `k`, the Rejoin crossing budget, the Aggravation threshold) plus [`plans/0013`](../plans/0013-tick-budget.md)'s dominant row are all denominated in it. ***Four numbers written four ways and one missing feedback term are one absence***, and it sits behind G.

~~**What that costs the ordering above:** nothing in 6–19 reads a congestion cost, so the demand spine is unaffected. What it costs is **21 and 22's acceptance criteria**, which cannot be written until the loop closes.~~

> ✅ **AMENDED 2026-08-16 by session E — the first sentence stands and the second is withdrawn**
> ([`plans/0029`](../plans/0029-session-e-fidelity.md) Q4). **Nothing in 6–19 reads a congestion cost and
> the demand spine is unaffected**, unchanged. **But the acceptance criteria are writable and two of
> them were written the same day.**
>
> ⚠ ***A milestone's acceptance criteria and the ratification of the numbers it produces are different
> things***, and this sentence was holding a position for the second while naming the first. That is
> [`adr/0043`](adr/0043-a-claim-a-measurement-could-settle-must-not-be-settled-by-argument.md) and
> [`adr/0052`](adr/0052-a-hash-bearing-number-is-chosen-with-a-named-ratifier-or-not-at-all.md)'s split
> arriving at a roadmap — and **an unratified number is not a reason to hold a position**, or this table
> could not be sequenced at all.
>
> **21's criterion is [`03 §5.1`](03-agent-architecture.md), in full, with an admission rule, and it has
> been since the first commit.** Its three scenarios are **constructed load profiles** — a junction held
> at capacity, a queue that fills its Segment, a volume spike that falls away — and **not one of them
> reads a route choice**. `§5.1`'s own admission rule proves it cannot: *"a scenario earns a place when
> it names a phenomenon the statistical tier structurally cannot produce"*, and diversion is not such a
> phenomenon — the statistical tier could divert perfectly well. ***The suite is defined over what the
> tier produces, and route choice is not it.***
>
> **22's criterion is `03 §4`'s invariants plus §5.1's hysteresis scenario**, and the one genuinely
> missing piece was invariant 3's unwritten enumeration — which
> [`adr/0107`](adr/0107-a-demotion-discards-the-cursor-and-nothing-it-discards-has-to-be-invented.md)
> supplied on 2026-08-16. **23's is [`adr/0108`](adr/0108-repeated-divergence-is-a-bug-report-and-the-queue-decides-how-long-a-promotion-lasts.md)**,
> which settled the escalation policy an audit with no decided response could not have had one without.
>
> **What the missing loop genuinely costs is the *ratification* of `T_high` and `T_low`**, which `03
> §3.3` measures by *"a sweep of threshold values"* over a city whose congestion pattern is wrong
> without diversion — plus [`adr/0062`](adr/0062-the-microscopic-cap-counts-vehicles-and-nothing-is-ever-evicted.md)'s
> *does the Cap bind in ordinary play*. Both are numbers, both were always measurable, and neither was
> ever this session's or `06`'s.
>
> ⚠ **And *"20 through 23"* above is wrong by one.** **20** is Life Stages and self-generation, gated on
> `adr/0011` and repaired by **18**; it has nothing to do with traffic and its own row carries no
> provisional marking. The cluster is **21 through 23**, which is what
> [`CLAUDE.md`](../CLAUDE.md) says. ***A range written as arithmetic includes whatever the arithmetic
> reaches***, and the row it reached was checked by nobody because the number looked adjacent.

### What Phase 2 as written would actually produce

Not *"a city that is alive"* — that claim stood here for a long time and is false. What the ten milestones above build is a **transport and housing simulation**: Households form, look for housing, fail with recorded reasons, move in, commute on multi-Leg Trips, park, age through Life Stages, and generate congestion that promotes specific Segments into microscopic simulation, all of it inspectable only through headless summaries and the hash trace.

It has **no money in it, nobody employed, and no way for anyone to arrive.** That is not a criticism of the ordering; it is the measure of how much the design gained after the ordering was written, and it is the work the re-derivation has to place.

> **⚠ Those two paragraphs contradicted each other for as long as they have both existed, and the
> sitting on `0002` §A found it by walking the milestone list looking for a destination.** The first
> says Phase 2 produces Households that *"**commute** on multi-Leg Trips"*; the second says
> *"**nobody employed**"*. Both are this document's, two paragraphs apart, and **the second is the true
> one** — no milestone in the table above employed anybody, so the commute in the first sentence had
> nothing to commute to. It is now true for 5b-bis onwards and false before it, and the first paragraph
> is left as written with this note rather than silently corrected, because *the milestone list was
> checked for its ordering and read for its prose* is the whole reason it survived.

✅ **The gap those two paragraphs named is what the re-derivation placed, 2026-08-16.** *"No money in
it, nobody employed, and no way for anyone to arrive"* — money is **10**, employment's wage half is
**15**, arrival is **14**, and the sentence calling that *"the work the re-derivation has to place"* is
discharged. **What Phase 2 now produces** is a city with a conserved currency, a land value field, a
market that clears Lots, Households that choose where to live and arrive from somewhere, jobs with
wages, Needs, decline and a way to clear land.

⚠ **It still is not *"a city that is alive"*, and two absences are worth naming rather than implying.**
It has **no renderer**, which is Phase 3 and is the honest boundary. And **20 through 23 are sequenced
around a loop nothing closes** — congestion is priced and never routed around — so what Phase 2 as
re-derived produces is a **demand-side city with a one-directional traffic model**, and the direction
of that gap is stated in *What is parked, and why* rather than left for a later milestone to discover.
***The measure of a re-derivation is which sentences it lets you delete***, and this section keeps two.

---

## Mechanisms with no milestone

**Every row below is settled by an ADR and appears in no milestone anywhere in this document.** They are listed rather than placed, because placing them is the re-derivation's job and a guess here would be exactly the kind of unsourced assertion [`adr/0042`](adr/0042-a-planning-document-cites-and-a-design-document-owns.md) forbids.

> ⚠ **This table has a blind spot, and it is structural rather than a gap in the rows.** *Settled by an ADR* is the entry condition, so the table can hold **unbuilt** mechanisms — specified, with no builder — and can never hold **undesigned** ones. ***An inventory of unplaced mechanisms structurally cannot list a mechanism nobody designed***, which means the corpus's one instrument for *this exists and nothing schedules it* is blind to the class [`adr/0070`](adr/0070-an-unbuilt-mechanism-is-not-a-design-constraint.md) says is most common in a project this young. Found 2026-08-14, when milestone 5c task 5 needed **mode choice** — how a Citizen weighs a walk against a drive — and found it named by no ADR, listed in no milestone, and absent from here. It is the **fourth consecutive milestone** to reach a task before discovering a precondition nobody had counted, after 5b task 4's missing destination set, 5b's missing path, and 5c scoping's *volume needs vehicles and not merely a path*. The sibling table below holds what is known to be undesigned; **it is a list of what somebody happened to notice and must not be read as complete**, which is precisely the property that makes it different from the one above.
>
> ⚠ **AND A THIRD, found 2026-08-15 by the sweep that produced the block of rows dated below — this one is about the table's *shape* rather than its entry condition, and it has two halves.** **The rows are a flat list and the mechanisms are a dependency graph.** Several rows that have sat here for months are downstream of other rows and say so nowhere: *Office, wages and Skill Tiers* cannot start before **Attended services**, which cannot start before the **Provider List**; *density as a cap* never binds until the **price surface** clears a Lot; `Demolish` cannot be priced until the **land value Layer** exists. **A re-derivation reading this table top to bottom would sequence it wrongly**, because a flat list presents twenty independent choices where there are perhaps five roots and fifteen consequences. The two deepest are the **Provider List** and the **land value Layer**. **And the table can only hold what a milestone could schedule** — so four of the corpus's largest debts are invisible to it by construction: a **Ruleset that models a city** is content, an **under-provisioned `Connect` world** is a fixture, a **play session** is neither, and `05 §6`'s **threading policy** is an argument. They are collected in their own table below, because a row saying *a milestone* against any of them would be false.
>
> ⚠ **THERE IS A SECOND BLIND SPOT, found 2026-08-15 assessing milestone 19's gate, and it is worse because it excludes the very class this table claims to hold.** *Settled by an **ADR*** names the **format** of a specification rather than its **existence** — so a mechanism specified thoroughly in a **design document** is unbuilt-and-specified, which is exactly what this table is for, and cannot appear in it. The instance is `02 §5.4`'s **residential choice model**: the logit, the candidate sample, the stay-put alternative, the Hinterland as an ordinary row. **Four ADRs lean on it and none of them settles it**, so every reader reaching for the *Settled by* column found an ADR that *cites* the mechanism and moved on. ***A column that asks where a decision is written will silently exclude everything decided somewhere else***, and the repair is to read the column as *settled anywhere* — the row is added below with `02 §5.4` in it, which is the first non-ADR entry this table has ever carried.

### Mechanisms nothing designs

**No row below is settled by anything.** These are absences that a milestone walked into, recorded so the next one does not walk into the same hole — and, unlike the table above, **this list has no way of being exhaustive**, because nothing generates it.

| Mechanism | Found by | Standing |
|---|---|---|
| **Mode choice** — how a Citizen weighs a walk against a drive on a given journey | 5c task 5, 2026-08-14 | ⚠ **Parked with the traffic cluster 2026-08-16, deliberately unnumbered** — it is `adr/0070`'s *undesigned* class and sessions E and G move whatever would hold it. **Worked around, honestly, one level up.** [`adr/0098`](adr/0098-a-citizen-travels-in-their-households-mode-and-mode-choice-is-undesigned-rather-than-unbuilt.md) makes mode follow **car ownership**, which `01 §8` ledger #2 *does* settle as a persistent Household state and whose simple assumption it names. That is following the design rather than compensating for its absence — but it is not mode choice, and a specification for one supersedes the *mode* half of that ADR outright|

| Mechanism | Settled by | What this document owes |
|---|---|---|
| **The residential choice model** — the logit over sampled candidates, the stay-put alternative, the Hinterland as an ordinary row | **[`02 §5.4`](02-simulation-model.md)**, not an ADR — and see the second blind spot above | ✅ **Placed 2026-08-16: milestone 16.** **a milestone, and it is the most leaned-on absence in the corpus.** ⚠ **Found 2026-08-15 assessing 19's gate**, having been invisible to this table for its whole life. **Four decisions rest on it**: [`adr/0102`](adr/0102-a-housed-departure-is-a-comparison-the-household-re-runs-not-a-threshold-it-crosses.md) makes a housed Departure *this mechanism firing*; [`adr/0069`](adr/0069-placement-is-a-mechanism-of-its-own-and-construction-houses-nobody.md)'s `candidates` is unratifiable *"until there is a choice model to score with"*; [`adr/0032`](adr/0032-services-are-delivered-by-trips-not-by-coverage.md) has school quality entering residential utility **automatically**; and [`adr/0026`](adr/0026-wages-are-posted-locally-and-never-cleared.md) has wages as a competing utility term. **It gates milestone 19 outright**|
| Conserved Money, treasury, balance of payments, borrowing | [`adr/0024`](adr/0024-money-is-conserved-and-the-city-has-a-balance-of-payments.md), [`adr/0031`](adr/0031-one-resource-abstraction-and-depth-not-count.md) | ✅ **Placed: 10.** a milestone|
| Hinterlands, arrival through an Outside Connection, rejected-arrival reasons | [`adr/0023`](adr/0023-immigration-arrives-through-the-gate.md), [`adr/0020`](adr/0020-one-live-world-and-settlements-are-derived.md) | ✅ **Placed: 14.** ⚠ It was read as *old milestone 10* and therefore as scheduled — see *Retired numbering*. a milestone. 19 has the Pool and Departure but nothing says where Households come from|
| Settlements — commute-shed components, merge and split | [`adr/0020`](adr/0020-one-live-world-and-settlements-are-derived.md) | ✅ **Placed: 14.** a milestone|
| Office, wages, the labour market, Skill Tiers, schooling | [`adr/0026`](adr/0026-wages-are-posted-locally-and-never-cleared.md) | ✅ **Placed: 15**, behind Attended services. **HALF PLACED 2026-08-12.** ~~a milestone~~ — **5b-bis takes the *assignment* half and not the wage half** ([`adr/0081`](adr/0081-the-commute-is-the-first-trip-generator-and-a-job-is-taken-by-satisficing-on-distance.md)): a kind declares job slots and a Citizen satisfices onto one on distance, with no wage read. **Office, wages, Skill Tiers and schooling still owe a milestone.** The row is struck in half rather than deleted, because a row deleted on the strength of a part shipping is `plans/0012` *Cause 3*|
| Density as a cap; subdivide versus stack | [`adr/0025`](adr/0025-density-is-a-cap-and-it-trades-land-for-materials.md) | ✅ **Placed: 13** — it never binds until a price clears a Lot. a milestone|
| Services delivered by Trips — Attended, Dispatched, Networked | [`adr/0032`](adr/0032-services-are-delivered-by-trips-not-by-coverage.md) | ✅ **Placed: 15.** a milestone. Service *coverage* survives here only as an overlay, which is what `0032` demoted it to|
| Crime and Incidents; dispatch response Trips | [`adr/0030`](adr/0030-crime-is-an-incident-with-no-perpetrator.md) | ✅ **Placed: 15**, with the Dispatched half. a milestone|
| The nine-Resource abstraction; Utility families; Waste | [`adr/0031`](adr/0031-one-resource-abstraction-and-depth-not-count.md) | ✅ **Placed: 9.** a milestone|
| Infrastructure pricing, Upkeep, design life, wear | [`adr/0035`](adr/0035-infrastructure-is-priced-by-what-it-consumes.md) | ✅ **Placed: 10.** a milestone. Nothing in this document says a road costs anything|
| Policy as a Sweep Rule, and the Sweep Rule family entire | [`adr/0033`](adr/0033-two-rule-families-scheduled-and-swept.md) | ✅ **Placed: 10.** a milestone. 3a/3b are Bin Rules only|
| Transit | [`adr/0029`](adr/0029-transit-is-in-and-right-of-way-is-the-only-axis.md) | ⚠ **Parked with the traffic cluster, deliberately unnumbered** — sessions E and G move it, and it waits on mode choice. a milestone|
| Taste, drawn per Household and persisting for life | [`adr/0027`](adr/0027-preference-is-drawn-per-household-and-persists-for-life.md) | ✅ **Placed: 20.** a milestone|
| Terraforming, procedural generation guarantees, ~~the three version numbers in the save header~~ | [`adr/0021`](adr/0021-the-map-is-bounded-procedural-and-terrain-never-enters-a-tick.md) | ✅ **Placed: 24.** a milestone. **The version numbers are paid, 2026-08-12 by session J** — `05 §7` now names all three and says what each does on a mismatch ([`adr/0086`](adr/0086-a-save-has-no-schema-of-its-own-and-the-field-declaration-is-the-format.md)). Terraforming still owes a milestone|
| Sealing, composed Fertility, Woodland, replanting | [`adr/0022`](adr/0022-land-is-a-stock-the-city-spends.md), [`adr/0034`](adr/0034-fields-are-sorted-by-source-geometry.md) | ✅ **Placed: 11**, with the land value target. a milestone|
| Water Bodies as Bins on the water graph | [`adr/0034`](adr/0034-fields-are-sorted-by-source-geometry.md) | ✅ **Placed: 11.** a milestone|
| Point-of-use noise and near-road pollution **queries** | [`adr/0034`](adr/0034-fields-are-sorted-by-source-geometry.md) | ✅ **Placed: 11.** a milestone. 3c correctly excludes them from the Layers and nothing then builds them|
| Segment volume attribution by the Traveller | [`adr/0041`](adr/0041-volume-is-attributed-by-the-traveller-not-the-district-pair.md) | ✅ **SHIPPED, and this row was stale.** `TripEngine.cs:572` increments it — 5c task 6 paid the debt on 2026-08-14 and nobody struck the row. Found 2026-08-16 by opening the symbol rather than by reading this cell ([`adr/0093`](adr/0093-a-description-of-the-build-is-where-to-look-and-never-what-you-found.md)); ***a gate is discharged by the work and struck by somebody*** applies to an inventory row too. a milestone, or a named place inside 5c–22|
| **The Provider List, the shopping occasion and the cursor** | [`04 §6`](04-economy-and-goods.md), [`adr/0032`](adr/0032-services-are-delivered-by-trips-not-by-coverage.md), [`adr/0067`](adr/0067-a-shopping-attempt-is-a-trip-and-a-household-tries-one-provider-per-occasion.md) | ✅ **Placed: 12.** **a milestone, and it is the deepest blocker in this table.** Every Need-driven Trip generator, six of the seven generators the corpus names, the *Attended* half of the Services row above, and `plans/0013`'s unmeasured *shopping occasions per Household per Day* are all downstream of one absent structure|
| **The land value Layer and the desirability composition** | [`02 §2.4`](02-simulation-model.md) — and `LayerCellTable.cs:126` and `MapLayers.cs:529` both call it a **named hole** | ✅ **Placed: 11.** ⚠ It is a **producer**, not a subsystem: `MapLayers.SetLandValueTarget` has **only test callers**, so the Layer, its drift and its cadence already ship. **a milestone, and it is the second-most leaned-on absence.** 3c built the Layers and not this. ⚠ **Six decisions read it as extant**: [`adr/0091`](adr/0091-clearing-land-is-bought-rather-than-taken-and-demolish-is-the-sixth-verb.md)'s compulsory-purchase price, `02 §5.9`'s contagion damper, `02 §5.6`'s initial prices, `04 §7`'s gentrification damper, `01 §6`'s recovery path, and `05 §9`'s hash-bearing `tick % 256 == 16` offset. The build is the honest party here — it says *hole*; the prose says *field*|
| **The price surface — submarket tâtonnement, the bid, the construction pro-forma** | [`02 §5.5`](02-simulation-model.md), `02 §5.6`, [`04 §4`](04-economy-and-goods.md) | ✅ **Placed: 13.** a milestone. `02 §5.2` names it directly: step 2b *"is the one part of step 2 that does not exist, because it needs a price surface and a Commute Budget"*. **The Budget arrived in 5b-bis and the surface did not**, so that sentence now has one live half and nothing recorded it|
| **The driver model — Habit variants, Sight, Temperament, Diversion, Rejoin, Aggravation** | [`adr/0046`](adr/0046-a-driver-routes-on-habit-sight-and-temperament-never-on-current-cost.md), [`adr/0060`](adr/0060-a-habit-route-is-a-small-set-of-variants-and-which-one-you-take-is-who-you-are.md), [`adr/0061`](adr/0061-a-diversion-rejoins-by-local-descent-and-a-rejoin-is-never-a-search.md) | ⚠ **Parked, deliberately unnumbered** — session G moves it. See *What is parked, and why*, which states the live consequence. **a milestone, and it is what closes `03 §3.4`'s loop.** ⚠ [`plans/0026`](../plans/0026-statistical-resolution-and-the-travel-time-matrix.md) says in as many words *"No Habit, Sight or Temperament"*; **21** is the Lane kernel and **22** is Stress, so neither holds it. ⚠ **The consequence is live**: 5c prices congestion on entry to a Segment (`TripEngine.cs:581`) and routes on free flow (`TravelTimeMatrix`), which is exactly the configuration `adr/0046` refuses by name — *"Route on free-flow and the VDF becomes a display quantity."* **Congestion is a cost paid and never a cost avoided**, so there is no feedback term, and five hash-bearing numbers plus `plans/0013`'s dominant row are denominated in the mechanism that would supply one|
| **Evidence — the accumulators that keep entity references, and the drill-down graph** | [`02 §9`](02-simulation-model.md), [`01 §7`](01-player-experience.md), [`00 §Evidence`](00-vision.md) | **a milestone — and ⚠ two documents already believe this one has it.** `01 §6` says Evidence *"is scheduled early in [`06-roadmap.md`]"*; `00-vision` says it is *"scheduled as a foundational feature"*; this document's only answer is the Phase 3 note below, which attributes the claim **back to `00-vision`**. ***Each names the other as the owner and neither holds a row*** — `plans/0012` *Cause 1* with the two copies pointing at each other rather than drifting apart, which is why no reader of either has ever noticed. `02 §9` calls it *"cheap if designed in and expensive if retrofitted"* |
| **Decline, abandonment and contagion** | `02 §5.9`, [`adr/0053`](adr/0053-failure-pressure-is-a-duration-not-a-tally.md), [`adr/0091`](adr/0091-clearing-land-is-bought-rather-than-taken-and-demolish-is-the-sixth-verb.md) | ✅ **Placed: 17.** a milestone. `01 §6`'s sustained-detection duration is *derived from the time contagion takes to reach a neighbour*, so an authored number is waiting on a mechanism nobody schedules|
| **Private capital — the fourth pacing mechanism** | `02 §5.7`, [`adr/0059`](adr/0059-a-zone-rules-sample-is-a-revisit-period-so-the-ruleset-states-a-duration.md) | ✅ **Placed: 13.** a milestone. `adr/0059` states the split: *"sampling paces cost, and **capital** paces growth. Until capital exists, growth is bounded by the Unplaced Pool"* — which is what the city does today, and it is a stand-in rather than the design|
| **Commercial and industrial placement — growth-loop steps 1 and 3** | `02 §5.2`, `02 §5.5` | ✅ **Placed: 13.** a milestone, and **distinct from the residential choice model above**: *unserved need in reach*, reachable inputs and buyers, Extraction pinned to the ground. `04 §1`'s Materials-gate-construction argument runs entirely through it|
| **Agglomeration — Productivity, Amenity, the congestion cost** | `02 §5.5` | ✅ **Placed: 15.** a milestone. It is a **termination condition rather than a feature** — `02 §5.5`: *"With only the first two, cities grow without bound"*|
| **Shipments — freight Vehicles between Districts and to the gate** | [`04 §3`](04-economy-and-goods.md), `02 §6` | ✅ **Placed: 14**, behind 9. a milestone. ⚠ `CONTEXT.md` → Good makes freight **the only family that contributes congestion**, so every congestion figure taken before it exists is taken on commuters alone|
| **Household finances — the savings reserve, discretionary spend, destitution as an absorbing state** | `CONTEXT.md` → Money and Unemployment, `01 §6` | ✅ **Placed: 10.** a milestone. The *conserved Money* row above supplies the **treasury**; this is the **balance sheet**, which is what the Destitute Departure channel and `04 §5`'s tax-as-velocity argument both read|
| **Needs, their decay, and the housed Departure comparison** | [`adr/0102`](adr/0102-a-housed-departure-is-a-comparison-the-household-re-runs-not-a-threshold-it-crosses.md), [`adr/0103`](adr/0103-a-need-is-where-a-frequent-private-failure-accumulates.md), [`adr/0104`](adr/0104-a-skill-tier-is-earned-by-attendance-and-the-credential-stays-a-wall.md) | ✅ **Placed: 18.** a milestone, **owed on the day they were written (2026-08-15)**. 19 schedules the Pool and the *unhoused* Departure; the accumulator, its decay, the re-evaluation countdown and the Education-Need-to-Skill-Tier step are in no row. All three are downstream of the Provider List|
| **The coarse Day wheel** | [`adr/0010`](adr/0010-one-clock-and-demographics-by-sorting.md), [`adr/0011`](adr/0011-household-life-stages-and-self-generating-population.md), [`adr/0019`](adr/0019-ticks-per-day-is-a-balance-constant-not-a-pacing-knob.md) | a milestone, or a named place inside **4**. Milestone 4 builds the fine Tick wheel; every Day countdown in the design — Life Stages, Need decay, the housed re-evaluation — presumes a second tier that nothing states is being built |
| **`Demolish`, and the four routes to cleared land** | [`01 §2`](01-player-experience.md), [`adr/0091`](adr/0091-clearing-land-is-bought-rather-than-taken-and-demolish-is-the-sixth-verb.md) | ✅ **Placed: 17.** a milestone. 5a-bis built the **road** bulldoze only. `CONTEXT.md` → Derelict makes `Demolish` a derelict Building's **sole** exit, so this is also [`adr/0006`](adr/0006-no-collection-grows-with-elapsed-time.md)'s sink for standing abandoned stock|
| **Shocks, Disasters and Hazard Regions** | `01 §5.2`, `01 §5.3` | ✅ **Placed: 24.** a milestone, and **not** the Terraforming row. `01 §5.2` names a disaster as *"the only instrument that can measure redundancy"*, and the Intensity Dial's third sub-dial has nothing to scale without it|
| **The Intensity Dial, the Modes and the lock policy** | `01 §5.4`–`01 §5.6` | ✅ **Placed: 24.** a milestone, **and it is core work rather than shell** — `01 §5.7` puts the dial in the **Input Log**, so it is simulation input and subject to replay|
| **Trajectory detection and notification** | `01 §6` | ✅ **Split and placed: the indicators fall out of 10, 15, 19 and 20; the notification is Phase 3**, named there. a milestone. Nine named syndromes, and the indicators behind most of them are produced by nothing: Replacement Rate, Retention, Destitute Departures, the underemployment share, the money supply. It also owns the checkable *no orphan figures* test over the Evidence graph|
| **Overlays as a primary view, and the *modelled* marking** | `01 §7`, [`03 §3.9`](03-agent-architecture.md) | ✅ **Placed: Phase 3**, named there — and the *modelled* marking travels with it. a milestone. ⚠ The marking rule is the **entire `HONEST DEGRADATION` discharge for the Microscopic Cap** — `01 §6` deleted the Cap's failure-mode row on the strength of it — and `CONTEXT.md` → Service demotes coverage to this overlay, so the Services row above depends on it too|
| **`Diagnose`'s second half — re-running the Zone Rule predicate on an empty Lot** | `01 §1`, `CONTEXT.md` → Frontage | ✅ **Placed: 17.** a milestone. It is the design's answer to its **most common signal, the non-event**, which Evidence structurally cannot explain because nothing happened. It needs one stored field — *when was this Lot last looked at* — and that is the whole retrofit cost|
| **Pin** | `01 §7`, `CONTEXT.md` → Pin | ✅ **Placed: Phase 3**, named there. a milestone. `01 §6` makes Pin the **third detection path**, and it is the honest limit on a notification system that can only ever warn about failures somebody thought of in advance|
| **A second Trip generator** | `plans/0021` §A, [`adr/0032`](adr/0032-services-are-delivered-by-trips-not-by-coverage.md) | ✅ **Placed: 12** — the shopping occasion is it. a milestone. 5b-bis discharged **one of seven**. ⚠ Every Commute Budget rung and every congestion figure in the corpus is calibrated against that one flow, so this is an **ordering** question rather than a scheduling one: a second generator arriving after 21 invalidates 21's and 22's baselines|

> ⚠ **AND A FOURTH, found 2026-08-16 by session K while sequencing, and it is the one none of the three above would have caught.** The **District Pool** — `Scope.Pool`, which `RuleEngine.cs:803` throws on **by name** — is settled (`02 §4.3` builds its worked example on it), is unbuilt, has a live consumer, and **appeared in no row of this table.** It is not excluded by the entry condition, and it is not undesigned. It was invisible for a different reason: ***a partially-shipped milestone reports as shipped, so a branch of it that throws is invisible to an inventory of unscheduled work.*** Milestone **3a** is marked done in Phase 1's table, `pool` is one scope inside the Rule engine 3a built, and no reader scanning for *what has no milestone* was ever going to open a milestone marked done. **The build was the honest party again** — it says `hole` in a comment and throws with a sentence explaining itself — which makes this [`adr/0093`](adr/0093-a-description-of-the-build-is-where-to-look-and-never-what-you-found.md)'s sibling rather than its instance: the description was not *wrong*, it was *coarser than the thing it described*. The row is now milestone **9**. **The check this implies is cheap and nobody runs it**: every `throw` in `Borough.Core` whose message names a mechanism is a candidate row for this table.
>
> ✅ **The third blind spot's ordering half is discharged, 2026-08-16.** *A re-derivation reading this table top to bottom would sequence it wrongly* was correct, and the edges it warned about are now written down in *The dependency graph* above rather than left in nobody's document. **Six roots and thirty-four consequences**, against the sweep's estimate of five and the rest. What is **not** discharged is the snapshot half: nothing generates this list, so it must be re-run rather than trusted, and session K found one stale row and one missing one on a list that was eight hours old.

**Rows below the Segment-volume line were added 2026-08-15 by a sweep of the whole corpus** — the ADRs, `CONTEXT.md`, `00`–`05` and `plans/` — run because *the choice model was invisible to this table for its whole life* and nobody had asked what else was. Twenty-one rows came back. **The sweep is a snapshot and not an instrument**: nothing generates this list, so it must be re-run rather than trusted, and the second and third blind spots above are what it was correcting for.

### Obligations no milestone can hold

**Nothing below is a mechanism, so no row here can say *a milestone*.** They are collected because each is a real, named debt that the table above **cannot represent** — the third blind spot — and because three of them are the standing ratifier for numbers already baked into the build.

✅ **Session K gave all five an owner and a trigger, 2026-08-16.** That is what this table was missing: a debt with no mechanism still needs somebody and a moment, and *"a milestone"* was the only answer the corpus had a shape for. **Two obligations turned out to have owners already** and were reading as unowned because nothing had said so; **two want a session letter**, which is now assigned; **one has no owner in Phase 2 at all**, and naming that is what stops the numbers behind it reading as neglected.

| Obligation | Owner | Trigger |
|---|---|---|
| **A Ruleset that models a city** | **Milestone 12**, and then 13, 15 and 17 | The first `[[building]]` kind that is a **Provider and not a dwelling**. ⚠ **It is deliberately not one content milestone.** 12 is the first row in the sequence whose mechanism cannot run on a single-kind Ruleset — a shopping occasion needs somewhere to shop — so the second `[[zone_rule]]`, the decline Rule and the density bands arrive with 13, 17 and 13 respectively, *beside the mechanism that needs each*. The alternative is a milestone whose risk statement is *"content exists"*, which retires nothing |
| **An under-provisioned `CommandKind.Connect` world** | ~~**5c's close-out**~~ ✅ **DISCHARGED 2026-08-16, and it split two and two.** `ConnectedCityCongestionTests` was built 2026-08-15 and the reading was taken on it: **`[traffic]`'s α, β and clamp survive** — loaded against free-flow occupancy runs 6,784/6,784 to 122,725/80,978 so *decorative* fails, and the clamp takes 0.52% of loaded Segment-Ticks at a peak **6.9× the clamp** so *binds routinely* fails. **`car_ownership_percent`'s two readings both fire and refute the wrong quantity**: at 100% ownership every commute is a drive across a ≤4 km city while `adr/0095`'s rungs are percentiles of a **foot-only** distribution, so it is inert on *reach* and load-bearing on *congestion*. ***A refuting reading named against one consequence cannot refute a number whose live consequence is a different one*** — so [`adr/0052`](adr/0052-a-hash-bearing-number-is-chosen-with-a-named-ratifier-or-not-at-all.md)'s amendment wants a **third clause: a ratifier names a machine, a world and a quantity**. What is owed on that one row is a different pair of refuting readings, denominated in what a drive costs. ⚠ This row read as an unbuilt fixture for a day after the fixture existed, which is [`plans/0002`](../plans/0002-open-questions.md) §D's own *a row moves between tables only when somebody moves it* |
| **A play session** | ⚠ **Nobody, and that is the answer rather than an omission** | The first build with a shell, which is **Phase 3**. The chain is stated below: **S1 + S3 → session L → Phase 3 is plannable**, and nothing in Phase 2 shortens it. **At least eight `plans/0002` §D numbers name this as their only ratifier**, including two world-creation constants already flipped into the build, so naming the chain is what keeps them reading as *waiting on a known thing* rather than as neglected |
| **`05 §6`'s threading policy** | **Session R** | ⚠ **Not milestone 8, and the stated gate was wrong twice.** *"Gates milestones 10 and 11"* named a milestone 11 that has never existed, and old-10 is new-**8**, which session K checked and found **unblocked**: [`adr/0087`](adr/0087-a-save-is-copied-at-save-cadence-not-read-from-a-past-that-no-longer-exists.md) already decides the one threading fact a save needs, in its opening sentence. What §6 still owns — which thread runs `step()`, who publishes the transform history, generation tagging, whether cold queries drain from a queue — gates nothing in 6–24. **Trigger: session L's chain**, since `05 §2`'s boundary is the consumer. ⚠ **AMENDED 2026-08-16 by session T: *gates nothing in 6–24* is true of milestones and is now false of a decision.** [`adr/0105`](adr/0105-the-target-speed-is-4x-at-a-million-and-a-rung-dilates-rather-than-being-withdrawn.md) budgets the simulation at **15.6 ms of a core**, and §6 is what says whose core — so [`adr/0106`](adr/0106-a-wall-clock-budget-names-a-machine-class-and-a-thread-count-or-it-is-not-a-budget.md) makes the target **conditional on R**, and `plans/0013`'s threading lever is now the **only named lever the size of the ~3× gap** between the bill and the target. ***A row that gates no milestone may still be what another decision rests on***, and this table can only see the first. **Trigger is now the earlier of session L's chain and anybody needing to evaluate `0013`'s stop-and-fix condition**, whose third term is a measured thread count. ⚠ **Record what happened instead**: an ADR settled a clause of threading policy inside a session about save format, while the document that owns threading stayed ungrilled — [`adr/0044`](adr/0044-the-map-layer-diffusion-cadence-is-the-designers-number-not-the-profilers.md)'s *citing an ADR is not applying it*, running forwards |
| ~~**The target speed** — 8× / 4× / 2× / 1×~~ | ~~Session T~~ | ✅ **DISCHARGED 2026-08-16 by session T** ([`plans/0027`](../plans/0027-session-t-the-target-speed.md)) — the first of these five to close. **The target is 4× at 1,000,000 Citizens, 15.6 ms on one core of the reference class, and no rung is ever withdrawn** ([`adr/0105`](adr/0105-the-target-speed-is-4x-at-a-million-and-a-rung-dilates-rather-than-being-withdrawn.md), [`adr/0106`](adr/0106-a-wall-clock-budget-names-a-machine-class-and-a-thread-count-or-it-is-not-a-budget.md)). A host that cannot sustain the chosen rung **dilates and says so**. ⚠ **The option set in this row's own title was a ladder session P retired** — 8× is not a rung and 0.5× and 3× are, and `plans/0013` priced the retired set for a month: *a table of options is a fact stored in prose and drifts like any other*. ⚠ **And it turned out the ledger could not have chosen a rung**: its sum was stale by ×4 against a correction it was carrying in its own sidebar, and at ≥44–50 ms **no rung fits**, so picking the one under 100% would have been picking the column where the row known to be wrong is small. ⚠ **The live consequence is that session R stops gating nothing** — see the row above |

⚠ **Session letters skip `I`, `O` and `S`** — the first two read as digits and `S` is the spike axis ([`PROCESS.md`](../PROCESS.md) → *Numbering*). `R` and `T` are the next two free.

**The original statement of each, kept because it carries the evidence:**

| Obligation | Where stated | What it discharges |
|---|---|---|
| **A Ruleset that models a city** — a workplace kind that is not a dwelling, a second `[[zone_rule]]`, a decline Rule, density bands | [`plans/0002`](../plans/0002-open-questions.md) §D1 and §D2, four rows naming it as ratifier; `plans/0023` — *"the city has no land use at all"* | **Content, not code.** ~14 hash-bearing numbers at once — `occupants`, `[placement]`'s three, the five `[roads]` numbers, the three Shift numbers. ⚠ **No milestone's risk statement will ever produce it by accident**, because every shipped Ruleset says in its own header that it models no city and each was correct to |
| **An under-provisioned `CommandKind.Connect` world** | `plans/0002` §D1, two consecutive rows | **A fixture.** `[traffic]`'s α, β and clamp, and `[households] car_ownership_percent`. ⚠ Their named ratifier — 5c task 8 — **ran and could not fire**, because a generated lattice grows with the population it serves. `connect` has existed since 5a-bis and an Input Log can carry one |
| **A play session** | `plans/0002` §D2, at least eight rows; `plans/0003` — *"do it before the first play session"* | **Neither content nor code.** Two world-creation numbers already flipped into the build and unratifiable by any machine, plus every pacing number in §D. ⚠ Phase 3 *"has no plan and cannot have one yet"*, and nothing sequences the chain this document lists as owed before it could |
| **`05 §6`'s threading policy** | `plans/0002` §C; `plans/0024` | **An argument.** ⚠ Both documents say it *"gates milestones 10 and 11"* — **and there is no milestone 11**; Phase 2 ended at 10 when that was written *(pre-2026-08-16 numbering)*. One fact, two homes, and the milestone it half-names has never existed. It also holds `plans/0013` lever 2, the largest unclaimed multiple in the ledger |
| **The target speed** — 8× / 4× / 2× / 1× | [`plans/0013`](../plans/0013-tick-budget.md) — *"a product decision nobody has argued… what would replace it: **a session***" | **An argument.** The whole ledger reads as a share of nothing until it is settled, and `0013`'s own stop-and-fix condition *"cannot be evaluated until it has been argued"*. [`adr/0096`](adr/0096-the-microscopic-cap-derives-from-the-design-speeds-budget-and-not-from-the-top-rungs.md) has already had to re-base the Microscopic Cap on it once |

### Instructions addressed to this document, unexecuted

These are debts with named creditors rather than a survey, so they are checkable.

⚠ **One of the three is executed and the other two now have homes, 2026-08-16 by session K.**

- [`adr/0029`](adr/0029-transit-is-in-and-right-of-way-is-the-only-axis.md) — *"**Dwell time is roadmap work**, and it is dwell time specifically — stops themselves are free."* ⚠ **Still owed, and it travels with Transit**, which is parked with the traffic cluster and deliberately unnumbered. It is a *cost on a Leg*, so it lands wherever Transit does and not before.
- ~~[`adr/0029`](adr/0029-transit-is-in-and-right-of-way-is-the-only-axis.md) — *"`06-roadmap.md` sequences none of this and must be re-derived."*~~ ✅ **EXECUTED 2026-08-16 by session K.** The re-derivation is the Phase 2 table above. ⚠ **Transit itself is still unnumbered**, and that is a refusal rather than an omission: it is one of three rows parked behind sessions E and G. ***An instruction to re-derive is discharged by the derivation, not by every row in it getting a number*** — and saying which rows deliberately did not is what keeps the discharge honest.
- [`adr/0032`](adr/0032-services-are-delivered-by-trips-not-by-coverage.md) — *"School Trips are roughly **+50% on the commute peak** at the 10k target"*, a sizing fact that lands inside 5b/5c and appears in neither. ⚠ **It has a home now and it is not 5b or 5c**: a School Trip is a Trip generator, so it arrives with **12** and its sizing lands on **15**. **Both have shipped without it**, so the standing consequence is the one `06` already records for the second generator — *every Commute Budget rung and every congestion figure in the corpus is calibrated against one flow*, and this names the size of the correction owed when the second arrives.

---

## Phase 3 — making it visible

**Phase 3 has no plan, and it cannot have one yet, because the design it would sequence does not exist.**

This is not a deferral. Every other phase is backed by a design document — Phase 1 and Phase 2 by [`02`](02-simulation-model.md), [`03`](03-agent-architecture.md), [`04`](04-economy-and-goods.md) and [`05`](05-technical-architecture.md) — and there is no equivalent for presentation. The work so far has been the simulation's functionality, deliberately and correctly, and rendering has never been designed, never been argued, and has no document to argue.

The one thing known about it is a risk rather than a plan, and it is worth stating precisely because it is easy to assume away: **the sim/render boundary has never been grilled, and the interface that would serve it was re-argued to serve something else.** [`0002`](../plans/0002-open-questions.md)'s coverage map puts `05 §2` among the never-argued sections and warns that *"threading policy, save format, and the sim/render boundary are assumed by every decision made so far."* [`adr/0002`](adr/0002-simulation-is-an-engine-agnostic-library.md) was then rewritten around **hot and cold query flavours** on the finding that it had *"assumed a renderer because rendering is what an engine boundary is usually for"* when the actual consumer is an inspector. So the boundary Phase 3 would build on is currently shaped for drill-down, not for drawing, and nobody has checked what that costs.

**What is owed before Phase 3 can be planned:**

1. **S1 and S3 run**, and their numbers recorded. They are the empirical inputs — a rendering ceiling and a UI-cost figure — and this is their real job, now that the engine decision they were framed as gating has been taken elsewhere.
2. **A presentation design written.** It does not exist. It has to cover at minimum what is drawn and what is not, the query surface the shell actually needs, and what `05 §2`'s boundary costs under a renderer.
3. **That design grilled**, as every other design document has been.

Only then is there something to sequence. The board tracks this chain.

### What Phase 3 already owns, named 2026-08-16 by session K

**Named rather than sequenced, and that is the point.** These are the shell halves of four rows that session K split on the core/shell line — the core half took a Phase 2 number, and the half that draws something came here. **Listing them is what stops them being invisible**: before this, three of the four sat in *Mechanisms with no milestone* saying *"a milestone"*, in a document whose only presentation section says no milestone can exist yet. ***An obligation routed to a phase that cannot hold it is an obligation with no owner***, which is the same defect as the Evidence row's — and this list is the cheapest available repair, because it commits to nothing but visibility.

| What | Its core half | Owned by |
|---|---|---|
| **The Evidence drill-down graph** | milestone **6**, the accumulators *(this cell said **21** until 2026-08-16)* | [`02 §9`](02-simulation-model.md), [`01 §7`](01-player-experience.md) |
| **Overlays as a primary view, and the *modelled* marking** | — none; it is shell throughout | `01 §7`, [`03 §3.9`](03-agent-architecture.md) |
| **Notification, and the nine syndromes** | the indicators, which fall out of milestones **10**, **15**, **19** and **20** *(this cell said **8** until 2026-08-16)* | `01 §6` |
| **Pin** | — none | `01 §7`, `CONTEXT.md` → Pin |

⚠ **Both corrections above are the renumber, and the second is the mapping run on a citation that had already taken it.** *Old 10 → 8* is **Save/load**, which produces no indicator; the indicator list names the **money supply**, which is **10**, and the inventory row and the edge list both say 10. ***A retired-numbering table makes an old citation resolve and cannot stop a new one being translated as though it were old*** — so the mapping is applied by reading each citation's **subject**, never by its digits, which is [`plans/0012`](../plans/0012-corpus-audit.md) *Cause 5* on numbers that name a position rather than a quantity.

⚠ **The *modelled* marking is load-bearing beyond presentation and must not be read as decoration.** `01 §6` deleted the Microscopic Cap's failure-mode row on the strength of it, so it is the entire `HONEST DEGRADATION` discharge for a cap whose value is still unset — which makes it the one row here that a Phase 2 decision already depends on.

Two disciplines are already settled elsewhere and will constrain whatever that design says, so they are cited here rather than argued: **not every Citizen is rendered** ([`01 §7`](01-player-experience.md) — *"every visible agent is a promise you have to keep"*), and **aggressive distance LOD and frustum culling from day one** ([`05 §11`](05-technical-architecture.md), [`00-vision`](00-vision.md) — the Cities: Skylines 2 cautionary tale). **Evidence is built early, not late** is [`00-vision §Evidence`](00-vision.md)'s, and what Evidence must answer is [`02 §9`](02-simulation-model.md)'s.

---

## Where the project stops being an engineering exercise

**Phase 3 is the transition, and getting there matters more than getting any individual earlier step perfect.**

Everything before it is a simulation that only its author can see, verified by hashes and headless assertions. That is the right way to build it and the tests are load-bearing forever, but a headless simulation is not a game and cannot be judged as one. The moment the city is visible and drillable is the moment the design starts producing feedback — patterns nobody designed, failures nobody predicted, and the first honest answer to whether the causal chain in [`00-vision.md`](00-vision.md) is actually interesting to follow.

The practical consequence for sequencing: when a milestone starts consuming sessions on polish rather than on retiring its named risk, that is the signal to stop and move on. The risk field exists partly to make that judgement easy — rule 4's corollary is a stopping rule, and it is meant to be used.

This is the failure mode with the best documented precedent in the reference material, and it is not a lack of skill. [`adr/0018`](adr/0018-prefer-off-the-shelf-infrastructure.md) records what happened to Citybound in full: the thesis survived and the yak-shave did not. Nothing in Phase 1 or Phase 2 is more interesting than the moment the city is visible and drillable, and the ordering above exists to keep that moment from receding.

The smell tests in [`00-vision.md`](00-vision.md) are the honest measure of whether the roadmap worked. Three of the five are testable at the end of Phase 1; the last two need Phase 3.

---

## What this document no longer contains

Removed under [`adr/0042`](adr/0042-a-planning-document-cites-and-a-design-document-owns.md), with where each now lives. Listed so nobody looks for them here and concludes they were dropped.

| Was here | Lives in |
|---|---|
| Milestone contents — mechanisms, algorithms, data structures | [`CONTEXT.md`](../CONTEXT.md), `00`–`05`, and the ADRs |
| Phase 0 and Phase 1 ordering, gates and sittings | [`plans/0003-build-plan.md`](../plans/0003-build-plan.md) |
| Live status — what is done, next, blocked | [`plans/0000-board.md`](../plans/0000-board.md) |
| *Open decisions that block milestones* | [`plans/0000-board.md`](../plans/0000-board.md)'s argument track, which is the live list. This document's table had been struck down to nothing while eleven real gates existed elsewhere |
| The definition of *runnable*, and the invariant list | [`CLAUDE.md`](../CLAUDE.md) and [`02 §10`](02-simulation-model.md). The version here said invariants *"run in debug builds"*, which `02 §10` calls backwards and which the shipped code does not do |
| *What is deliberately not in this roadmap* | [`deferred.md`](deferred.md), which owns deferrals. The version here still listed transit, which [`adr/0029`](adr/0029-transit-is-in-and-right-of-way-is-the-only-axis.md) admitted |
| The Simulator Effect, and the answer to it | [`00-vision.md`](00-vision.md), with the full Hopkins quote |
| What Evidence must be able to answer | [`02 §9`](02-simulation-model.md) |
| Citybound's yak-shave, in detail | [`adr/0018`](adr/0018-prefer-off-the-shelf-infrastructure.md) |
| Severance, and why the pedestrian layer earns its cost | [`adr/0014`](adr/0014-grid-streets-with-freeform-arterials.md), [`03 §3.7`](03-agent-architecture.md) |
| Spike specifications and target figures | [`plans/0003-build-plan.md`](../plans/0003-build-plan.md), [`spike-results.md`](spike-results.md) |
