# The Ruleset is validated where it is parsed, and only integers and strings cross into the core

**`Borough.Formats` parses the Ruleset, resolves every name to an id, and runs every refusal; `Borough.Core` receives a validated, string-free table of integers.** The parser is **Tomlyn**, and it is not a core dependency, so [`0003`](0003-deterministic-integer-simulation.md)'s exception machinery does not apply. **A tuning number reaches the simulation as a bare TOML integer or as a quoted decimal string** converted by our own routine — never through a library's `double`. `LEGIBLE CAUSE` `FAST ITERATION` `SOLVE THE ACTUAL PROBLEM`

## Why

### `02 §4.3` says TOML *"requires no parser"*, and that is false

.NET has no TOML reader. Something is taken or something is written, and the sentence is why the dependency sat unnamed through six slices while three documents referred to it. **Tomlyn** is taken: 5.3M downloads, actively maintained, and — the property that actually decides it — a syntax tree carrying source spans, which is what [`0015`](0015-all-tuning-data-is-hot-reloadable.md) already requires when it promises *"a file, a line, and a rule name."* [`0018`](0018-prefer-off-the-shelf-infrastructure.md) demands a written exception naming a property no library provides before anything is built by hand. There is no such property here, so writing one is not available.

### The float hazard is real, and it is invisible

[`0003`](0003-deterministic-integer-simulation.md) bans `float` and `double` from simulation state and arithmetic. TOML has a native float type, and every library will parse `decline_rate = 0.15` into a `double`. Three spellings were available:

| | The designer writes | Exposure |
|---|---|---|
| decimals, converted in `Formats` | `decline_rate = 0.15` | a `double` exists for microseconds, and the conversion must be pinned by hand |
| fixed-point integers | `decline_rate = 9830` | none |
| **decimals as strings** | `decline_rate = "0.15"` | **none** |

**The middle one is correct and defeats the ADR it serves.** `0015`'s acceptance test is *"changing a production ratio and seeing the effect must take seconds"*, and a designer computing Q16.16 in their head is not that. It also fails the reason the Ruleset exists at all: the balance surface is meant to be **enumerable and readable**, and `9830` is neither.

**The first one is rejected on the shape of its failure rather than on its likelihood.** A `double`-mediated conversion is right on both machines almost always, and when it is not, the symptom is a State Hash divergence between two developers with no diff to look at — the class of bug `05 §7` says costs days. Quote marks make the hazard structurally impossible instead of carefully managed, which is the same move `0034` made when it split the Cell out of the Chunk: remove the welding rather than document it.

So the library only ever hands us **strings, integers and booleans**. Its float path is never on the path, and an unquoted decimal is a **named refusal** rather than a coercion.

### The validator cannot live in the core, and the argument is `adr/0002`

A cycle check walks `on_fail` names. A `fills` check compares a declared resource against a Bin's. Both need names, and both must report one — *"rule `bake_bread` has a cycle"* is the whole deliverable, because `0015` makes **error-message quality part of the deliverable, not polish**. [`0002`](0002-simulation-is-an-engine-agnostic-library.md) forbids the core from producing a string a human reads.

**A validator that cannot name what it rejected is not a validator.** So it sits with the parser, where the names are, and the core receives ids.

The cost is drift: `Formats` could accept a Ruleset `Core` cannot run. That is answered the way this project answers drift elsewhere and not with a second validator — **the interpreter refuses an id it does not know** rather than trusting the table. One assertion, not a duplicated rule set.

### What this does to `adr/0003`'s owed exception

`0003` requires any **core** dependency be argued explicitly, because a determinism liability entering the core is not recoverable. Tomlyn enters `Borough.Formats`, which already declines to load `Borough.Analysers` — its own project file says so — because its job is to write the strings the core's lints forbid.

**So the exception is not owed, and something narrower is.** What matters was never which assembly holds the parser; a nondeterministic parse poisons the simulation from any distance. What matters is the **values crossing the boundary**, and the rule is one line: *nothing but integers and strings crosses from the parser into the loader.* Under that rule the exposure is a digit-to-integer conversion and our own decimal routine, both of which are ours and both of which are testable.

## Rejected

**Decimals converted through the library's `double`.** Rejected on failure shape, above. It reopens if the conversion is ever proven bit-identical across platforms *and* the proof is mechanised — but the proof is the expensive part and the quote marks are free.

**Fixed-point integers in the file.** Correct, zero-exposure, and it attacks `0015`'s acceptance test directly. Rejected on the ADR it serves.

**Validation in the core.** Fails `0002` at the exact point that matters, since the validator's entire output is human-readable.

**Splitting refusals between the two projects** — structural in `Formats`, semantic in `Core`. No principle decides which check goes where, and [`0045`](0045-a-fallback-chain-is-a-source-ladder-over-one-bin.md) already established that its two refusals are *"one walk at load"* — the same walk. Two error surfaces where `0015` promises one.

## Consequences

- **Three refusals, not two.** `0045` handed this session the `on_fail` **cycle check** and the **`fills` check**. The decimal rule generates a third: **an unquoted decimal is refused by name**, never coerced. All three are one load-time walk in `Borough.Formats`, on the error surface `0015` specifies.
  - **There are now SIXTY-FOUR at load and a sixty-fifth on reload, and this bullet is the count of record — and it is the ONLY copy of it.** ⚠ **That figure is a MERGE SUM taken 2026-08-24**: the common ancestor stood at fifty-nine, `milestone-24-terrain-scoping` added four and `main` added one, and the two sets are disjoint. ***It is the one count in this file that composed across the merge***, because each branch recorded what it ADDED rather than what the total then was — which is the habit the recount bullets below did not have, and the reason they collided. ⚠ **The sixtieth is milestone 27 task 6's**: a `[[business]]` kind count past 254, which is the Building kind's byte-width refusal arriving on the **second** kind namespace (`adr/0141`). ***It is a separate site rather than a widened one*** because the two namespaces are independent, so one file may declare 200 of each and neither guard should fire. ⚠ **RECOUNTED 2026-08-18 by `06` milestone 10 task 3, which added one and found the other thirty-six.** The figure went **22 → 58** before this milestone's own refusal, and the shape of the drift is worse than its size: **seventeen of the thirty-six sit in `[[rule]]`, `[[resource]]` and `[[building]]`, every one of which existed on the day of the 2026-08-11 correction.** So the number was an undercount of its own scope on the day it was last written down. ***A count corrected by adding what you remember adding is still a count nobody has taken*** — the 2026-08-11 pass walked the four ADRs it knew had moved it and never walked the loader, which is `adr/0093` on a number rather than on a mechanism: *the build is where to look*. The uncounted blocks are **seven over `[[rule]]`** (`rate` outside the wheel; `apply` declaring a band *and* a `derived`; a `percent` that is not one; a band with `max` below `min`; `fills` naming the write-only map scope; a non-positive `amount`; a map term in `inputs`), **three over `[[building]] bins`** (a money Bin declaring a capacity, a non-positive capacity, two Bins for one Resource), **six more over `[[building]]`** (a kind count past 254; a negative `jobs`; and four over the Shift band — stated with no employment, employment with none stated, and each hour outside its range), **one over `[[resource]]`** (`storage`, a named hole), **the shared `interval` range check** (one site serving `[[zone_rule]]`, `[placement]` **and** `[jobs]`, so the last two each silently gained a refusal no document counted), and **eighteen over the five tables that did not exist yet** — `[lots]` 1, `[trips]` 5, `[jobs]` 8, `[households]` 1, `[traffic]` 3. **Milestone 10 task 3 adds the fifty-ninth**: a `global` term naming a **non-conserved** Resource, which is `02 §4.3`'s own *"that is the shape the loader accepts"* acquiring an implementation — the loader had accepted every other shape for six slices, because `Scope.Global` threw in the Rule engine and ***a scope that throws is a scope nothing has to validate***.
  - ⚠ **RECOUNTED AGAIN 2026-08-19 by milestone 10 task 5, and the count of record is now SEVENTY at load and a seventy-first on reload.** Eleven more, all of the *loads clean and misbehaves in silence* class. **Two over `[households]`'s opening-balance band** — one end of a band authored without the other, which has two readings that are different cities; and a band in a file that names no money, where `World.Endow` would throw at world creation and the loader can see both halves. **Two more over the same pair** are ordinary range checks and are counted with them: a negative floor, and a top below the floor. **Six over `[[policy]]`**: a `sweeps` naming no population; a `sweeps` naming one `02 §4.2` declares and this build does not sweep; a missing or non-table `transfer`; an end of a transfer that is not `local` or `global` — `pool` is a *market* whose payment is never authored (`adr/0050`) and `map` holds nothing; a transfer from a Bin to itself, which nets to zero and bounds nothing; a transfer of a **non-money** Resource, which would be a city-wide larder nobody has designed; and an `amount` below one. **And one over `apply`** that is new in kind: a Readout of the **wrong scope**. Every Readout before `balance` hung off a Building, so a Bin Rule could name any declared one; a Policy sweeps Households, so `occupancy` in a `[[policy]]` and `balance` in a `[[rule]]` are both real names with no row to read them from. ⚠ **What `[[policy]]` does NOT get is a money-balance refusal, and that is the interesting half.** Refusal 4 exists because a `[[rule]]`'s money terms are two free-form lists and have to be walked; a Policy states a `from` and a `to`, so the same quantity leaves one Bin and enters the other by construction. ***A transfer written as a direction cannot leak; one written as two lists has to be checked*** — so a new section arrived and the refusal count went up by less than the surface did.
  - ⚠ **RECOUNTED A THIRD TIME AT THE milestone 7 / milestone 10 MERGE, 2026-08-19: SEVENTY-FIVE at load, a seventy-sixth on reload, and 108 sites.** Milestone 7 added **five** over `[parking]` and this bullet did not move, because `RefusalCountTests` was built on the *other* branch and main never had it. **Four are the ordinary shapes** — a second `[parking]` table, a negative `[[building]] parking`, and a non-positive `radius_metres` or `shed_keeps`, each of which loads clean and makes every arrival fail to park. **The fifth is the missing-key pair** that makes the table's two numbers all-or-nothing. ⚠ **The interesting fact is not the five; it is that nothing on the branch that wrote them could have noticed.** ***A guard that holds a number to the build protects the branch it is on and no other***, so a single-copy fact is safe against drifting from itself and defenceless against a concurrent tree — which is `plans/0012` **Cause 1** arriving on the one axis reducing the copies to one was supposed to close. The merge is the first instrument that could see them, and it saw them because the *other* branch had built the check. ***Two branches can each be right about a count of a whole that neither of them holds.***
  - ⚠ **RECOUNTED A FOURTH TIME 2026-08-20 by `06` milestone 11 task 1: SEVENTY-SIX at load, a seventy-seventh on reload, and 109 sites.** **One**, over `[[building]] arrivals_per_day` — the key whose presence declares a Building kind an **Outside Connection** (`adr/0088`) — and it refuses a stated **zero or negative**: a gate that admits nobody is a door that never opens, which is this bullet's own *loads clean and does nothing* class. ⚠ **This one is load-bearing in a way the others are not, and that is why it is worth its line.** The refusal is what lets a **single key** do duty as the declaration: with zero refused, *absent* means **not a gate** unambiguously and no second `outside_connection = true` is needed beside it — two spellings of one fact that could disagree. ***A refusal can be cheaper than the field it saves you from adding***, which is `adr/0101`'s Shift-band pairing arriving at a key that has no partner to be paired with.
  - ⚠ **RECOUNTED A FIFTH TIME 2026-08-21 by `06` milestone 11 task 2: EIGHTY-ONE at load, an eighty-second on reload, and 114 sites.** **Five, all over `[[hinterland]]`** — the economy behind one map edge (`adr/0131`). Two are the ordinary band shapes this ADR has seen twice already, a negative floor and a top below the floor, and one is `[households]`' money-balance refusal arriving at the other door: emigrants carrying a balance into a file that names no money would have nowhere to put it, and the loader can see both halves. **The two worth their line are about *identity*.** ⚠ **The duplicate refusal is on a VALUE rather than on a name**, which is new: `[[policy]]` and `[[zone_rule]]` are not registered because nothing refers to one, and a Hinterland is not registered for the same reason — but it *is* referred to, by the **edge a gate stands on**, so two tables declaring `edge = "south"` is ambiguous rather than additive and the collision is invisible until the key is read. ***A section can need a uniqueness check without needing a name.*** And the fifth refuses a `[[hinterland]]` stating only its edge: at 11 the object **is** an edge and a band, so an optional band would let a designer declare that an economy exists and say nothing about it. ⚠ **A zero band is accepted, and the contrast with task 1's `arrivals_per_day = 0` is the point** — a gate admitting nobody is a door that never opens, where a Hinterland whose emigrants carry nothing is a poor economy whose Households still arrive. ***A zero that is a real answer is not the same zero as one that disables the mechanism stating it.***
  - ⚠ **RECOUNTED AN EIGHTH TIME 2026-08-22 by [`plans/0003`](../../plans/0003-build-plan.md)'s hash-moving queue item 12: 118 sites.** **One, over a `[[rule]]`'s map output** — a Rule naming a Map Layer that **exists and cannot be emitted into**. `TryLayer` resolved all three Layer names and nothing downstream asked whether the one named was emittable, so `layer = "land-value"` loaded clean and `RuleEngine.Emit` threw on the first application. ⚠ **It is the first member of this count that did NOT load clean and misbehave in *silence*** — it misbehaved loudly, in the wrong place and at an unpredictable later time, which is the same defect wearing a louder coat: ***a refusal in the engine is a refusal the designer meets as a crash***, and the file had already been accepted by the component whose job was to accept it. **The predicate now has one home**, `MapEmission.IsEmittable`, asked by the loader and by the engine's surviving backstop, because a rule stated in two places is two rules and the one that drifts is the one nobody runs. ⚠ **It is deliberately a different sentence from the unknown-name refusal beside it**: `land-value` is not a typo for a Layer, it *is* a Layer, so telling the author it "is not a Map Layer" would have been false — ***two mistakes that resolve to one message is a message that is wrong for one of them.***
  - ⚠ **RECOUNTED A SEVENTH TIME 2026-08-21 by `06` milestone 11 task 8's diagnosis sitting: 117 sites.** **One, and it is the same guard through a wider door** — task 7 refused a `[placement]` stating no `gives_up_after_days` and said nothing about a file stating **no `[placement]` at all**, which has an inflow into the Pool, no housing *and* no sink. ***A guard written against a missing key does not cover a missing table***, and the case it missed is the worse of the two. `plans/0035` **F28**, found while building a Ruleset variant for an unrelated measurement.
  - ⚠ **RECOUNTED A SIXTH TIME 2026-08-21 by `06` milestone 11 task 7: 116 sites.** **Two, both over
    `[placement] gives_up_after_days`** — how long a Household keeps looking before it gives up and
    leaves (`adr/0130`). One is the ordinary range shape this ADR has seen many times. **The other is
    the one worth its line, and it is a *conditional* requirement, which is new**: the key is optional
    in general and **required of a Ruleset that declares a kind with `arrivals_per_day`**. A gate is
    an inflow into the Unplaced Pool, a Pool with an inflow and no sink grows with elapsed time, and
    `adr/0006` forbids that — so *whoever builds the gate owes the give-up rule* stops being a
    sentence somebody has to remember. ⚠ **What the loader tests is a declared *kind*, not a placed
    gate**, and that line is exactly the one milestone 11 task 5 drew when it found the
    gate↔Hinterland pairing **could not** be checked at load: which edge a gate stands on is a
    property of the *world*, and the loader cannot see a world. ***A kind is a fact about the file and
    a Building is a fact about the world***, and a loader may only refuse on the first.
  - ⚠ **RECOUNTED A NINTH TIME 2026-08-22 by `06` milestone 12 task 1: 123 sites.** **Five, all over `[[lattice]]`** — where the generator lays a Street lattice, and the key that makes a world with **two centres** in it authorable at all (`adr/0134`). Three are identity and geometry: an origin **off the map**, an origin **off the block grid**, and a **duplicate origin**, which is `[[hinterland]]`'s *uniqueness without a name* arriving at a section whose identity is a pair of numbers rather than one string. **The two worth their line are cross-table.** One refuses a `[[lattice]]` in a file with no `[roads]`: a Lattice **is** a Street lattice, so the origin would name ground nothing is ever laid on — the conditional-requirement shape task 7 introduced, pointing the other way. **The other refuses two or more lattices together with `arterial_count > 0`**, and it is the one that would otherwise have been silent: an Arterial is laid **per lattice**, so a file saying *four Arterials cross the map* would mean *four in each of two lattices*. ⚠ ***Which of the two readings the designer meant is not recoverable from the file***, so it is refused rather than divided or doubled — and `adr/0090` has Arterials as a player tool a generator should not be building anyway, which is why every city-modelling Ruleset states 0.
  - ⚠ **RECOUNTED A TENTH TIME 2026-08-22 by `06` milestone 12 task 3: 126 sites.** **Three, all over `[districts]`** — the table that says when a concentration of Buildings is a **centre** (`adr/0134`), and the first Ruleset key a **watershed** reads. One is the duplicate-table refusal every singular section carries. **The two worth their line are the ends of the range, and both are refused for the same reason: a value that silently turns the mechanism off.** At `prominence_percent = 0` every dip in the field clears the threshold, so every local bump is a centre and the city has as many Districts as it has Cells; above **100** no peak can ever clear it, so the Districts collapse to the **road components** and the clip is doing the work alone while the file appears to be configuring a watershed. ⚠ ***Both ends load clean and misbehave in silence***, which is this ADR's own test for a refusal worth writing — and the second is the sharper one, because its symptom is *a plausible answer* rather than an absurd one.
  - ⚠ **RECOUNTED AN ELEVENTH TIME 2026-08-22 by `06` milestone 12 task 4: 129 sites.** **Three more over `[districts]`**, which now carries four keys rather than one — the **cadence**, the **hysteresis band** and the **migration bound**, which are `adr/0134`'s three stability mechanisms as Ruleset data. ⚠ **All three are REQUIRED of a file that states the table**, on `[parking]`'s rule that a stated table states its keys, and the reason is sharper than symmetry: a defaulted cadence is a hash-bearing number nobody chose, and a defaulted band or bound would be a stability mechanism arriving at a setting no designer picked. **Two of the three refuse only the bottom of the range and the third refuses both ends.** `hysteresis_percent = 0` means *a Cell moves on a tie*, which is the one case the band exists for — and above **100** no Cell can ever move, so the boundaries are frozen while the file appears to be tuning them. `migrate_cells` has **no ceiling on purpose**: a large bound is an undamped boundary, which is a legitimate thing to author and announces itself. ⚠ ***The pattern across all six `[districts]` refusals is one shape*** — a value that switches the mechanism off while reading as a setting — which is this ADR's own *loads clean and misbehaves in silence*.
  - ⚠ **RECOUNTED A TWELFTH TIME 2026-08-22 by `06` milestone 12 task 6: 138 sites.** **Nine more, over `[[hinterland]] prices` and the new `[market]` table**, and the one worth reading is neither of theirs. Six are the array-of-inline-tables idiom `[[building]] bins` already had — *not an array*, *an entry that is not an inline table*, *a Resource that is not a good*, *a price of zero*, *the same Good priced twice* — plus a second `[market]`. Two more guard `[market]`'s own keys: `decay_percent` accepts **zero** and refuses **100**, because zero is *no smoothing* and is a real if twitchy market while 100 is a rate that never takes on a new Day and therefore a price frozen at its seed; `move_cap_percent` refuses **both ends**, zero being the omitted table and anything above 100 being a cap that can never bind, since a price never leaves `[0, ceiling]` anyway. ⚠ **The ninth is the one that is not a range check**: a file that states `[districts]` and leaves a `good` unpriced at every `[[hinterland]]` is refused, because a District opens a Pool per Good and the Hinterland's price is the **only** ceiling on it — so an unpriced Good is not unanchored, it is ***free everywhere for ever***, which reads as a balance problem rather than as a missing key. ⚠ **It is gated on `[districts]` and NOT on a gate kind, and the asymmetry is the point**: whether a city has Districts is stated in the file, where which edge a gate stands on is a property of where it was **placed** — so the first is visible at parse time and this ADR reaches it, and the second never can be.
  - ⚠ **RECOUNTED A FOURTEENTH TIME 2026-08-22 by `06` milestone 24 task 3: 139 sites.** **One more over `[[building]] footprint_tiles`**, which counts the Tiles a Building of a kind covers and therefore Seals. ⚠ **It refuses ZERO as well as negatives, and that is the opposite of the three keys beside it**: absent `occupants` means a kind that houses nobody, which is a real building, whereas a footprint of zero is a Building covering no ground — not a thing. **Absent means ONE**, which is `CONTEXT.md` → Sealing's own illustration of the unit, so a file that does not state it moves no world. ⚠ **It was written as the twelfth and is the thirteenth.** Milestone 12 task 6 recounted on `main` the same day and neither branch could see the other — the numbering collision [`06`](../06-roadmap.md) → *Retired numbering* records in its third block, arriving inside a bullet rather than in a filename.
  - ⚠ **RECOUNTED A FIFTEENTH TIME 2026-08-23 by `06` milestone 25 task 2: 140 sites.** **Two, over the tenancy `adr/0141` draws through a Building** — a Bin belongs to the Occupant whose leaving would empty it, and to the premises otherwise. The first refuses an `owner` on a `[[building]] bins` entry that is neither `premises` nor `occupant`, and it is this bullet's own class in its purest form: a mis-spelled owner would leave the Bin on the premises **silently**, and the symptom — stock that does not follow a tenant out of a demolished building — is several milestones downstream of the typo. ⚠ **The second is not authored at all, and that is what makes it worth its line.** A Rule's tenancy is **derived** from its `local` terms, because `adr/0141` declined to split a Rule from its Bins; so the refusal is of a Rule whose local terms address the premises' Bins **and** the Occupant's, which has no subject to run on. ***A derived property still needs a refusal, because deriving it is where the contradiction becomes visible*** — and the contradiction is `adr/0050`'s: a term crossing an ownership boundary is a **trade**, which is `scope = "pool"` and not a second `local`. ⚠ **`fills` counts as a local term here**, which is what makes a fallback chain answerable without walking it: `adr/0045` makes a chain a ladder over one Bin, so a link relieving a tenant's Bin from a premises Bin is the same mixed case arriving one link along.
  - ⚠ **RECOUNTED A SIXTEENTH TIME 2026-08-24 by `06` milestone 24 task 6a: 147 sites.** **Two, over the new `[water]` table** — `adr/0160`, the one key a Ruleset states about water. One refuses a **second `[water]`**, on `[parking]`'s reasoning: there is one sea level, so two tables of numbers for it is ambiguous rather than additive. The other refuses `sea_level_percent` outside **1–99**, and it is the one worth reading, because ***the two ends are refused for different reasons and neither is a range check***. 100 puts every Cell under water, which is not a city. **0 puts the sea at the lowest Cell on the map, which is a world with no water — and that is exactly what omitting the table already says.** So zero is refused not because it is out of range but because it is a *second spelling of an existing state*, and a designer who wrote it would mean something the generator cannot hear. ⚠ **The count went from 145 to 147 on a branch, and 145 was itself a merge artefact** — see the note below.
  - ⚠ **RECOUNTED A SEVENTEENTH TIME 2026-08-24 by `06` milestone 27 task 8: 148 sites, up SEVEN.** **All seven guard the `[founding]` table** — [`adr/0145`](0145-a-business-is-founded-by-a-household-or-arrives-through-a-gate-and-both-land-in-the-pool.md)'s founding channel — and they are worth their line because **six of the seven refuse a file that would LOAD AND RUN AND MEAN NOTHING**, which is this bullet's own class. A second `[founding]`; a `[founding]` in a file declaring no `[[business]]`, so every shop founded would name no trade; one declaring no `family = "money"` Resource, so the band has no Bin to move out of and the channel is inert for ever; one stating no `[placement]`, since the pass rides that trigger rather than owning one; a `founding_band` of zero, which every Household can always afford — ***so the means test that is the whole of the trigger stops discriminating and founding becomes a bare rate***; and a `reconsider_ticks` below the interval, which cannot divide into a sample (`adr/0059`). 🔴 **THE SEVENTH IS THE ONE THAT MATTERS AND IT IS `adr/0006`**: a `[founding]` in a file whose `[placement]` states no `gives_up_after_days`. ***`[founding]` is an inflow into the unpremised pool and nothing tenants a Business yet***, so without a bound every shop founded sits there for ever and the pool grows with elapsed time. ⚠ **That is [`adr/0130`](0130-the-pools-bound-is-a-duration-and-the-unhoused-channel-ships-with-the-gate.md)'s *whoever builds the gate owes the give-up rule* arriving at a SECOND pool** — the gate check above it says the identical sentence about the Unplaced Pool, and neither check can see the other's collection.
  - ⚠ **RECOUNTED AN EIGHTEENTH TIME 2026-08-24 by `06` milestone 24 task 4: 149 sites.** **Two, and the first is not a range check at all** — it refuses `sealing_decay_tau` **in `[layers]`**, where it lived for two milestones as a single global pinned at zero, and names `[[terrain]]` as where it went. `02 §2.4` keys the rate **by terrain type**, so the key moved the day terrain existed. ⚠ ***A moved key must be refused where it used to be rather than ignored***: a file still carrying it means something by it, and reading nothing would leave a designer's stated rate doing nothing with no way to tell — this bullet's own *loads clean and misbehaves in silence*, arriving through a **stale key** rather than a stale value. **The second is the new per-type key's range**, and its ceiling is derived rather than picked: the decay step is `value ÷ tau`, so a tau past **twice a Cell's Tile count** rounds that step to nothing on a *full* Cell and the ground never moves — ***a rate so slow it is silently the same as zero***, which is the one value a designer must not be able to write by accident. **Zero itself is admissible and means never**, which is rock's real answer, so the two ends are again refused for different reasons and only one of them is a bound.
  - ⚠ **RECOUNTED A NINETEENTH TIME 2026-08-24 by `06` milestone 24 task 8b: 150 sites.** **One, over `[layers] woodland_regrowth_days`** — how many Days a Cell cleared of all its forest takes to grow back, and 🔴 **the constant [`adr/0022`](0022-land-is-a-stock-the-city-spends.md) calls load-bearing by name while giving it no owner.** It refuses a duration **above a Cell's Tile count**, and the ceiling is derived rather than picked: one pass puts back `TilesInCell ÷ days` Tiles, so past the Cell that step is under one Tile, a floor of one takes over, and ***the ground would recover in `TilesInCell` Days whatever the file said*** — a duration the mechanism cannot express, which is this bullet's *loads clean and misbehaves in silence* wearing a plausible number. ⚠ **The interesting half is the refusal it shares with the range check beside it, and the contrast with task 4.** A **stated zero is refused here and admissible there**: `sealing_decay_tau = 0` is a *time constant* and zero means **never**, which is rock's real answer, where `woodland_regrowth_days = 0` is a *duration* and zero means **instantly** — the opposite of the absence a designer writing it would mean. ***Two keys on one table, both bounded at zero, and the value means opposite things in each*** — so absence carries *never* for this one and the zero is refused as a spelling of the wrong state, which is [`adr/0160`](0160-a-sea-level-is-authored-ruleset-data-and-a-world-without-water-is-a-world-and-not-a-hole.md)'s rule arriving where the two spellings are not merely redundant but contradictory.
  - ⚠ **RECOUNTED A TWENTIETH TIME 2026-08-24 by `06` milestone 27 task 7: 150 sites, up TWO.** Both guard the `[[business]]` trade's new declaration ([`adr/0141`](0141-a-tenant-owns-what-leaves-with-it-and-the-premises-own-the-capacity.md)'s *Declares* row arriving as `BusinessKindDefinition`). The first refuses a **negative `jobs`**, which reads as *sack everybody* — `KindDefinition.Jobs`' own rule transplanted. 🔴 **The second is the one worth the line: a trade stating a `wage` is REFUSED BY NAME.** `adr/0141` gives the trade *"`jobs`, shift hours and the wage"*, so ***a designer reading that ADR has positive reason to write `wage = 100***" — and the wage is [`adr/0026`](0026-wages-are-posted-locally-and-never-cleared.md) at milestone **15**, where each Business *posts* one and adjusts it by its own fill rate. **A wage is not a declared number; it moves**, so it can never be a key on a kind. ⚠ **Without this refusal the key would LOAD CLEAN AND DO NOTHING FOR EVER**, which is this bullet's own class. 🔴 **It is refused BY NAME because there is no unknown-key check in this loader at all** — every table reads the keys it wants and ignores the rest, so a typo in any Ruleset is silent everywhere ([`plans/0041`](../../plans/0041-the-business-is-a-thing-the-city-contains.md) **G31**). ⚠ **The Shift band added NO site**: `ReadShiftStartBand` is reused rather than mirrored, and every one of its refusals says *this kind*, which a trade is.
  - ⚠ **RECOUNTED A TWENTY-FIRST TIME 2026-08-24 by `06` milestone 24 task 9: 151 sites.** **One, over `[water] flood_level_percent`** — how high a flood reaches, on the same scale as `sea_level_percent`, and the key that decides how much of a world is Hazard Region. It refuses a level **at or below the sea** and one **above 99**, and the lower bound is the interesting half: ground at or below the sea is already under water, so a flood level there describes an **empty** Hazard Region — a key a designer wrote deliberately, that loads clean, and that derives nothing. ***That is [`adr/0123`](0123-desirability-ships-without-its-only-positive-term-and-a-caveat-that-must-travel-gets-a-test.md)'s present-and-permanently-zero arriving in a loader rather than in a mechanism***, which is why it is a refusal and not a clamp. ⚠ **Absence is NOT refused and means no floodplain**, on `adr/0160`'s rule: a steep coast is a world, and the absent key is how that is spelled — the table holds **no rows** rather than rows of depth zero.
  - ⚠ **RECOUNTED A TWENTY-SECOND TIME 2026-08-24 by `06` milestone 27 task 7's content half: 152 sites, up TWO.** Both belong to [`adr/0148`](0148-a-premises-kind-may-declare-its-trade-and-instantiating-one-is-not-housing-anybody.md)'s `[[building]] business` key. The first refuses a kind naming a **trade no `[[business]]` declares** — a premises that comes with nothing, which loads clean and employs nobody. The second refuses a kind declaring a trade with **`occupants = 0`**: one ceiling counts both kinds of tenant (`adr/0147`), so a premises with no room for the shop it comes with is half a sentence. 🔴 **The three keys `adr/0148` MOVED cost no new site** — `jobs`, `shift_start_earliest_hour` and `shift_start_latest_hour` are refused on `[[building]]` from **one** loop over a named array, and the message says where they went rather than that they are unknown, because the author most likely to write one is somebody copying a file that predates the move.
  - ⚠ **RECOUNTED A TWENTY-THIRD TIME 2026-08-24 by `06` milestone 27 task 8's founder half: 153 sites, up ONE.** A `[[business]]` declaring **no `jobs`** is refused in a file that states `[founding]`. [`adr/0146`](0146-founding-costs-a-citizen-and-the-households-money-so-the-founder-is-the-first-worker.md) makes the founder their Business's **first worker** — the whole of the labour cost milestone 27 ships — so ***a trade nobody can work at is a trade nobody can found***, and a founder hired into a jobless trade is over the ceiling from the instant they are hired and sacked on the next `EvictOverflow` sweep. 🔴 **It is EVERY declared trade rather than at least one**, because the pass draws uniformly and a jobless trade in the draw is reachable. ⚠ **Conditional on `[founding]` exactly as `gives_up_after_days` is conditional on a gate**: a trade nobody founds may employ nobody, and several shipped files have one.
  - ⚠ **RECOUNTED A TWENTY-FOURTH TIME 2026-08-24 by `06` milestone 24 task 6b: 157 sites.** **Six, all over `[water]`'s four Bin keys** — `carries`, `capacity_per_cell`, `outflow_per_exit_per_day` and `runoff_per_sealed_cell_per_day` — and they are worth enumerating because only one of the five is a range check. **(1) A SUBSET of the three is refused**: a Bin needs a Resource to hold, a ceiling and a way out, so stating one and defaulting the others would author two numbers on the designer's behalf, and *absent altogether* already means water with no level. **(2) A `carries` naming no `[[resource]]`** — a Water Body holds a Resource the Ruleset declared, not a name it invented. 🔴 **(3) A `carries` whose family is not `utility`, which is [`adr/0161`](0161-a-water-bodys-bin-holds-one-utility-resource-because-a-good-moving-downstream-would-move-with-no-vehicle.md) expressed as a check** — a Water Body moves its contents along an edge of the water graph with no Vehicle, and `adr/0031` defines a **Good** as a Resource whose inter-District movement *requires* one, so ***a good here would be a counterexample to the definition of good sitting inside a loaded world***. **(4) `capacity_per_cell` below 1**: a body that holds nothing is an infinite sink wearing the opposite spelling, and `CONTEXT.md` → Water Body's *"nothing is an infinite sink"* is the whole reason a capacity exists. **(5) `outflow_per_exit_per_day` below 1**, which would mean no body anywhere drains — what omitting the keys already says, which is [`adr/0160`](0160-a-sea-level-is-authored-ruleset-data-and-a-world-without-water-is-a-world-and-not-a-hole.md)'s two-spellings rule for the fourth time on this milestone. 🔴 **(6) `runoff_per_sealed_cell_per_day` below 1, and it is the one whose refusal names another ADR's failure mode outright**: a zero inflow leaves every Water Body's Bin at zero for ever, which is a level nothing can move and therefore a shoreline term that is *present and permanently zero* — [`adr/0123`](0123-desirability-ships-without-its-only-positive-term-and-a-caveat-that-must-travel-gets-a-test.md) refused exactly that, and without this check a Ruleset could reintroduce it with one keystroke.
  - 🔴 ⚠ **THE RECOUNT BULLETS ABOVE ARE A HISTORY AND NOT AN ARITHMETIC SEQUENCE, AND THE MERGE OF 2026-08-24 IS WHY.** `milestone-24-terrain-scoping` and `main` each recounted against their own corpus and neither could see the other's guards, so **both reused the ordinals *thirteenth* through *seventeenth*** and **both landed on 150 sites for entirely different keys**. The ordinals here have been renumbered into one sequence; **the totals inside each bullet have not**, because each was true of the branch it was taken on. ***A running total taken on a branch does not compose across a merge***, so read a bullet for **what it added and why**, and read the machine-readable figure below for **how many there are**. ⚠ This is `plans/0002` §F2's own defect — *a count maintained on two branches is two facts* — landing in a second document on the same day, which is what says the shape is general rather than that file's.
  - ⚠ **RECOUNTED A TWENTY-FIFTH TIME 2026-08-24 at the merge: 170 sites**, which is exactly `157 + 153 − 140` — the two branch totals less their common ancestor — so **the union is clean and no guard was lost or double-counted**. Nothing was added here; the number moved because two sets of guards met. ⚠ **One thing WAS changed and it is a correction rather than a guard**: the unknown-section message named eighteen tables on one branch and nineteen on the other and **neither named `[water]`**, a real section since milestone 24 task 6a — so an author who mistyped it was told it is not a Ruleset section at all. It is in the list now, and it costs no `Refuse(` site because the refusal was always there and only its *message* was wrong.
  - ⚠ **RECOUNTED A TWENTY-NINTH TIME 2026-08-26 by milestone 17 task 3: 180 sites, up THREE**, and all three guard the ONE new key `[[building]] sheds_occupant_after_days` — `CONTEXT.md` → Failure Pressure's first threshold. **Only one of them is a range check.** The other two are *relations between two keys*, which is the class this ADR exists for: a non-positive duration is refused as a value; the key **stated without `condemn_after_days`** is refused because shedding is paced off a pressure nothing would ever read; and the key **at or above `condemn_after_days`** is refused because the premises verdict is taken first, so a Building is abandoned before it could shed anything. 🔴 **That third one is the interesting refusal and no range check could catch it**: both numbers are individually valid and the file loads clean, sheds nobody, and looks exactly like a world whose Buildings simply never got that far — ***a key authored into inertness reads identically to a key that is working***, which is `adr/0130`'s reason for refusing a stated zero arriving as a relation rather than as a range. **RECOUNTED A TWENTY-EIGHTH TIME 2026-08-26 by milestone 17's threshold split: 177 sites, up TWO**, and the pair is one key **renamed with its unit changed**. `[[building]] condemn_after` counted firings a starved Rule may miss; `condemn_after_days` is a duration in Days. 🔴 **The rename's refusal is the interesting one and it guards nothing a range check would catch**: the two units differ by the Rule's own `rate` — a factor of **16** on the shipped `upkeep` — so a file that kept the old key and its old value would load clean and decline sixteen times too slowly, ***which is a change nobody would attribute to a rename***. A silent unit change is `plans/0012` **Cause 5** arriving in content rather than in prose, so the loader names the old key, says the unit moved, and refuses. The second site is the negative-value guard on the new `tenancy_ends_after_days`, which is `adr/0141`'s tenant threshold given a number of its own. ⚠ **Why the split needed a refusal at all**: one key drove both verdicts, so stripping decline from a world also stopped every tenancy in it ending — and `rulesets/evicted.toml`, the one file whose entire purpose is a tenancy that ends, ***silently stopped demonstrating it with every test green except its own.***
  - ⚠ **RECOUNTED A TWENTY-SEVENTH TIME 2026-08-25 by milestone 17 task 2: 175 sites, up THREE**, and all three guard one new key — `[[building]] collapses_after_days`, the sink for abandoned stock. **Two of them are a COUPLING rather than a range check**, which is what makes them worth enumerating: `condemn_after` without `collapses_after_days` is refused, and `collapses_after_days` without `condemn_after` is refused. ***A kind that can be abandoned and cannot collapse is a collection with an inflow and no sink*** — [`adr/0006`](0006-no-collection-grows-with-elapsed-time.md) — and the third refuses a non-positive duration, which would collapse the shell on the sweep that found it and leave the abandoned state with no observable extent. ⚠ **This is `adr/0130`'s disposition for `gives_up_after_days` arriving on the other collection abandonment fills**, and the reason it is at the parse site rather than in an invariant is this ADR's own: the alternative is a city that discovers it at Tick 100,000, which is exactly how milestone 17 task 1 found the problem — **19 tests in eight subsystems, a land value field peaking at zero and a divide-by-zero in the placement pass.** ***A Ruleset that can author `adr/0006` is a Ruleset the loader should refuse.***
  - 🔴 ⚠ **RECOUNTED A TWENTY-SIXTH TIME 2026-08-25 by `plans/0041` G31: 172 sites, up TWO — and the two of them close the ONE CLASS OF AUTHORING MISTAKE THIS LOADER COULD NOT SEE.** A key nothing reads was passed over in silence, so ***a typo and an omission were the same file to this parser***: the reader asked for the name it wanted, did not find it, and used the default. One site refuses a key no reader asked its table for; the other refuses a key above the first section header, which belongs to no table at all. ⚠ **G31's own proposed fix was not taken and the reason is in this ADR's own history.** The finding says close it by "every table declaring its permitted key set" — twenty-two lists hand-written beside twenty-two readers. ***A hand-authored name list in this file is a thing that drifts***, and the bullet above records exactly that happening to the unknown-*section* list at the merge the day before. So the permitted set is instead **derived**: `Find` records every key it is *asked* for, and what nothing asked for is refused. Adding a key to a reader adds it to the permission in the same edit. 🔴 ⚠ **IT FOUND FOUR STRANDED KEYS IN ALL EIGHTEEN SHIPPED RULESETS ON ITS FIRST RUN, AND THEY HAD BEEN STRANDED SINCE THE DAY THEY WERE WRITTEN** — `noise_range_metres`, `noise_intensity_percent`, `desirability_pollution_percent` and `desirability_noise_percent` sat ABOVE the `[layers]` header, inside `[placement]` (or `[founding]` in two files), from milestone 9 task 3 until 2026-08-25. ⚠ **Nothing observable ever moved and that is why nothing caught it**: every file authored exactly the loader's default, so re-homing all eighteen moved three Ruleset *content* fingerprints and ***not one State Hash***. **The cost was never a wrong city — it was that a designer retuning `noise_intensity_percent` in a shipped file would have had NO EFFECT and NO MESSAGE**, which is the whole of what `adr/0015` promises. ⚠ **A nineteenth stranded key, `fertility_pollution_percent` in `varied.toml`, was written during milestone 24 and had the same defect**, which is the point: this was not one old mistake but a shape the format admitted, and it was still being made sixteen days ago. ⚠ **One refusal MESSAGE was also wrong and it was teaching the mistake**: the unquoted-decimal refusal advised writing `decline_rate = "0.15"`, and `decline_rate` is not a key of `[[building]]` — so a designer following the advice in front of them wrote a line that did nothing. The example no longer names a key.
  - ⚠ **RECOUNTED A TWENTY-SEVENTH TIME 2026-08-26 by `06` milestone 26 task 6: 175 sites, up THREE.** All over `[[zone_rule]]`'s two new keys, `build_threshold_days` and `cooldown_days`, which are [`0163`](0163-demand-for-a-shop-is-elapsed-unserved-need-in-reach-and-building-claims-it.md)'s tier-1 demand signal as Ruleset data. Two are ordinary bottom-of-range checks — a threshold below one Day is a threshold every District clears, which is the tier-0 signal wearing a number; a negative cooldown is not a duration. 🔴 **The third is the one worth its line: a `cooldown_days` stated WITHOUT a `build_threshold_days` is refused.** The cooldown damps how fast a District may raise premises, and a rule that does not read demand raises on its ordinary sample regardless — so the key would **load clean and do nothing for ever**, this ADR's own class, and it would do it in the file of an author who had just written down the one number that actually moves the shop count. ⚠ **The converse is deliberately NOT refused**: a threshold with no cooldown is a live configuration — it means *raise whenever demand justifies it* — and `plans/0044` **F46** measures that world at **11 shops against the cooldown's 4**. ***A pair where one half implies the other and the other does not imply the one gets a refusal in one direction only.***
  - 🔴 ⚠ **RECOUNTED A THIRTIETH TIME 2026-08-27 at the merge of `milestone-17-decline-and-cleared-land`: 183 sites**, which is exactly `180 + 175 − 172` — the two branch totals less their common ancestor — so **the union is clean and no guard was lost or double-counted**. Nothing was added here; the merge is the occasion. ⚠ **TWO BULLETS ABOVE BOTH SAY *TWENTY-SEVENTH* AND BOTH SAY *175*, and neither is wrong**: milestone 17 task 2 counted 175 on 2026-08-25 against its own corpus and milestone 26 task 6 counted 175 on 2026-08-26 against `main`'s, and the two 175s are different sets of the same size. ***That is the clearest demonstration this file has of its own standing warning*** — the recount bullets are a **history and not an arithmetic sequence** — because here the history forked and rejoined, and an ordinal allocated on a branch is a number allocated twice for exactly the reason `plans/0002`'s ADR-count row now records at length. **Left as they stand rather than renumbered**: they are what each session honestly saw.
  - ⚠ **RECOUNTED A TWENTY-EIGHTH TIME 2026-08-27 by `plans/0045` item 6: 179 sites, up FOUR.** All over `[[business]]`'s `wage_per_day` and `pay_period_days`: a wage without `jobs`, one key without the other, a negative rate, a period below one.
  - ⚠ **RECOUNTED A THIRTY-FIRST TIME 2026-08-27 at the merge of `main` into `milestone-17-decline-and-cleared-land`: 187 sites**, which is exactly `183 + 179 − 175` — the two branch totals less their common ancestor — so **the union is clean and no guard was lost or double-counted**. Nothing was added here; the merge is the occasion. ⚠ **The figure was COUNTED and the arithmetic only checked against it afterwards**, which is the order this file's own standing warning requires: `grep -c "Refuse(" RulesetLoader.cs` reads 188 against one `void Refuse(` declaration. ***Quote the census, never the arithmetic.***
  - ⚠ **RECOUNTED AGAIN 2026-08-31 by [`plans/0045`](../../plans/0045-amnesty.md) queue item 9: 220 sites** — **three, all over `[[life_stage]]`'s centrality pair** ([`adr/0027`](0027-preference-is-drawn-per-household-and-persists-for-life.md)). Two are ordinary range checks on a percent. ⚠ **The third is the one worth its line, and it is a refusal NEITHER KEY CAN BE CHECKED FOR ALONE**: a base of 80 with a width of 40 puts the top of the band at 120%, past the end of the axis — a Household wanting the centre harder than the centre exists. ***A pair of keys that are each individually legal can still not add up***, which is `[households]`' opening-balance band arriving on a different quantity. ⚠ **What this pair does NOT get is a required-together refusal**, and the contrast with `spread_days` two keys up is the point: `W` is required there because its zero is one of two real cities and a defaulted `W` would silently pick one, where the neutral centrality pair is not a city at all — it is the placement this build already had. ***A default is dangerous when zero is an answer and harmless when absence is.***
  - ⚠ **RECOUNTED 2026-08-31 by [`plans/0045`](../../plans/0045-amnesty.md) queue item 12: 217 sites** — the three new ones are `[disasters]`': a second table, a schedule with no floodplain to happen on, and a non-positive duration. **Recounted 2026-08-30 by queue item 10 at 214.** **Seven, all over the ATTENDED Services** ([`adr/0032`](0032-services-are-delivered-by-trips-not-by-coverage.md)). **Five are over `[[building]] serves`**, whose whole job is to say what a Building is attended *for*: a Need a Household **buys** rather than attends, a **Dispatched** service, a **Networked** one, `recreation` — which is Attended and is an Amenity rather than a Household scalar — and an unknown value. ⚠ **All five are refused by name and none falls through to a generic message**, because `adr/0070` turns on the difference and *not a Need* reads as *not yet*. **The other two are the conditional pair**: an `education_degrade` in a file declaring no serving kind, and a serving kind in a file stating no rate, which is [`adr/0130`](0130-the-pools-bound-is-a-duration-and-the-unhoused-channel-ships-with-the-gate.md)'s rule for `gives_up_after_days` one table along. 🔴 **And the fifth bullet below is now WRONG about its own message**: `[[resource]] need = "education"` is still refused by name, and the reason changed from *undesigned* to *wrong door* — `docs/deferred.md` named the trigger and this queue item was it.
  - ⚠ **RECOUNTED 2026-08-30 by [`plans/0045`](../../plans/0045-amnesty.md) queue item 9: 207 sites.** **Five, all over Needs** (`adr/0103`). **Four are over `[needs]`**, whose five keys are all-or-nothing on `[parking]`'s precedent: a second table, a missing key, a **non-positive step** — which is the table omitted written the long way — and a **floor at or above zero**, which would mean no Household can ever be in deficit while the file appears to configure hunger. **The fifth is the one worth its line.** A `[[resource]] need` naming `education` or `health` is refused **BY NAME**, and the message says ***undesigned***. ⚠ **A generic *not a Need* would tell the designer the opposite of the truth**: those two ARE two of `adr/0103`'s four, and `adr/0070` turns on the difference between *undesigned* and *refused* — only the second is evidence. ***A refusal that miscategorises an absence is how somebody later reasons from it***, which is the first time in this enumeration that a refusal's **wording** was the design decision rather than its existence.
  - ⚠ **RECOUNTED 2026-08-31 by [`plans/0045`](../../plans/0045-amnesty.md) queue item 13: 222 sites, up TWO** — both over `[[building]] abandoned_when_empty_after_days`, the dwelling stock's demand-side sink. One is an ordinary bottom-of-range check, and it is not quite ordinary: **zero is refused rather than meaning *never*, because `adr/0069` has construction house nobody** — so a zero would abandon every Building on the sweep after it was raised, and *omit the key* is how a kind says it stands empty for ever. ⚠ **The second is a refusal NEITHER KEY CAN BE CHECKED FOR ALONE and it lives in `ReadPlacement`**: an empty clock shorter than `[placement] revisit_ticks` gives a Building a lifetime drawn from the **cadence** rather than from the file, because the placement pass is the only thing that fills one and `revisit_ticks` is authored as how long that pass takes to look at everybody waiting once. ***A duration is only a duration if the mechanism that would relieve it has had a turn.*** ⚠ **The existing `collapses_after_days` pair check became a three-way one in the same edit** — two keys can now abandon a Building, so the sink is owed if **either** is stated and refused only when **neither** is.
  - ⚠ **RECOUNTED 2026-09-02 by [`plans/0053`](../../plans/0053-the-block.md) step 2: 223 sites, up SIX** — all over `[[band]] admits`, `adr/0025`'s density band arriving as Ruleset data. Three are ordinary: a non-integer entry, a bit outside `0..15`, and a table stating no `admits` at all. ⚠ **The other three are all the same refusal wearing three faces** — a bit admitted twice, an `admits` that is not an array, and **an `admits` that is empty** — and the last is the one worth its line. ***An empty cap is not a permissive band and it is not a restrictive one; it is land nobody may ever build on***, which the Ruleset already expresses by not zoning it. So the key would load clean and produce a city with a hole in it that no sentence anywhere accounts for, which is the class this ADR exists for. **The permissive answer lives one level up instead**: a band nobody *declared* admits everything, because a cap is applied by intersection and the identity for a bandless world has to be all-bits-set.
  - 🔴 **RECOUNTED 2026-09-02 by [`plans/0052`](../../plans/0052-the-parcel.md) stage 1: 222 sites, DOWN ONE — and no refusal was lost.** `[[building]] footprint_tiles` is **retired**: a Building now covers its Lot's **parcel**, which is a partition of the ground the player drew. Its range check went, and the key gained a `RefuseRetired` — which records the retirement and refuses in one call and therefore counts as **no** `Refuse(` site. ⚠ **So this is the second time the number has gone DOWN while the loader got stricter**, for exactly the reason [`plans/0050`](../../plans/0050-the-ruleset-sweep.md) recorded the first: ***the count counts sites because a site is a fact***, and a retirement is one call where a refusal is one call plus a sentence somewhere else. **The named-refusal total is now under-represented by eight.**
  - ⚠ **RECOUNTED 2026-09-02 by [`plans/0053`](../../plans/0053-the-block.md): 223 sites, up ONE.** `[lots] setback_tiles` — how much ground a Building leaves on each side of its parcel — is refused outside `[0, block_tiles / 2]`. ⚠ **Half the block, because two setbacks meet**: a parcel is at most a block deep, so a setback past half of one leaves **no footprint on any parcel in the world** and the city seals nothing while raising Buildings. 🔴 **Zero is ALLOWED and is not the absence** — it means every Building covers its whole parcel, which is what the simulation did before the key existed while the shell drew something smaller; ***the key arrived because those two were the same quantity and disagreed***, so absence is refused and the file has to say which city it wants.
  - 🔴 **RECOUNTED 2026-09-02 by [`plans/0053`](../../plans/0053-the-block.md) step 3: 223 sites, UNCHANGED — and three refusals went out while three came in.** ***This is the first recount where the number is right and every reason behind it moved***, which is exactly the failure the enumeration exists to catch: a site count is a fact and a swap is invisible to it. **Out**, all three into `RefuseRetired` — `[[building]] occupants`, `jobs` and `parking`. A Building's capacity is **derived from the ground it stands on** now: its floor area over `[capacity] floor_tiles_per_occupant`, `floor_tiles_per_job` and `floor_tiles_per_parking_space`. ***A kind stopped declaring how many it holds and now declares only WHAT IT DOES.*** **In**: a second `[capacity]` table (a city holds one set of rates); one shared range check over all three rates — at least 1 Tile and at most a Cell, ⚠ **with a stated zero refused and absence the way to mean *none***, because a rate of zero divides by nothing and a key written to mean *none* reads as an author who thought they were setting a quantity; and one over the two new truth keys `[[building]] tenanted` and `parked`. ⚠ **The truth keys are a SHAPE this loader had not had** — every other key answers *how much* — so `false` and an absent key are the same city by construction, which is the property that makes the split writable at all.
  - ⚠ **RECOUNTED 2026-09-03 by [`plans/0054`](../../plans/0054-the-kind.md) F3: 224 sites, up ONE** — `[capacity] floor_tiles_per_place`, the ceiling on how many may attend a service Building in a Day. Its range check is the shared one the other three rates already had, so the site is the **pair** check: ***the rate is refused in a Ruleset where no `[[building]]` declares `serves`.*** ⚠ **It cannot live in `ReadCapacity`**, which runs FIRST because `ReadBusinessKinds` asks it whether anybody is employed — so the kinds are not read yet, and the check sits beside `RefuseUnpricedGoods` where both halves are in hand. 🔴 **This key needs the refusal more than any other in the table, because ITS ABSENCE IS INVISIBLE BY DESIGN**: the other three are supplies and an absent one is a city with none of that thing, while this one is a ceiling and an absent one is a city where nothing is ever full. So an inert statement of it would sit on the page reading as a bound this world has, and no run would ever say otherwise. It is `TryAttendedRates`' refusal from the other side — that one refuses an attended Need's *rates* where nothing serves the Need; this refuses the *ceiling* on how many may attend where nothing is attended at all.
  - ⚠ **RECOUNTED 2026-09-03 by [`plans/0058`](../../plans/0058-the-plot-ratio.md): 229 sites, up one.** The new refusal is `[lots] pattern_spread` **outside 0 to `BlockPatterns.Count - 1`** — how many rungs either side of its band's own rung a block's pattern may be drawn. ⚠ **The ceiling is the interesting half**: a spread reaching the whole ladder makes every band draw uniformly from every form, which is a city with no density bands in it — ***and that city is already writeable by declaring no `[[band]]`***, so the refusal is `plans/0002`'s *two spellings of one city* rather than a range check. The floor is 0 and is deliberately **not** refused, because 0 is the absent value and a file has to be able to say *no scatter here* out loud rather than by omission.
  - 🔴 **RECOUNTED 2026-09-03 BY THE MERGE ITSELF: 225 sites, up ONE — and NEITHER BRANCH'S NUMBER WAS RIGHT FOR THE MERGED TREE.** `main` stood at **218** having added `[[building]] rent`; `the-parcel` stood at **224** having added `[[band]] admits`, `[lots] setback_tiles` and `[capacity] floor_tiles_per_place` and retired four keys into `RefuseRetired`. ***Three numbers, none of them wrong where it was written***, which is the sentence this file already carries about the merge of 2026-08-24 — and it is the second time. ⚠ **The refusal that survives from `main` is `rent` below zero**: what a Household pays per Day to live in a Building of this kind, so a negative one is a rent you are paid to live in, which is `parking`'s reasoning on a quantity that has a sign. 🔴 **What caught it is `RefusalCountTests` and NOT a reader**: the merge was textually clean at this bullet, both sides' prose was true of their own tree, and a human resolving it would have kept one number and been wrong either way. ***A count that a test recomputes is the only kind that survives a merge***, which is what this bullet's own last sentence has been claiming since it was written. ⚠ **And the count was not the only thing the merge broke silently** — `tests/Borough.Tests/Rules/PlacementTests.cs` auto-merged with **no conflict** and would not compile, because `main`'s three new `rent` tests set `KindDefinition.Occupants` and `plans/0053` had retired it on the other side. ***Git had nothing to say about it because neither branch touched the other's lines.*** The compiler caught that one; nothing but a test could have caught this one.
  - ⚠ **RECOUNTED 2026-09-03 BY THE SECOND MERGE OF THE SAME DAY: 227 sites, up TWO** — both from `main`'s `[placement] reconsider_ticks`, the period over which the reassessment sweep checks every housed Household once. One is an ordinary bottom-of-range check, and its refusal names the spelling that was meant: ***omit the key to skip reassessment entirely***, so zero is not how a file says never. ⚠ **The second is a refusal NEITHER KEY CAN BE CHECKED FOR ALONE**: a period shorter than the `interval` it is delivered in. ***A period spread over triggers is not a period if it is shorter than one trigger*** — the sweep would be asked to cover the population in less time than passes between its own firings — which is `revisit_ticks` against the same `interval` arriving on a second consumer of the same table. 🔴 **AND THIS BULLET WAS WRONG AGAIN FOR THE SAME REASON, ONE MERGE LATER**: the number the file carried was 225, true of the tree it was written on and of no other. ***A count of record recomputed by a test is a count of record; one maintained by a reader is a count of the last tree that reader saw.*** ⚠ **AND THE ENUMERATION NEARLY WENT IN WRONG** — the first draft of this bullet described the second refusal as *a cadence stated where no kind declares a rent*, from memory of what the sweep is for. Reading `RulesetLoader.cs` says otherwise. ***That is [`adr/0093`](0093-a-description-of-the-build-is-where-to-look-and-never-what-you-found.md) happening inside the ADR whose enumeration exists to prevent it.***
  - ⚠ **RECOUNTED 2026-09-03 by [`plans/0056`](../../plans/0056-the-ladder.md): 228 sites, up ONE.** `[lots] storeys_per_rung` — how many storeys one step up the density ladder is worth — is refused outside `[1, 50]`. ⚠ **The floor is 1 and not 0, and the refusal names the spelling that was meant**: a flat ladder is a city that declares one `[[band]]`, so a step of zero is a second way to write a city that is already writable, and ***two spellings of one city is a key doing nothing while reading as a mechanism.*** ⚠ **The ceiling is the COLUMN and not a design bound** — `LotTable.Storeys` is a byte, and 50 gives `(4 × 50) + 2 + 50 = 252` on the tallest rung. 🔴 **It is the first refusal in this loader whose absence is what the whole corpus had been treating as a decision**: the key is optional and absent means 1, which is the step the ladder had when nobody had chosen one — and that step set the tallest Building any world could hold at **seven storeys**, on every world at every population, stated nowhere.
  - ⚠ **This is now the only copy of the number, and a test holds it to the build.** `adr/0015` and `plans/0003`'s gate board carried their own and cited this one as the record, which is three copies of a fact and `plans/0012` *Cause 1* by construction; both now cite and neither states. ***The cheapest way to stop two copies drifting is to have one copy.*** What no amount of citing fixes is a single copy going stale against the code, which is what happened twice — so `RefusalCountTests` reads `RulesetLoader.cs`, counts its `Refuse(` call sites, and holds this ADR to the total. **The count of record's machine-readable half: refusal sites in `RulesetLoader.cs`: **229**.** 🔴 **IT FELL 222 → 217 ON 2026-09-01 WITHOUT A REFUSAL BEING LOST, WHICH IS THE FIRST TIME THIS NUMBER HAS GONE DOWN.** [`plans/0050`](../../plans/0050-the-ruleset-sweep.md) found eight keys the loader refuses **by name** — `condemn_after`, `sample`, `wage`, `storage`, `[layers] sealing_decay_tau` and `adr/0148`'s three — being published to every editor as valid completions by the schema generated from the loader's own key surface. The repair routes all eight through one `RefuseRetired`, which records the retirement **and** refuses in a single call, ***because a retirement and its refusal written apart are two things that drift***. Six call sites became one and the eight sentences are unchanged. ⚠ **So the count now under-represents named refusals by seven**, and that is the instrument working rather than failing: it counts sites because a site is a fact, and it went red the moment the shape of the code moved. That is *sites*, which is a fact; the fifty-nine above is the **semantic** subset, which is a judgement under the *loads clean and misbehaves in silence* rule three bullets down, and a judgement cannot be a test. The site count moves the day a guard is written, which is the day the enumeration above can still be extended from memory. ⚠ **⚠ **BOTH BRANCHES RECOUNTED THIS AS *THE THIRTEENTH* ON DIFFERENT DAYS AND NEITHER COULD SEE THE OTHER**, so the merge of 2026-08-24 found 143 on one side, 140 on the other and **145** in the merged loader — ***three numbers, none of them wrong where it was written.*** The semantic subset above is owed a recount and has not had one; it is a judgement rather than a fact, so no test holds it. It is the corpus's first document-to-*code* check** — the other eight all read one document against another, which is precisely why a number describing the build could drift 36 with every test green.
  - *The 2026-08-11 correction, kept:* ⚠ **CORRECTED 2026-08-11, and it had been stale by seven before the correction's own occasion added four.** `adr/0053` and `adr/0068` added two over `[[building]]` that were never counted at all, `adr/0059` turned *a sample of zero* into three refusals over `revisit_ticks`, `adr/0069` added three over `[placement]`, and milestone 5a added four over `[roads]`. **Four ADRs moved this number and none came back to the sentence** — which is the failure this bullet names in its own last line, happening to the bullet. *Original text:* **There are now eleven at load and a twelfth on reload, and this bullet is the count of record.** Slice 7 added an **unbalanced money** refusal (`adr/0024`, when the Resource family was taken out of order) and an **unterminated chain** refusal (task 8, enforcing `0045`'s own reporting terminal). Slice 10 task 3 added three over `[[zone_rule]]`: an **undeclared kind**, a **permission bit no `zone` verb can paint**, and a **sample of zero**. The number is stated here, in `0015`, and in `0003`'s gate board, and all three had drifted apart by task 8 — which is `plans/0012`'s *Cause 1* in the ADR that owns the surface.
  - **Slice 8 task 3 added three more of that same class, over `[layers]`**, and they are what a Map Layer's version of *loads clean and does nothing* looks like: a **period of zero**, which freezes the field; an **offset outside its period**, which has no solution to `tick % period == offset` so the Layer is never recomputed; and a **decay that rounds to never**, where a duration shorter than the cadence it is counted in divides to a tau of 0 and 0 is the sentinel for *never* — a file that reads *fades in a minute* and behaves as *never fades*. The last of the three exists only because the tau is a **derivation** rather than a literal, which is `plans/0015`'s decision owed 2; a literal would have had no second number to be inconsistent with.
  - **The twelfth runs on a second entry point and is the first refusal that is not a property of the file alone.** `0015`'s world-creation constants are read from the Ruleset and frozen when a world is created, so *whether a value changed* is only knowable against the world it would be applied to. `RulesetLoader.Reload(text, file, frozen)` takes the world's frozen constants — a handful of integers, which cross the boundary under this ADR's own rule — and refuses a change. **It compares the effective value, not the authored one**: the kernel is written in metres and used in Cells, and two authored figures that round to one Cell count build the same kernel, so refusing between them would refuse a reload that reinterprets nothing. That comparison *is* `0015`'s membership test rather than an approximation of it.
  - **The argument of that entry point is one number where the category enumerates four, and that is a finding.** See `plans/0012` — `0015` says its world-creation members *"live in the Ruleset like everything else and are read from it"*, and after slice 8 task 3 that is true of the kernel radius alone. `TICKS_PER_DAY`, `WHEEL_SIZE` and the Cell are `const`s no Ruleset can state, so a refusal over them would be a check with nothing on the other side of it. Each joins the signature on the day it becomes authorable.
  - **The nine added by `plans/0046` stage 3 guard the fertility decision** — an inverted or negative child band, a band with no `childless` successor and a `childless` successor with no band (each half alone is a stage nothing reaches), an adult age band outside `1..65535`, and a stage naming an undeclared stage or *itself*. 🔴 **One of the nine is `adr/0006` arriving at a door that did not exist when its refusal was written.** `gives_up_after_days` was required of a **gate** kind because a Pool with a door and no sink grows without bound; ***internal generation is a second door***, so the obligation was written as a property of gates and is a property of anything that fills the Pool. See `RefuseIncompleteGeneration`.
  - **The six added by `plans/0046` stage 1 are one class, and the class is *a stage table that loads clean and produces a city with no demography*.** A `duration_days` below 1 is a stage entered and left on the same Day, so a chain of them collapses a whole life into one Tick. A `spread_days` below 0 is a window that runs backwards — ⚠ **zero is allowed and is the lockstep world `adr/0011`'s `W` exists to prevent**, which is why it is required rather than defaulted: a key whose zero is a real answer must not be chosen silently by the loader. A `next` naming an undeclared stage is a chain that stops. A `next` naming *itself* is a Household that transitions for ever and arrives back where it started — indistinguishable from a typo, and terminal is spelled by omitting the key. And a stage whose draw reaches `EventWheel.CoarseDays` would throw at whichever Household happened to draw the top of its window, which is ***a Ruleset defect reported as an engine crash***. **The bound belongs at load** because the wheel's period and the stage table would otherwise drift apart with nothing checking.
  - **What is *not* counted, and the line is worth stating because it is where the number would drift next.** A duplicate `[layers]`, an unknown section name and a duplicate `[[resource]]` name are all refused and none of them is in the count: they are ambiguities a designer sees immediately. The count is of refusals that catch a Ruleset which would otherwise **load clean and misbehave in silence**, which is what every member of it has in common and what makes it worth stating at all.
  - **The three added by slice 10 are one class rather than three, which is why they were worth stating separately.** Each describes a Zone Rule that loads clean, triggers on schedule for ever, and builds nothing — the same symptom from three causes, and a symptom no author would recognise as a Ruleset defect. That is the `apply = {min=1,max=4}` behaving as `{1,1}` failure generalised: a silent narrowing is indistinguishable from a quiet design decision, so the loader has to be the thing that tells them apart.
- **`02 §4.3`'s *"requires no parser"* is struck.** It is the sentence that hid the dependency.
- **`Borough.Formats` gains a package reference and stays lint-free by design.** Nothing changes in `Borough.Core.csproj`.
- **The core's interpreter carries one cheap assertion** — an unknown id is refused — and no defensive checking beyond it.
- **`CONTEXT.md` → Ruleset gains the boundary**: it is validated where it is parsed, and refused rather than warned about.
- **A Ruleset is TOML-shaped rather than idiomatic TOML**, and that is the price paid. Anybody reading `= "0.15"` and reaching for the quotes should find this ADR.

## What would trigger revisiting

- **A tuning number that cannot be expressed as an integer or a decimal string.** A curve, a table of pairs, an expression. TOML arrays cover more of this than it first appears; something that genuinely does not fit is the trigger for the DSL already parked in [`deferred.md`](../deferred.md), not for loosening this rule.
- **Tomlyn becoming unmaintained, or its integer parsing proving culture-sensitive.** The second is testable today and should be tested when the loader is written rather than assumed — under [`0043`](0043-a-claim-a-measurement-could-settle-must-not-be-settled-by-argument.md) that claim is **measurable** and the machine that settles it is a unit test with a non-invariant culture set.
- **The drift assertion firing in practice.** If `Formats` repeatedly accepts what `Core` refuses, the single-validator claim is wrong and the rules want a shared declaration rather than two implementations.
- **A designer study finding the quote marks are a real obstacle.** This decision rests on them being trivial. That is an assumption about people, not about machines, and the acceptance test is `0015`'s: does changing a ratio still take seconds.
